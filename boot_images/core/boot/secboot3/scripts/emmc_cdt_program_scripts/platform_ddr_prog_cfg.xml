<?xml version="1.0"?>
<configuration>
  <options>
    <!-- NOTE: Defaults are shown here -->

    <!-- Replace MPRG8x10.mbn with the appropriate target -->

    VERBOSE_FEEDBACK        = true
    SEARCH_CWD_LAST         = true
    HEX_PROGRAMMER          = MPRG8916.mbn
  </options>

  <search_paths>
    <!-- NOTE: Search paths IN ORDER, as in first look here, then there, then there etc -->
    <!-- NOTE: CWD is an implied search path, and it is always last -->
  </search_paths>

  <rawprogram>
    rawprogram2.xml
  </rawprogram>

  <patch>
    patch2.xml
  </patch>
  
</configuration>
