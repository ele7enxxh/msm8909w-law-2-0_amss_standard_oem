
/* Build this component into a library */

#include <lib.qbd>
rule hdrmac_c_compile : c_compile
{
command compiler += -DBUILD_ASIC=""; 
command compiler += -DBUILD_TARGET=""; 
command compiler += -DBUILD_VER="";
}

