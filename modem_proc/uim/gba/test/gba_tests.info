# On each line:
# The format is as follows:
#    [Test search string]:tag1,tag2,tag3,...,tagN,
# First parameter must be the string to be used
# to search for test names.
# The rest are the tags associated with those tests.
# Line must be ended with a comma.
#
.*GBA.*:uim,gba,
.*_CSIM.*:csim,
.*_SIM.*:sim,
.*_USIM.*:usim,
.*_RUIM.*:ruim,
