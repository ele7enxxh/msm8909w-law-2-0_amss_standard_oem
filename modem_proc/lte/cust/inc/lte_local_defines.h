#ifndef LTE_LOCAL_DEFINES_H
#define LTE_LOCAL_DEFINES_H

/* includes for local cust files
 * Note - these files are only visible to lte.  Any new FEATURE used by multiple SUs must be 
 * provisioned in the FEATURE database, after which it will be included in the <su>_variation.h 
 * file generated for each SUs that uses the FEATURE. */
#include "custlte.h"

#endif /* LTE_LOCAL_DEFINES_H */
