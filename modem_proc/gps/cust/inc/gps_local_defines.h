#ifndef GPS_LOCAL_DEFINES_H
#define GPS_LOCAL_DEFINES_H

/* includes for local cust files
 * Note - these files are only visible to gps.  Any new FEATURE used by multiple SUs must be 
 * provisioned in the FEATURE database, after which it will be included in the <su>_variation.h 
 * file generated for each SUs that uses the FEATURE. */
#include "custcgps.h"

#endif /* GPS_LOCAL_DEFINES_H */
