/* NV TOOL VERSION: 4.37 */
/* GENERATED: MON JUN  7 2010 */
/*=============================================================================
                                N V _ S V C . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that dispatches RPC requests targetting the
  nv api.

  ---------------------------------------------------------------------------
  Copyright (c) 2010 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */
/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/htorpc.pl#8 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Start.pm#3 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Htoxdr.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/XDR.pm#3 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Output.pm#7 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Parser.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Metacomments.pm#5 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/SymbolTable.pm#3   

nv Definition File(s):
Id: //source/qcom/qct/core/services/nv/meta/main/latest/inc/nv.h#5
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/services/src/nv_svc.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "nv.h"
#include "nv_rpc.h"

/* Only one copy needed per API */
//static opaque_auth NvCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth NvVerf = { ONCRPC_AUTH_NONE, 0, 0 };

/*=======================================================================
             Prototypes for the API's RPC Server Functions
=======================================================================*/

static void nvprog_0x00090002 ( struct svc_req *rqstp, xdr_s_type *srv );
static void nv_null_0x00090002( xdr_s_type *svc );
static void nv_rpc_glue_code_info_remote_0x00090002( xdr_s_type *svc );
static void nv_api_versions_0x00090002( xdr_s_type *srv );

static void nv_read_err_log_0x00090002( xdr_s_type *srv );

static void nv_write_err_log_0x00090002( xdr_s_type *srv );

static void nv_max_size_of_roaming_list_0x00090002( xdr_s_type *srv );

static void nv_prl_is_valid_0x00090002( xdr_s_type *srv );

static void nv_built_0x00090002( xdr_s_type *srv );

static void nv_prl_version_0x00090002( xdr_s_type *srv );

static void nv_determine_esn_validity_0x00090002( xdr_s_type *srv );

static void nv_cmd_remote_0x00090002( xdr_s_type *srv );

static void nv_cmd_ext_remote_0x00090002( xdr_s_type *srv );

static void nv_rtre_control_0x00090002( xdr_s_type *srv );

static void nv_rtre_use_ruim_for_nam_0x00090002( xdr_s_type *srv );

static void nv_rtre_polling_control_0x00090002( xdr_s_type *srv );

static void nv_rtre_polling_for_nam_0x00090002( xdr_s_type *srv );

/******************************************************************************/
/*=======================================================================
             API RPC Server Implementation
=======================================================================*/
static uint32 nv_api_versions_table[] = {  0x00090001 , 0x00090002  };

uint32 * nv_api_versions(uint32 *size_entries)
{
  *size_entries = sizeof( nv_api_versions_table) / sizeof(uint32);
  return nv_api_versions_table;
}

void nv_app_init( void )
{
  (void) svc_register_auto_apiversions(NVPROG, NVVERS, nvprog_0x00090002, 
                                       nv_api_versions);
} /* nv_app_init */

void nv_app_lock( boolean lock )
{
  svc_lock( NVPROG, NVVERS, lock );
} /* nv_app_lock */

static void nvprog_0x00090002 ( struct svc_req *rqstp, xdr_s_type *srv )
{
  switch ( rqstp->rq_proc ) {
    case ONCRPC_NV_NULL_PROC:
      nv_null_0x00090002( srv );
      break;
    case ONCRPC_NV_RPC_GLUE_CODE_INFO_REMOTE_PROC:
      nv_rpc_glue_code_info_remote_0x00090002( srv );
      break;
    case ONCRPC_NV_READ_ERR_LOG_PROC:
      nv_read_err_log_0x00090002( srv );
      break;
    case ONCRPC_NV_WRITE_ERR_LOG_PROC:
      nv_write_err_log_0x00090002( srv );
      break;
    case ONCRPC_NV_MAX_SIZE_OF_ROAMING_LIST_PROC:
      nv_max_size_of_roaming_list_0x00090002( srv );
      break;
    case ONCRPC_NV_PRL_IS_VALID_PROC:
      nv_prl_is_valid_0x00090002( srv );
      break;
    case ONCRPC_NV_BUILT_PROC:
      nv_built_0x00090002( srv );
      break;
    case ONCRPC_NV_PRL_VERSION_PROC:
      nv_prl_version_0x00090002( srv );
      break;
    case ONCRPC_NV_DETERMINE_ESN_VALIDITY_PROC:
      nv_determine_esn_validity_0x00090002( srv );
      break;
    case ONCRPC_NV_CMD_REMOTE_PROC:
      nv_cmd_remote_0x00090002( srv );
      break;
    case ONCRPC_NV_CMD_EXT_REMOTE_PROC:
      nv_cmd_ext_remote_0x00090002( srv );
      break;
    case ONCRPC_NV_RTRE_CONTROL_PROC:
      nv_rtre_control_0x00090002( srv );
      break;
    case ONCRPC_NV_RTRE_USE_RUIM_FOR_NAM_PROC:
      nv_rtre_use_ruim_for_nam_0x00090002( srv );
      break;
    case ONCRPC_NV_RTRE_POLLING_CONTROL_PROC:
      nv_rtre_polling_control_0x00090002( srv );
      break;
    case ONCRPC_NV_RTRE_POLLING_FOR_NAM_PROC:
      nv_rtre_polling_for_nam_0x00090002( srv );
      break;
    case ONCRPC_NV_API_VERSIONS_PROC:
      nv_api_versions_0x00090002( srv );
      break;

    default:
      // invalid RPC procedure number
      (void) XDR_MSG_DONE( srv );
      svcerr_default_err( srv, rqstp, nv_api_versions  );
      break;
  }

  oncrpcxdr_mem_free( srv );
} /* nvprog_0x00090002 */

/******************************************************************************/

static void nv_null_0x00090002( xdr_s_type *srv )
{
  if ( ! XDR_MSG_DONE( srv ) ) {
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  if ( ! xdr_reply_msg_start( srv, &NvVerf ) ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_null_0x00090002 */

static void nv_rpc_glue_code_info_remote_0x00090002( xdr_s_type *srv )
{
  uint32 toolvers   = NV_TOOLVERS; /* 4.37 */
  uint32 proghash   = 0x00090002; /* 0x00090002 */
  uint32 cbproghash = 0x00090002; /* 0x00090002 */
  uint32 features   = NV_FEATURES; /* ONCRPC Server Cleanup Support */
                      

  if ( ! XDR_MSG_DONE( srv ) ) {
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  if ( ! xdr_reply_msg_start( srv, &NvVerf ) ||
       ! XDR_SEND_UINT32( srv, &toolvers ) ||
       ! XDR_SEND_UINT32( srv, &features ) ||
       ! XDR_SEND_UINT32( srv, &proghash ) ||
       ! XDR_SEND_UINT32( srv, &cbproghash ) )
  {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_rpc_glue_code_info_remote_0x00090002 */

static void nv_read_err_log_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  nv_err_log_type *nv_err_log_ptr = NULL;

  nv_stat_enum_type nv_read_err_log_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  XDR_RECV_POINTER( srv, &nv_err_log_ptr, xdr_nv_recv_nv_err_log_type, xdr_status );

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_read_err_log_result = nv_read_err_log(nv_err_log_ptr);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &nv_read_err_log_result );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    XDR_SEND_POINTER( srv, &nv_err_log_ptr, xdr_nv_send_nv_err_log_type, xdr_status );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_read_err_log_0x00090002 */

static void nv_write_err_log_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  nv_err_log_type *nv_err_log_ptr = NULL;

  nv_stat_enum_type nv_write_err_log_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  XDR_RECV_POINTER( srv, &nv_err_log_ptr, xdr_nv_recv_nv_err_log_type, xdr_status );

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_write_err_log_result = nv_write_err_log(nv_err_log_ptr);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &nv_write_err_log_result );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    XDR_SEND_POINTER( srv, &nv_err_log_ptr, xdr_nv_send_nv_err_log_type, xdr_status );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_write_err_log_0x00090002 */

static void nv_max_size_of_roaming_list_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  word nv_max_size_of_roaming_list_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_max_size_of_roaming_list_result = nv_max_size_of_roaming_list();

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_UINT16( srv, &nv_max_size_of_roaming_list_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_max_size_of_roaming_list_0x00090002 */

static void nv_prl_is_valid_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  byte nam = 0;

  boolean nv_prl_is_valid_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT8( srv, &nam );

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_prl_is_valid_result = nv_prl_is_valid(nam);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_BOOLEAN( srv, &nv_prl_is_valid_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_prl_is_valid_0x00090002 */

static void nv_built_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  boolean nv_built_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_built_result = nv_built();

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_BOOLEAN( srv, &nv_built_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_built_0x00090002 */

static void nv_prl_version_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  byte nam = 0;

  word nv_prl_version_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT8( srv, &nam );

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_prl_version_result = nv_prl_version(nam);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_UINT16( srv, &nv_prl_version_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_prl_version_0x00090002 */

static void nv_determine_esn_validity_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  boolean nv_determine_esn_validity_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_determine_esn_validity_result = nv_determine_esn_validity();

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_BOOLEAN( srv, &nv_determine_esn_validity_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_determine_esn_validity_0x00090002 */

static void nv_cmd_remote_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  nv_func_enum_type cmd = NV_READ_F;
  nv_items_enum_type item = NV_ESN_I;
  nv_item_type *data_ptr = NULL;

  nv_stat_enum_type nv_cmd_remote_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_ENUM( srv, &cmd );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( srv, &item );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_RECV_U_POINTER2( srv, &data_ptr, &(item), xdr_nv_recv_nv_item_type, XDR_RECV_ENUM, xdr_status );
    /*lint -restore */
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_cmd_remote_result = nv_cmd_remote(cmd, item, data_ptr);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &nv_cmd_remote_result );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_SEND_U_POINTER2( srv, &data_ptr, &(item), xdr_nv_send_nv_item_type, XDR_SEND_ENUM, xdr_status );
    /*lint -restore */
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_cmd_remote_0x00090002 */

static void nv_cmd_ext_remote_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  nv_func_enum_type cmd = NV_READ_F;
  nv_items_enum_type item = NV_ESN_I;
  nv_item_type *data_ptr = NULL;
  uint16 context = 0;

  nv_stat_enum_type nv_cmd_ext_remote_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_ENUM( srv, &cmd );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( srv, &item );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_RECV_U_POINTER2( srv, &data_ptr, &(item), xdr_nv_recv_nv_item_type, XDR_RECV_ENUM, xdr_status );
    /*lint -restore */
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT16( srv, &context );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_cmd_ext_remote_result = nv_cmd_ext_remote(cmd, item, data_ptr, context);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &nv_cmd_ext_remote_result );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_SEND_U_POINTER2( srv, &data_ptr, &(item), xdr_nv_send_nv_item_type, XDR_SEND_ENUM, xdr_status );
    /*lint -restore */
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_cmd_ext_remote_0x00090002 */

static void nv_rtre_control_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  nv_rtre_control_type nv_rtre_control_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_rtre_control_result = nv_rtre_control();

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_UINT16( srv, &nv_rtre_control_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_rtre_control_0x00090002 */

static void nv_rtre_use_ruim_for_nam_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  byte nam = 0;

  boolean nv_rtre_use_ruim_for_nam_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT8( srv, &nam );

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_rtre_use_ruim_for_nam_result = nv_rtre_use_ruim_for_nam(nam);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_BOOLEAN( srv, &nv_rtre_use_ruim_for_nam_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_rtre_use_ruim_for_nam_0x00090002 */

static void nv_rtre_polling_control_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  nv_rtre_polling_control_type nv_rtre_polling_control_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_rtre_polling_control_result = nv_rtre_polling_control();

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_UINT16( srv, &nv_rtre_polling_control_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_rtre_polling_control_0x00090002 */

static void nv_rtre_polling_for_nam_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  byte nam = 0;

  boolean nv_rtre_polling_for_nam_result;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT8( srv, &nam );

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_rtre_polling_for_nam_result = nv_rtre_polling_for_nam(nam);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_BOOLEAN( srv, &nv_rtre_polling_for_nam_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_rtre_polling_for_nam_0x00090002 */

/*=========================================================================== 
              API Standard function for api versioning
===========================================================================*/

static void nv_api_versions_0x00090002( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  uint32 i;
  boolean output_pointer_not_null;
  uint32 length_uint32;
  uint32 *len = NULL;

  uint32 *nv_api_versions_result = NULL;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  /* The server must know whether to allocate memory for the output parameter
   * len or not. A boolean is received to indicate that.
   */
  xdr_status = XDR_RECV_UINT8( srv, &output_pointer_not_null );

  if ( xdr_status && output_pointer_not_null ) {
    len = oncrpcxdr_mem_alloc( srv, sizeof(*len) );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  nv_api_versions_result = nv_api_versions(len);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &NvVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    if ( nv_api_versions_result != NULL ) {
      length_uint32 = *len;

      xdr_status = XDR_SEND_UINT32( srv, &length_uint32 );

      /* XDR OP NUMBER = 3 */
      if ( xdr_status )
      {
        xdr_op_number  = 3;

        /* Calling array of XDR routines */
        for ( i = 0; xdr_status && i < (length_uint32); i++ ) {
          /*lint -save -e545*/
          xdr_status = XDR_SEND_UINT32( srv, &(nv_api_versions_result[i]) );
          /*lint -restore */
        }
      }
    } else {
      length_uint32 = 0;

      xdr_status = XDR_SEND_UINT32( srv, &length_uint32 );
    }
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_SEND_POINTER( srv, &len, XDR_SEND_UINT32, xdr_status );
    /*lint -restore */
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* nv_api_versions_0x00090002 */

