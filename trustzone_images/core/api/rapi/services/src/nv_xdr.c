/* NV TOOL VERSION: 4.37 */
/* GENERATED: MON JUN  7 2010 */
/*=============================================================================
                                N V _ X D R . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that provides xdr encode/decode routines
  specific to the data types used in the exported nv API.

  ---------------------------------------------------------------------------
  Copyright (c) 2010 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */

/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/htorpc.pl#8 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Start.pm#3 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Htoxdr.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/XDR.pm#3 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Output.pm#7 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Parser.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Metacomments.pm#5 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/SymbolTable.pm#3   

nv Definition File(s):
Id: //source/qcom/qct/core/services/nv/meta/main/latest/inc/nv.h#5
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/services/src/nv_xdr.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "nv.h"
#include "nv_rpc.h"


/*=======================================================================
                  XDR send/recv routines for API types
=======================================================================*/


/*
 * nv_minmax_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_minmax_type (xdr_s_type *xdrs, nv_minmax_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->min) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->max) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_minmax_type */

/*
 * nv_minmax_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_minmax_type (xdr_s_type *xdrs, nv_minmax_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->min) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->max) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_minmax_type */




/*
 * nv_sec_code_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sec_code_type (xdr_s_type *xdrs, nv_sec_code_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sec_code_type */

/*
 * nv_sec_code_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sec_code_type (xdr_s_type *xdrs, nv_sec_code_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sec_code_type */




/*
 * nv_enabled_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_enabled_type (xdr_s_type *xdrs, nv_enabled_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->enabled) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_enabled_type */

/*
 * nv_enabled_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_enabled_type (xdr_s_type *xdrs, nv_enabled_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->enabled) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_enabled_type */




/*
 * nv_a_key_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_a_key_type (xdr_s_type *xdrs, nv_a_key_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->key[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_a_key_type */

/*
 * nv_a_key_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_a_key_type (xdr_s_type *xdrs, nv_a_key_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->key[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_a_key_type */




/*
 * nv_a_key_chksum_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_a_key_chksum_type (xdr_s_type *xdrs, nv_a_key_chksum_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_a_key_chksum_type */

/*
 * nv_a_key_chksum_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_a_key_chksum_type (xdr_s_type *xdrs, nv_a_key_chksum_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_a_key_chksum_type */




/*
 * nv_ssd_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ssd_type (xdr_s_type *xdrs, nv_ssd_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->ssd[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_ssd_type */

/*
 * nv_ssd_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ssd_type (xdr_s_type *xdrs, nv_ssd_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->ssd[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ssd_type */




/*
 * nv_pap_user_id_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_pap_user_id_type (xdr_s_type *xdrs, nv_pap_user_id_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->user_id_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->user_id, 64) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_pap_user_id_type */

/*
 * nv_pap_user_id_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_pap_user_id_type (xdr_s_type *xdrs, nv_pap_user_id_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->user_id_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->user_id, 64) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_pap_user_id_type */




/*
 * nv_pap_password_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_pap_password_type (xdr_s_type *xdrs, nv_pap_password_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->password, 14) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_pap_password_type */

/*
 * nv_pap_password_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_pap_password_type (xdr_s_type *xdrs, nv_pap_password_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->password, 14) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_pap_password_type */




/*
 * nv_ppp_password_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ppp_password_type (xdr_s_type *xdrs, nv_ppp_password_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->password, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ppp_password_type */

/*
 * nv_ppp_password_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ppp_password_type (xdr_s_type *xdrs, nv_ppp_password_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->password, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ppp_password_type */




/*
 * nv_ppp_user_id_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ppp_user_id_type (xdr_s_type *xdrs, nv_ppp_user_id_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->user_id_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->user_id, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ppp_user_id_type */

/*
 * nv_ppp_user_id_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ppp_user_id_type (xdr_s_type *xdrs, nv_ppp_user_id_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->user_id_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->user_id, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ppp_user_id_type */




/*
 * nv_sec_device_key_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sec_device_key_type (xdr_s_type *xdrs, nv_sec_device_key_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->device_key_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->device_key_data, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sec_device_key_type */

/*
 * nv_sec_device_key_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sec_device_key_type (xdr_s_type *xdrs, nv_sec_device_key_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->device_key_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->device_key_data, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sec_device_key_type */




/*
 * nv_esn_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_esn_type (xdr_s_type *xdrs, nv_esn_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->esn) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_esn_type */

/*
 * nv_esn_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_esn_type (xdr_s_type *xdrs, nv_esn_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->esn) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_esn_type */




/*
 * nv_esn_chksum_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_esn_chksum_type (xdr_s_type *xdrs, nv_esn_chksum_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_esn_chksum_type */

/*
 * nv_esn_chksum_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_esn_chksum_type (xdr_s_type *xdrs, nv_esn_chksum_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_esn_chksum_type */




/*
 * nv_config_chksum_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_config_chksum_type (xdr_s_type *xdrs, nv_config_chksum_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_config_chksum_type */

/*
 * nv_config_chksum_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_config_chksum_type (xdr_s_type *xdrs, nv_config_chksum_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_config_chksum_type */




/*
 * nv_nam_chksum_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_nam_chksum_type (xdr_s_type *xdrs, nv_nam_chksum_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_nam_chksum_type */

/*
 * nv_nam_chksum_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_nam_chksum_type (xdr_s_type *xdrs, nv_nam_chksum_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_nam_chksum_type */




/*
 * nv_run_time_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_run_time_type (xdr_s_type *xdrs, nv_run_time_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->time) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_run_time_type */

/*
 * nv_run_time_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_run_time_type (xdr_s_type *xdrs, nv_run_time_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->time) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_run_time_type */




/*
 * nv_err_log_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_err_log_type (xdr_s_type *xdrs, nv_err_log_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->err_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->file_name, 8) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->line_num) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->fatal) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_err_log_type */

/*
 * nv_err_log_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_err_log_type (xdr_s_type *xdrs, nv_err_log_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->err_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->file_name, 8) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->line_num) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->fatal) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_err_log_type */




/*
 * nv_fsc_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_fsc_type (xdr_s_type *xdrs, nv_fsc_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->fsc, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_fsc_type */

/*
 * nv_fsc_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_fsc_type (xdr_s_type *xdrs, nv_fsc_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->fsc, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_fsc_type */




/*
 * nv_fsc2_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_fsc2_type (xdr_s_type *xdrs, nv_fsc2_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->fsc2) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_fsc2_type */

/*
 * nv_fsc2_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_fsc2_type (xdr_s_type *xdrs, nv_fsc2_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->fsc2) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_fsc2_type */




/*
 * nv_fsc2_chksum_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_fsc2_chksum_type (xdr_s_type *xdrs, nv_fsc2_chksum_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->fsc2_chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_fsc2_chksum_type */

/*
 * nv_fsc2_chksum_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_fsc2_chksum_type (xdr_s_type *xdrs, nv_fsc2_chksum_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->fsc2_chksum) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_fsc2_chksum_type */




/*
 * nv_wdc_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_wdc_type (xdr_s_type *xdrs, nv_wdc_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->wdc, 5) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->extra, 3) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_wdc_type */

/*
 * nv_wdc_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_wdc_type (xdr_s_type *xdrs, nv_wdc_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->wdc, 5) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->extra, 3) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_wdc_type */




/*
 * nv_up_key_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_up_key_type (xdr_s_type *xdrs, nv_up_key_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->key, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_up_key_type */

/*
 * nv_up_key_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_up_key_type (xdr_s_type *xdrs, nv_up_key_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->key, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_up_key_type */




/*
 * nv_sid_lock_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sid_lock_type (xdr_s_type *xdrs, nv_sid_lock_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (6); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->sid[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_sid_lock_type */

/*
 * nv_sid_lock_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sid_lock_type (xdr_s_type *xdrs, nv_sid_lock_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (6); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->sid[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sid_lock_type */




/*
 * nv_sid_acq_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sid_acq_type (xdr_s_type *xdrs, nv_sid_acq_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (6); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->sid[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_sid_acq_type */

/*
 * nv_sid_acq_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sid_acq_type (xdr_s_type *xdrs, nv_sid_acq_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (6); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->sid[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sid_acq_type */




/*
 * nv_min1_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_min1_type (xdr_s_type *xdrs, nv_min1_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->min1[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_min1_type */

/*
 * nv_min1_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_min1_type (xdr_s_type *xdrs, nv_min1_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->min1[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_min1_type */




/*
 * nv_min2_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_min2_type (xdr_s_type *xdrs, nv_min2_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->min2[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_min2_type */

/*
 * nv_min2_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_min2_type (xdr_s_type *xdrs, nv_min2_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->min2[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_min2_type */




/*
 * nv_firstchp_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_firstchp_type (xdr_s_type *xdrs, nv_firstchp_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->channel) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_firstchp_type */

/*
 * nv_firstchp_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_firstchp_type (xdr_s_type *xdrs, nv_firstchp_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->channel) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_firstchp_type */




/*
 * nv_sid_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sid_type (xdr_s_type *xdrs, nv_sid_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->sid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sid_type */

/*
 * nv_sid_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sid_type (xdr_s_type *xdrs, nv_sid_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->sid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sid_type */




/*
 * nv_analog_reg_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_analog_reg_type (xdr_s_type *xdrs, nv_analog_reg_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->kind) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_analog_reg_type */

/*
 * nv_analog_reg_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_analog_reg_type (xdr_s_type *xdrs, nv_analog_reg_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->kind) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_analog_reg_type */




/*
 * nv_cdmach_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_cdmach_type (xdr_s_type *xdrs, nv_cdmach_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->channel_a) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->channel_b) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_cdmach_type */

/*
 * nv_cdmach_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_cdmach_type (xdr_s_type *xdrs, nv_cdmach_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->channel_a) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->channel_b) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_cdmach_type */




/*
 * nv_mob_term_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_mob_term_type (xdr_s_type *xdrs, nv_mob_term_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->enabled[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_mob_term_type */

/*
 * nv_mob_term_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_mob_term_type (xdr_s_type *xdrs, nv_mob_term_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->enabled[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_mob_term_type */




/*
 * nv_accolc_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_accolc_type (xdr_s_type *xdrs, nv_accolc_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ACCOLCpClass, 2) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_accolc_type */

/*
 * nv_accolc_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_accolc_type (xdr_s_type *xdrs, nv_accolc_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ACCOLCpClass, 2) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_accolc_type */




/*
 * nv_call_time_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_call_time_type (xdr_s_type *xdrs, nv_call_time_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->time) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_call_time_type */

/*
 * nv_call_time_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_call_time_type (xdr_s_type *xdrs, nv_call_time_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->time) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_call_time_type */




/*
 * nv_sid_nid_pair_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sid_nid_pair_type (xdr_s_type *xdrs, nv_sid_nid_pair_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->sid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->nid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sid_nid_pair_type */

/*
 * nv_sid_nid_pair_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sid_nid_pair_type (xdr_s_type *xdrs, nv_sid_nid_pair_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->sid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->nid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sid_nid_pair_type */




/*
 * nv_home_sid_nid_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_home_sid_nid_type (xdr_s_type *xdrs, nv_home_sid_nid_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (20); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_send_nv_sid_nid_pair_type( xdrs, &(sptr->pair[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_home_sid_nid_type */

/*
 * nv_home_sid_nid_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_home_sid_nid_type (xdr_s_type *xdrs, nv_home_sid_nid_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (20); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_recv_nv_sid_nid_pair_type( xdrs, &(sptr->pair[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_home_sid_nid_type */




/*
 * nv_sid_nid_lock_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sid_nid_lock_type (xdr_s_type *xdrs, nv_sid_nid_lock_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (10); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_send_nv_sid_nid_pair_type( xdrs, &(sptr->pair[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_sid_nid_lock_type */

/*
 * nv_sid_nid_lock_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sid_nid_lock_type (xdr_s_type *xdrs, nv_sid_nid_lock_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (10); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_recv_nv_sid_nid_pair_type( xdrs, &(sptr->pair[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sid_nid_lock_type */




/*
 * nv_pref_voice_so_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_pref_voice_so_type (xdr_s_type *xdrs, nv_pref_voice_so_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->evrc_capability_enabled) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->home_page_voice_so) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->home_orig_voice_so) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->roam_orig_voice_so) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_pref_voice_so_type */

/*
 * nv_pref_voice_so_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_pref_voice_so_type (xdr_s_type *xdrs, nv_pref_voice_so_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->evrc_capability_enabled) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->home_page_voice_so) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->home_orig_voice_so) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->roam_orig_voice_so) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_pref_voice_so_type */




/*
 * nv_imsi_mcc_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_imsi_mcc_type (xdr_s_type *xdrs, nv_imsi_mcc_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->imsi_mcc) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_imsi_mcc_type */

/*
 * nv_imsi_mcc_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_imsi_mcc_type (xdr_s_type *xdrs, nv_imsi_mcc_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->imsi_mcc) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_imsi_mcc_type */




/*
 * nv_imsi_11_12_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_imsi_11_12_type (xdr_s_type *xdrs, nv_imsi_11_12_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->imsi_11_12) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_imsi_11_12_type */

/*
 * nv_imsi_11_12_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_imsi_11_12_type (xdr_s_type *xdrs, nv_imsi_11_12_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->imsi_11_12) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_imsi_11_12_type */




/*
 * nv_imsi_addr_num_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_imsi_addr_num_type (xdr_s_type *xdrs, nv_imsi_addr_num_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_imsi_addr_num_type */

/*
 * nv_imsi_addr_num_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_imsi_addr_num_type (xdr_s_type *xdrs, nv_imsi_addr_num_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_imsi_addr_num_type */




/*
 * nv_dir_number_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_dir_number_type (xdr_s_type *xdrs, nv_dir_number_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->dir_number, 10) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_dir_number_type */

/*
 * nv_dir_number_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_dir_number_type (xdr_s_type *xdrs, nv_dir_number_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->dir_number, 10) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_dir_number_type */




/*
 * nv_mob_dir_data_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_mob_dir_data_type (xdr_s_type *xdrs, nv_mob_dir_data_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->n_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digitn, 15) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_mob_dir_data_type */

/*
 * nv_mob_dir_data_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_mob_dir_data_type (xdr_s_type *xdrs, nv_mob_dir_data_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->n_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digitn, 15) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_mob_dir_data_type */




/*
 * nv_roaming_list_union_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_roaming_list_union_type (xdr_s_type *xdrs, nv_roaming_list_union_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->prl_version) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->size) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->valid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->roaming_list, 1) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_roaming_list_union_type */

/*
 * nv_roaming_list_union_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_roaming_list_union_type (xdr_s_type *xdrs, nv_roaming_list_union_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->prl_version) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->size) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->valid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->roaming_list, 1) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_roaming_list_union_type */




/*
 * nv_pref_mode_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_pref_mode_type (xdr_s_type *xdrs, nv_pref_mode_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->mode) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_pref_mode_type */

/*
 * nv_pref_mode_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_pref_mode_type (xdr_s_type *xdrs, nv_pref_mode_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->mode) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_pref_mode_type */




/*
 * nv_count_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_count_type (xdr_s_type *xdrs, nv_count_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->count) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_count_type */

/*
 * nv_count_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_count_type (xdr_s_type *xdrs, nv_count_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->count) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_count_type */




/*
 * struct_nv_sid_nid_type_pair_arr XDR_NV_SEND
 */
boolean xdr_nv_send_struct_nv_sid_nid_type_pair_arr (xdr_s_type *xdrs, nv_sid_nid_pair_type  const (*objp)[1])
{
  int i;
  /* Calling array of XDR routines */
  for ( i = 0; i < (1); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_send_nv_sid_nid_pair_type( xdrs, &((*objp)[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_struct_nv_sid_nid_type_pair_arr */

/*
 * struct_nv_sid_nid_type_pair_arr XDR_NV_RECV
 */
boolean xdr_nv_recv_struct_nv_sid_nid_type_pair_arr (xdr_s_type *xdrs, nv_sid_nid_pair_type (*objp)[1])
{
  int i;
  /* Calling array of XDR routines */
  for ( i = 0; i < (1); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_recv_nv_sid_nid_pair_type( xdrs, &((*objp)[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_struct_nv_sid_nid_type_pair_arr */




/*
 * nv_sid_nid_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sid_nid_type (xdr_s_type *xdrs, nv_sid_nid_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_send_struct_nv_sid_nid_type_pair_arr( xdrs, &(sptr->pair[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_sid_nid_type */

/*
 * nv_sid_nid_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sid_nid_type (xdr_s_type *xdrs, nv_sid_nid_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_recv_struct_nv_sid_nid_type_pair_arr( xdrs, &(sptr->pair[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sid_nid_type */




/*
 * nv_min_chksum_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_min_chksum_type (xdr_s_type *xdrs, nv_min_chksum_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->chksum[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_min_chksum_type */

/*
 * nv_min_chksum_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_min_chksum_type (xdr_s_type *xdrs, nv_min_chksum_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->chksum[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_min_chksum_type */




/*
 * nv_name_nam_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_name_nam_type (xdr_s_type *xdrs, nv_name_nam_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->name, 12) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_name_nam_type */

/*
 * nv_name_nam_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_name_nam_type (xdr_s_type *xdrs, nv_name_nam_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->name, 12) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_name_nam_type */




/*
 * nv_sys_pref_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sys_pref_type (xdr_s_type *xdrs, nv_sys_pref_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->sys) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sys_pref_type */

/*
 * nv_sys_pref_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sys_pref_type (xdr_s_type *xdrs, nv_sys_pref_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->sys) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sys_pref_type */




/*
 * nv_data_pkt_orig_str_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_data_pkt_orig_str_type (xdr_s_type *xdrs, nv_data_pkt_orig_str_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_data_pkt_orig_str_type */

/*
 * nv_data_pkt_orig_str_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_data_pkt_orig_str_type (xdr_s_type *xdrs, nv_data_pkt_orig_str_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_data_pkt_orig_str_type */




/*
 * nv_ds_mip_gen_user_prof_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_mip_gen_user_prof_type (xdr_s_type *xdrs, nv_ds_mip_gen_user_prof_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->nai, 72) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->mn_ha_spi_set) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->mn_ha_spi) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->mn_aaa_spi_set) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->mn_aaa_spi) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->rev_tun_pref) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->home_addr) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->primary_ha_addr) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->secondary_ha_addr) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_mip_gen_user_prof_type */

/*
 * nv_ds_mip_gen_user_prof_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_mip_gen_user_prof_type (xdr_s_type *xdrs, nv_ds_mip_gen_user_prof_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->nai, 72) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->mn_ha_spi_set) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->mn_ha_spi) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->mn_aaa_spi_set) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->mn_aaa_spi) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->rev_tun_pref) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->home_addr) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->primary_ha_addr) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->secondary_ha_addr) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_mip_gen_user_prof_type */




/*
 * nv_ds_mip_ss_user_prof_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_mip_ss_user_prof_type (xdr_s_type *xdrs, nv_ds_mip_ss_user_prof_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->mn_ha_shared_secret_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->mn_ha_shared_secret, 16) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->mn_aaa_shared_secret_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->mn_aaa_shared_secret, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_mip_ss_user_prof_type */

/*
 * nv_ds_mip_ss_user_prof_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_mip_ss_user_prof_type (xdr_s_type *xdrs, nv_ds_mip_ss_user_prof_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->mn_ha_shared_secret_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->mn_ha_shared_secret, 16) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->mn_aaa_shared_secret_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->mn_aaa_shared_secret, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_mip_ss_user_prof_type */




/*
 * nv_ds_mip_mn_ha_time_delta_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_mip_mn_ha_time_delta_type (xdr_s_type *xdrs, nv_ds_mip_mn_ha_time_delta_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->time_delta) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_mip_mn_ha_time_delta_type */

/*
 * nv_ds_mip_mn_ha_time_delta_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_mip_mn_ha_time_delta_type (xdr_s_type *xdrs, nv_ds_mip_mn_ha_time_delta_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->time_delta) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_mip_mn_ha_time_delta_type */




/*
 * nv_ds_mip_dmu_pkoid_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_mip_dmu_pkoid_type (xdr_s_type *xdrs, nv_ds_mip_dmu_pkoid_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->pkoid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_mip_dmu_pkoid_type */

/*
 * nv_ds_mip_dmu_pkoid_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_mip_dmu_pkoid_type (xdr_s_type *xdrs, nv_ds_mip_dmu_pkoid_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->pkoid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_mip_dmu_pkoid_type */




/*
 * nv_ds_mip_dmu_mn_auth_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_mip_dmu_mn_auth_type (xdr_s_type *xdrs, nv_ds_mip_dmu_mn_auth_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->mn_auth, 3) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_mip_dmu_mn_auth_type */

/*
 * nv_ds_mip_dmu_mn_auth_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_mip_dmu_mn_auth_type (xdr_s_type *xdrs, nv_ds_mip_dmu_mn_auth_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->mn_auth, 3) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_mip_dmu_mn_auth_type */




/*
 * nv_rtsp_proxy_server_addr XDR_NV_SEND
 */
boolean xdr_nv_send_nv_rtsp_proxy_server_addr (xdr_s_type *xdrs, nv_rtsp_proxy_server_addr const *uptr, unsigned short  obj_disc)
{
  int i;
  switch ( obj_disc ){
 case NV_IPV4_ADDR_MASK:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->v4) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_ADDR_MASK:

      /* Calling array of XDR routines */
      for ( i = 0; i < (2); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT64( xdrs, &(uptr->v6[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
    case NV_ADDR_INVALID:

      ; // case ignored
      break;
    default:

      ; // case ignored

  }

  return (TRUE);
} /* xdr_nv_send_nv_rtsp_proxy_server_addr */

/*
 * nv_rtsp_proxy_server_addr XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_rtsp_proxy_server_addr (xdr_s_type *xdrs, nv_rtsp_proxy_server_addr *uptr, unsigned short  obj_disc)
{
  int i;
  switch ( obj_disc ){
 case NV_IPV4_ADDR_MASK:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->v4) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_ADDR_MASK:

      /* Calling array of XDR routines */
      for ( i = 0; i < (2); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT64( xdrs, &(uptr->v6[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
    case NV_ADDR_INVALID:

      ; // case ignored
      break;
    default:

      ; // case ignored

  }

  return (TRUE);
} /* xdr_nv_recv_nv_rtsp_proxy_server_addr */




/*
 * nv_rtsp_proxy_server_addr_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_rtsp_proxy_server_addr_type (xdr_s_type *xdrs, nv_rtsp_proxy_server_addr_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->flags) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->port) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->flags) ) ) {
    return (FALSE);
  }

  if ( ! xdr_nv_send_nv_rtsp_proxy_server_addr( xdrs, &(sptr->addr), sptr->flags ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_rtsp_proxy_server_addr_type */

/*
 * nv_rtsp_proxy_server_addr_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_rtsp_proxy_server_addr_type (xdr_s_type *xdrs, nv_rtsp_proxy_server_addr_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->flags) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->port) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->flags) ) ) {
    return (FALSE);
  }

  if ( ! xdr_nv_recv_nv_rtsp_proxy_server_addr( xdrs, &(sptr->addr), sptr->flags ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_rtsp_proxy_server_addr_type */




/*
 * nv_pkt_dial_string_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_pkt_dial_string_type (xdr_s_type *xdrs, nv_pkt_dial_string_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->dial_string, 5) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->call_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->modes_permitted) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_pkt_dial_string_type */

/*
 * nv_pkt_dial_string_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_pkt_dial_string_type (xdr_s_type *xdrs, nv_pkt_dial_string_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->dial_string, 5) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->call_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->modes_permitted) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_pkt_dial_string_type */




/*
 * nv_ppp_configure_data_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ppp_configure_data_type (xdr_s_type *xdrs, nv_ppp_configure_data_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->lcp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->lcp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->lcp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->lcp_nak_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->lcp_term_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->auth_retry) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->auth_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->ipcp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->ipcp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipcp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipcp_nak_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipcp_term_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipcp_compression_enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->ipv6cp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->ipv6cp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipv6cp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipv6cp_nak_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipv6cp_term_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipv6cp_compression_enable) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ppp_configure_data_type */

/*
 * nv_ppp_configure_data_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ppp_configure_data_type (xdr_s_type *xdrs, nv_ppp_configure_data_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->lcp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->lcp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->lcp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->lcp_nak_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->lcp_term_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->auth_retry) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->auth_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->ipcp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->ipcp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipcp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipcp_nak_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipcp_term_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipcp_compression_enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->ipv6cp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->ipv6cp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipv6cp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipv6cp_nak_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipv6cp_term_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipv6cp_compression_enable) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ppp_configure_data_type */




/*
 * nv_ps_ipv6_iid_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ps_ipv6_iid_type (xdr_s_type *xdrs, nv_ps_ipv6_iid_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ipv6_iid_enum_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT64( xdrs, &(sptr->iid_val) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ps_ipv6_iid_type */

/*
 * nv_ps_ipv6_iid_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ps_ipv6_iid_type (xdr_s_type *xdrs, nv_ps_ipv6_iid_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ipv6_iid_enum_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT64( xdrs, &(sptr->iid_val) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ps_ipv6_iid_type */




/*
 * nv_ip6_sm_config_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ip6_sm_config_type (xdr_s_type *xdrs, nv_ip6_sm_config_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_INT16( xdrs, &(sptr->init_sol_delay) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_INT16( xdrs, &(sptr->sol_interval) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_INT16( xdrs, &(sptr->resol_interval) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_INT16( xdrs, &(sptr->max_sol_attempts) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_INT16( xdrs, &(sptr->max_resol_attempts) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_INT16( xdrs, &(sptr->pre_ra_exp_resol_time) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ip6_sm_config_type */

/*
 * nv_ip6_sm_config_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ip6_sm_config_type (xdr_s_type *xdrs, nv_ip6_sm_config_type *sptr)
{
  if ( ! XDR_RECV_PACKED_INT16( xdrs, &(sptr->init_sol_delay) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_INT16( xdrs, &(sptr->sol_interval) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_INT16( xdrs, &(sptr->resol_interval) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_INT16( xdrs, &(sptr->max_sol_attempts) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_INT16( xdrs, &(sptr->max_resol_attempts) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_INT16( xdrs, &(sptr->pre_ra_exp_resol_time) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ip6_sm_config_type */




/*
 * nv_ipv6_address_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ipv6_address_type (xdr_s_type *xdrs, nv_ipv6_address_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT64( xdrs, &(sptr->prefix) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT64( xdrs, &(sptr->iid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ipv6_address_type */

/*
 * nv_ipv6_address_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ipv6_address_type (xdr_s_type *xdrs, nv_ipv6_address_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT64( xdrs, &(sptr->prefix) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT64( xdrs, &(sptr->iid) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ipv6_address_type */




/*
 * nv_hdramp_address_data_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdramp_address_data_type (xdr_s_type *xdrs, nv_hdramp_address_data_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->session_seed) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->transmit_ati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->receive_mati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->receive_rati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->receive_current_uati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->receive_new_uati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->new_uati_valid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->uati, 16) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->uati_color_code) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->uati_subnet_mask) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdramp_address_data_type */

/*
 * nv_hdramp_address_data_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdramp_address_data_type (xdr_s_type *xdrs, nv_hdramp_address_data_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->session_seed) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->transmit_ati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->receive_mati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->receive_rati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->receive_current_uati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->receive_new_uati) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->new_uati_valid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->uati, 16) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->uati_color_code) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->uati_subnet_mask) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdramp_address_data_type */




/*
 * nv_hdr_an_auth_nai_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdr_an_auth_nai_type (xdr_s_type *xdrs, nv_hdr_an_auth_nai_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nai_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->nai, 64) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdr_an_auth_nai_type */

/*
 * nv_hdr_an_auth_nai_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdr_an_auth_nai_type (xdr_s_type *xdrs, nv_hdr_an_auth_nai_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nai_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->nai, 64) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdr_an_auth_nai_type */




/*
 * nv_hdr_an_auth_password_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdr_an_auth_password_type (xdr_s_type *xdrs, nv_hdr_an_auth_password_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->password, 14) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdr_an_auth_password_type */

/*
 * nv_hdr_an_auth_password_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdr_an_auth_password_type (xdr_s_type *xdrs, nv_hdr_an_auth_password_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->password, 14) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdr_an_auth_password_type */




/*
 * nv_hdr_an_ppp_password_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdr_an_ppp_password_type (xdr_s_type *xdrs, nv_hdr_an_ppp_password_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->password, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdr_an_ppp_password_type */

/*
 * nv_hdr_an_ppp_password_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdr_an_ppp_password_type (xdr_s_type *xdrs, nv_hdr_an_ppp_password_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->password, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdr_an_ppp_password_type */




/*
 * nv_hdr_an_auth_passwd_long_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdr_an_auth_passwd_long_type (xdr_s_type *xdrs, nv_hdr_an_auth_passwd_long_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->passwd_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->passwd, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdr_an_auth_passwd_long_type */

/*
 * nv_hdr_an_auth_passwd_long_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdr_an_auth_passwd_long_type (xdr_s_type *xdrs, nv_hdr_an_auth_passwd_long_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->passwd_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->passwd, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdr_an_auth_passwd_long_type */




/*
 * nv_hdr_an_auth_user_id_long_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdr_an_auth_user_id_long_type (xdr_s_type *xdrs, nv_hdr_an_auth_user_id_long_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->user_id_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->user_id, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdr_an_auth_user_id_long_type */

/*
 * nv_hdr_an_auth_user_id_long_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdr_an_auth_user_id_long_type (xdr_s_type *xdrs, nv_hdr_an_auth_user_id_long_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->user_id_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->user_id, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdr_an_auth_user_id_long_type */




/*
 * nv_sms_bc_service_table_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sms_bc_service_table_type (xdr_s_type *xdrs, nv_sms_bc_service_table_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->active_service) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->service) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->language) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->selected) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->priority) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->label, 30) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sms_bc_service_table_type */

/*
 * nv_sms_bc_service_table_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sms_bc_service_table_type (xdr_s_type *xdrs, nv_sms_bc_service_table_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->active_service) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->service) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->language) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->selected) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->priority) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->label, 30) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sms_bc_service_table_type */




/*
 * nv_sms_vm_number_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sms_vm_number_type (xdr_s_type *xdrs, nv_sms_vm_number_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sms_vm_number_type */

/*
 * nv_sms_vm_number_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sms_vm_number_type (xdr_s_type *xdrs, nv_sms_vm_number_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sms_vm_number_type */




/*
 * nv_sms_routing_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_sms_routing_type (xdr_s_type *xdrs, nv_sms_routing_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->pp_routes, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->pp_mem_stores, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->bc_routes, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->bc_mem_stores, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->transfer_status_report) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_sms_routing_type */

/*
 * nv_sms_routing_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_sms_routing_type (xdr_s_type *xdrs, nv_sms_routing_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->pp_routes, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->pp_mem_stores, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->bc_routes, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->bc_mem_stores, 6) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->transfer_status_report) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_sms_routing_type */




/*
 * nv_dial_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_dial_type (xdr_s_type *xdrs, nv_dial_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->status) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 32) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->letters, 12) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_dial_type */

/*
 * nv_dial_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_dial_type (xdr_s_type *xdrs, nv_dial_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->status) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 32) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->letters, 12) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_dial_type */




/*
 * nv_stdial_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_stdial_type (xdr_s_type *xdrs, nv_stdial_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->status) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->time_stamp) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 32) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->letters, 12) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->duration) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_stdial_type */

/*
 * nv_stdial_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_stdial_type (xdr_s_type *xdrs, nv_stdial_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->status) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->time_stamp) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 32) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->letters, 12) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->duration) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_stdial_type */




/*
 * nv_stack_idx_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_stack_idx_type (xdr_s_type *xdrs, nv_stack_idx_type const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->index, 20) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (20); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->secret[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_stack_idx_type */

/*
 * nv_stack_idx_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_stack_idx_type (xdr_s_type *xdrs, nv_stack_idx_type *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->index, 20) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (20); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->secret[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_stack_idx_type */




/*
 * nv_page_set_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_page_set_type (xdr_s_type *xdrs, nv_page_set_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->rings) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_page_set_type */

/*
 * nv_page_set_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_page_set_type (xdr_s_type *xdrs, nv_page_set_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->rings) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_page_set_type */




/*
 * nv_page_msg_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_page_msg_type (xdr_s_type *xdrs, nv_page_msg_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->time_stamp) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->newPage) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_chars) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->chars, 20) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_page_msg_type */

/*
 * nv_page_msg_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_page_msg_type (xdr_s_type *xdrs, nv_page_msg_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->address) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->time_stamp) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->newPage) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_chars) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->chars, 20) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_page_msg_type */




/*
 * nv_banner_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_banner_type (xdr_s_type *xdrs, nv_banner_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->letters, 12) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_banner_type */

/*
 * nv_banner_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_banner_type (xdr_s_type *xdrs, nv_banner_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->letters, 12) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_banner_type */




/*
 * nv_auto_answer_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_auto_answer_type (xdr_s_type *xdrs, nv_auto_answer_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->rings) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_auto_answer_type */

/*
 * nv_auto_answer_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_auto_answer_type (xdr_s_type *xdrs, nv_auto_answer_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->rings) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_auto_answer_type */




/*
 * nv_auto_redial_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_auto_redial_type (xdr_s_type *xdrs, nv_auto_redial_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->rings) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_auto_redial_type */

/*
 * nv_auto_redial_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_auto_redial_type (xdr_s_type *xdrs, nv_auto_redial_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->rings) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_auto_redial_type */




/*
 * nv_lock_code_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_lock_code_type (xdr_s_type *xdrs, nv_lock_code_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 4) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_lock_code_type */

/*
 * nv_lock_code_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_lock_code_type (xdr_s_type *xdrs, nv_lock_code_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 4) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_lock_code_type */




/*
 * nv_call_cnt_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_call_cnt_type (xdr_s_type *xdrs, nv_call_cnt_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->cnt) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_call_cnt_type */

/*
 * nv_call_cnt_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_call_cnt_type (xdr_s_type *xdrs, nv_call_cnt_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->cnt) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_call_cnt_type */




/*
 * nv_ruim_chv_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ruim_chv_type (xdr_s_type *xdrs, nv_ruim_chv_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->digits, 8) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ruim_chv_type */

/*
 * nv_ruim_chv_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ruim_chv_type (xdr_s_type *xdrs, nv_ruim_chv_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->digits, 8) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ruim_chv_type */




/*
 * nv_bd_addr_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_bd_addr_type (xdr_s_type *xdrs, nv_bd_addr_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->bd_addr, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_bd_addr_type */

/*
 * nv_bd_addr_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_bd_addr_type (xdr_s_type *xdrs, nv_bd_addr_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->bd_addr, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_bd_addr_type */




/*
 * nv_srv_domain_pref_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_srv_domain_pref_type (xdr_s_type *xdrs, nv_srv_domain_pref_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->srv_domain) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_srv_domain_pref_type */

/*
 * nv_srv_domain_pref_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_srv_domain_pref_type (xdr_s_type *xdrs, nv_srv_domain_pref_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nam) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->srv_domain) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_srv_domain_pref_type */




/*
 * nv_err_fatal_options_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_err_fatal_options_type (xdr_s_type *xdrs, nv_err_fatal_options_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->option) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_err_fatal_options_type */

/*
 * nv_err_fatal_options_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_err_fatal_options_type (xdr_s_type *xdrs, nv_err_fatal_options_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->option) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_err_fatal_options_type */




/*
 * nv_ue_imei_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ue_imei_type (xdr_s_type *xdrs, nv_ue_imei_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ue_imei, 9) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ue_imei_type */

/*
 * nv_ue_imei_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ue_imei_type (xdr_s_type *xdrs, nv_ue_imei_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ue_imei, 9) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ue_imei_type */




/*
 * nv_ds_mip_rm_nai_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_mip_rm_nai_type (xdr_s_type *xdrs, nv_ds_mip_rm_nai_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->nai, 72) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_mip_rm_nai_type */

/*
 * nv_ds_mip_rm_nai_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_mip_rm_nai_type (xdr_s_type *xdrs, nv_ds_mip_rm_nai_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->nai, 72) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_mip_rm_nai_type */




/*
 * nv_ds_sip_rm_nai_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_sip_rm_nai_type (xdr_s_type *xdrs, nv_ds_sip_rm_nai_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->nai, 72) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_sip_rm_nai_type */

/*
 * nv_ds_sip_rm_nai_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_sip_rm_nai_type (xdr_s_type *xdrs, nv_ds_sip_rm_nai_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->nai, 72) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_sip_rm_nai_type */




/*
 * nv_trm_config_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_trm_config_type (xdr_s_type *xdrs, nv_trm_config_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->rf_config) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->mask) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_trm_config_type */

/*
 * nv_trm_config_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_trm_config_type (xdr_s_type *xdrs, nv_trm_config_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->rf_config) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->mask) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_trm_config_type */




/*
 * nv_rmnet_autoconnect_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_rmnet_autoconnect_type (xdr_s_type *xdrs, nv_rmnet_autoconnect_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_rmnet_autoconnect_type */

/*
 * nv_rmnet_autoconnect_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_rmnet_autoconnect_type (xdr_s_type *xdrs, nv_rmnet_autoconnect_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_rmnet_autoconnect_type */




/*
 * nv_nitz_plmn_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_nitz_plmn_type (xdr_s_type *xdrs, nv_nitz_plmn_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->plmn_name, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_nitz_plmn_type */

/*
 * nv_nitz_plmn_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_nitz_plmn_type (xdr_s_type *xdrs, nv_nitz_plmn_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->plmn_name, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_nitz_plmn_type */




/*
 * nv_hdrkep_config_params_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdrkep_config_params_type (xdr_s_type *xdrs, nv_hdrkep_config_params_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->key_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->keys_are_valid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->rpc_auth_key, 20) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdrkep_config_params_type */

/*
 * nv_hdrkep_config_params_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdrkep_config_params_type (xdr_s_type *xdrs, nv_hdrkep_config_params_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->key_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->keys_are_valid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->rpc_auth_key, 20) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdrkep_config_params_type */




/*
 * nv_hdrscmdb_secure_config_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hdrscmdb_secure_config_type (xdr_s_type *xdrs, nv_hdrscmdb_secure_config_type const *sptr)
{
  int i;
  /* Calling array of XDR routines */
  for ( i = 0; i < (4); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_send_nv_hdrkep_config_params_type( xdrs, &(sptr->kep_config[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->hash, 20) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hdrscmdb_secure_config_type */

/*
 * nv_hdrscmdb_secure_config_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hdrscmdb_secure_config_type (xdr_s_type *xdrs, nv_hdrscmdb_secure_config_type *sptr)
{
  int i;
  /* Calling array of XDR routines */
  for ( i = 0; i < (4); i++ ) {
    /*lint -save -e545*/
    if ( ! xdr_nv_recv_nv_hdrkep_config_params_type( xdrs, &(sptr->kep_config[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->hash, 20) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hdrscmdb_secure_config_type */




/*
 * nv_gpsone_password_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_gpsone_password_type (xdr_s_type *xdrs, nv_gpsone_password_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->password, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_gpsone_password_type */

/*
 * nv_gpsone_password_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_gpsone_password_type (xdr_s_type *xdrs, nv_gpsone_password_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->password_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->password, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_gpsone_password_type */




/*
 * nv_dcvsapps_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_dcvsapps_type (xdr_s_type *xdrs, nv_dcvsapps_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->timer_period) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->lower_n) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->raise_n) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->high_watermark) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->low_watermark) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_dcvsapps_type */

/*
 * nv_dcvsapps_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_dcvsapps_type (xdr_s_type *xdrs, nv_dcvsapps_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->timer_period) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->lower_n) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->raise_n) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->high_watermark) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->low_watermark) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_dcvsapps_type */




/*
 * nv_ipv6_priv_addr_config XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ipv6_priv_addr_config (xdr_s_type *xdrs, nv_ipv6_priv_addr_config const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->ipv6_priv_preferred_lifetime) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->ipv6_priv_valid_lifetime) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ipv6_priv_addr_config */

/*
 * nv_ipv6_priv_addr_config XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ipv6_priv_addr_config (xdr_s_type *xdrs, nv_ipv6_priv_addr_config *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->ipv6_priv_preferred_lifetime) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->ipv6_priv_valid_lifetime) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ipv6_priv_addr_config */




/*
 * nv_nd_config_items XDR_NV_SEND
 */
boolean xdr_nv_send_nv_nd_config_items (xdr_s_type *xdrs, nv_nd_config_items const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->max_nd_solicits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->max_nd_retransmits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->max_nd_reachable_time) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->max_nd_delay_first_probe_time) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->max_nd_retransmit_interval) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_nd_config_items */

/*
 * nv_nd_config_items XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_nd_config_items (xdr_s_type *xdrs, nv_nd_config_items *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->max_nd_solicits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->max_nd_retransmits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->max_nd_reachable_time) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->max_nd_delay_first_probe_time) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->max_nd_retransmit_interval) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_nd_config_items */




/*
 * nv_jcdma_ruimid XDR_NV_SEND
 */
boolean xdr_nv_send_nv_jcdma_ruimid (xdr_s_type *xdrs, nv_jcdma_ruimid const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->ruim_id) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_jcdma_ruimid */

/*
 * nv_jcdma_ruimid XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_jcdma_ruimid (xdr_s_type *xdrs, nv_jcdma_ruimid *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->ruim_id) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_jcdma_ruimid */




/*
 * nv_jcdma_uim_lock_data XDR_NV_SEND
 */
boolean xdr_nv_send_nv_jcdma_uim_lock_data (xdr_s_type *xdrs, nv_jcdma_uim_lock_data const *sptr)
{
  int i;
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->ruim_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->imsi_mcc1) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->imsi_mcc2) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->sp_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ug_id) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (4); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->indicator[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_send_nv_jcdma_uim_lock_data */

/*
 * nv_jcdma_uim_lock_data XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_jcdma_uim_lock_data (xdr_s_type *xdrs, nv_jcdma_uim_lock_data *sptr)
{
  int i;
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->ruim_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->imsi_mcc1) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->imsi_mcc2) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->sp_id) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ug_id) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (4); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->indicator[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  return (TRUE);
} /* xdr_nv_recv_nv_jcdma_uim_lock_data */




/*
 * nv_ds_at_v250_registers_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_at_v250_registers_type (xdr_s_type *xdrs, nv_ds_at_v250_registers_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->e_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->q_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->v_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->x_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->amp_c_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->amp_d_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->amp_s_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ifc_reg_val, 2) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->slash_v_reg_val) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_at_v250_registers_type */

/*
 * nv_ds_at_v250_registers_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_at_v250_registers_type (xdr_s_type *xdrs, nv_ds_at_v250_registers_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->e_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->q_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->v_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->x_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->amp_c_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->amp_d_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->amp_s_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ifc_reg_val, 2) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->slash_v_reg_val) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_at_v250_registers_type */




/*
 * nv_ds_at_s_registers_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_at_s_registers_type (xdr_s_type *xdrs, nv_ds_at_s_registers_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->s0_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->s7_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->s10_reg_val) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_at_s_registers_type */

/*
 * nv_ds_at_s_registers_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_at_s_registers_type (xdr_s_type *xdrs, nv_ds_at_s_registers_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->s0_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->s7_reg_val) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->s10_reg_val) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_at_s_registers_type */




/*
 * nv_process_restart_switch_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_process_restart_switch_type (xdr_s_type *xdrs, nv_process_restart_switch_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->timer_period) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->modem_restart_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->apps_restart_count) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_process_restart_switch_type */

/*
 * nv_process_restart_switch_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_process_restart_switch_type (xdr_s_type *xdrs, nv_process_restart_switch_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->timer_period) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->modem_restart_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->apps_restart_count) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_process_restart_switch_type */




/*
 * nv_ds_at_csas_csca_register XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_at_csas_csca_register (xdr_s_type *xdrs, nv_ds_at_csas_csca_register const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->sca, 22) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->tosca, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_at_csas_csca_register */

/*
 * nv_ds_at_csas_csca_register XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_at_csas_csca_register (xdr_s_type *xdrs, nv_ds_at_csas_csca_register *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->sca, 22) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->tosca, 6) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_at_csas_csca_register */




/*
 * nv_ds_at_csas_csmp_settings XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_at_csas_csmp_settings (xdr_s_type *xdrs, nv_ds_at_csas_csmp_settings const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->fo, 4) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->vp, 23) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->pid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->dcs) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_at_csas_csmp_settings */

/*
 * nv_ds_at_csas_csmp_settings XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_at_csas_csmp_settings (xdr_s_type *xdrs, nv_ds_at_csas_csmp_settings *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->fo, 4) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->vp, 23) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->pid) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->dcs) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_at_csas_csmp_settings */




/*
 * nv_ds_at_csas_cscb_settings XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_at_csas_cscb_settings (xdr_s_type *xdrs, nv_ds_at_csas_cscb_settings const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->mode) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->mids, 51) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->dcss, 51) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_at_csas_cscb_settings */

/*
 * nv_ds_at_csas_cscb_settings XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_at_csas_cscb_settings (xdr_s_type *xdrs, nv_ds_at_csas_cscb_settings *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->mode) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->mids, 51) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->dcss, 51) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_at_csas_cscb_settings */




/*
 * nv_ds_sip_profile_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_sip_profile_type (xdr_s_type *xdrs, nv_ds_sip_profile_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->app_priority) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->data_rate_mode) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->data_bearer) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->auth_algorithm) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->app_type) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_sip_profile_type */

/*
 * nv_ds_sip_profile_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_sip_profile_type (xdr_s_type *xdrs, nv_ds_sip_profile_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->app_priority) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->data_rate_mode) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->data_bearer) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->auth_algorithm) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->app_type) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_sip_profile_type */




/*
 * nv_ds_sip_profile_nai_info_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_sip_profile_nai_info_type (xdr_s_type *xdrs, nv_ds_sip_profile_nai_info_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->nai, 126) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_sip_profile_nai_info_type */

/*
 * nv_ds_sip_profile_nai_info_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_sip_profile_nai_info_type (xdr_s_type *xdrs, nv_ds_sip_profile_nai_info_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->nai_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->nai, 126) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_sip_profile_nai_info_type */




/*
 * nv_ds_sip_ppp_ss_info_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ds_sip_ppp_ss_info_type (xdr_s_type *xdrs, nv_ds_sip_ppp_ss_info_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ss_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ss, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ds_sip_ppp_ss_info_type */

/*
 * nv_ds_sip_ppp_ss_info_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ds_sip_ppp_ss_info_type (xdr_s_type *xdrs, nv_ds_sip_ppp_ss_info_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ss_length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ss, 32) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ds_sip_ppp_ss_info_type */




/*
 * nv_iwlan_operator_id_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_iwlan_operator_id_type (xdr_s_type *xdrs, nv_iwlan_operator_id_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->value, 100) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_iwlan_operator_id_type */

/*
 * nv_iwlan_operator_id_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_iwlan_operator_id_type (xdr_s_type *xdrs, nv_iwlan_operator_id_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->value, 100) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_iwlan_operator_id_type */




/*
 * nv_iwlan_realm_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_iwlan_realm_type (xdr_s_type *xdrs, nv_iwlan_realm_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->value, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_iwlan_realm_type */

/*
 * nv_iwlan_realm_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_iwlan_realm_type (xdr_s_type *xdrs, nv_iwlan_realm_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->value, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_iwlan_realm_type */




/*
 * nv_iwlan_pdif_fqdn_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_iwlan_pdif_fqdn_type (xdr_s_type *xdrs, nv_iwlan_pdif_fqdn_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->value, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_iwlan_pdif_fqdn_type */

/*
 * nv_iwlan_pdif_fqdn_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_iwlan_pdif_fqdn_type (xdr_s_type *xdrs, nv_iwlan_pdif_fqdn_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->length) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->value, 127) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_iwlan_pdif_fqdn_type */




/*
 * nv_ppp_vsncp_config_data_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ppp_vsncp_config_data_type (xdr_s_type *xdrs, nv_ppp_vsncp_config_data_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->vsncp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(sptr->vsncp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->vsncp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->vsncp_term_try) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ppp_vsncp_config_data_type */

/*
 * nv_ppp_vsncp_config_data_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ppp_vsncp_config_data_type (xdr_s_type *xdrs, nv_ppp_vsncp_config_data_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->vsncp_term_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(sptr->vsncp_ack_timeout) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->vsncp_req_try) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->vsncp_term_try) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ppp_vsncp_config_data_type */




/*
 * nv_eap_shared_secret_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_eap_shared_secret_type (xdr_s_type *xdrs, nv_eap_shared_secret_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->eap_shared_secret_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->eap_shared_secret, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_eap_shared_secret_type */

/*
 * nv_eap_shared_secret_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_eap_shared_secret_type (xdr_s_type *xdrs, nv_eap_shared_secret_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->eap_shared_secret_len) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->eap_shared_secret, 16) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_eap_shared_secret_type */




/*
 * nv_wlan_atheros_specific_cfg XDR_NV_SEND
 */
boolean xdr_nv_send_nv_wlan_atheros_specific_cfg (xdr_s_type *xdrs, nv_wlan_atheros_specific_cfg const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->atheros_clk_freq_index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->idle_period) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->ps_poll_number) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(sptr->dtim_policy) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_wlan_atheros_specific_cfg */

/*
 * nv_wlan_atheros_specific_cfg XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_wlan_atheros_specific_cfg (xdr_s_type *xdrs, nv_wlan_atheros_specific_cfg *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->atheros_clk_freq_index) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->idle_period) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->ps_poll_number) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(sptr->dtim_policy) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_wlan_atheros_specific_cfg */




/*
 * nv_ehrpd_milenage_op_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ehrpd_milenage_op_type (xdr_s_type *xdrs, nv_ehrpd_milenage_op_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ehrpd_milenage_op, 16) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->ehrpd_milenage_op_length) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ehrpd_milenage_op_type */

/*
 * nv_ehrpd_milenage_op_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ehrpd_milenage_op_type (xdr_s_type *xdrs, nv_ehrpd_milenage_op_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ehrpd_milenage_op, 16) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->ehrpd_milenage_op_length) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ehrpd_milenage_op_type */




/*
 * nv_ehrpd_imsi_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_ehrpd_imsi_type (xdr_s_type *xdrs, nv_ehrpd_imsi_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ehrpd_mcc, 3) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_of_mcc_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ehrpd_mnc, 3) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_of_mnc_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_BYTES(xdrs, sptr->ehrpd_msin, 10) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->num_of_msin_digits) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_ehrpd_imsi_type */

/*
 * nv_ehrpd_imsi_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_ehrpd_imsi_type (xdr_s_type *xdrs, nv_ehrpd_imsi_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ehrpd_mcc, 3) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_of_mcc_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ehrpd_mnc, 3) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_of_mnc_digits) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_BYTES(xdrs, sptr->ehrpd_msin, 10) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->num_of_msin_digits) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_ehrpd_imsi_type */




/*
 * nv_hs_usb_phy_config_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_hs_usb_phy_config_type (xdr_s_type *xdrs, nv_hs_usb_phy_config_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->pre_emphasis_enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->pre_emphasis_value) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->amplitude_adjustment_enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->amplitude_adjustment_value) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_hs_usb_phy_config_type */

/*
 * nv_hs_usb_phy_config_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_hs_usb_phy_config_type (xdr_s_type *xdrs, nv_hs_usb_phy_config_type *sptr)
{
  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->pre_emphasis_enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->pre_emphasis_value) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->amplitude_adjustment_enable) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->amplitude_adjustment_value) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_hs_usb_phy_config_type */




/*
 * nv_data_call_over_ehrpd_only_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_data_call_over_ehrpd_only_type (xdr_s_type *xdrs, nv_data_call_over_ehrpd_only_type const *sptr)
{
  if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(sptr->ehrpd_only_flag) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->max_failure_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->max_failure_total_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(sptr->new_attempt_max_timer) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_nv_data_call_over_ehrpd_only_type */

/*
 * nv_data_call_over_ehrpd_only_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_data_call_over_ehrpd_only_type (xdr_s_type *xdrs, nv_data_call_over_ehrpd_only_type *sptr)
{
  if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(sptr->ehrpd_only_flag) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->max_failure_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->max_failure_total_count) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(sptr->new_attempt_max_timer) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_nv_data_call_over_ehrpd_only_type */




/*
 * union_nv_item_type_ecc_list_arr XDR_NV_SEND
 */
boolean xdr_nv_send_union_nv_item_type_ecc_list_arr (xdr_s_type *xdrs, __packed byte  const (*objp)[3])
{
  if ( ! XDR_SEND_PACKED_BYTES(xdrs, (*objp), 3) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_send_union_nv_item_type_ecc_list_arr */

/*
 * union_nv_item_type_ecc_list_arr XDR_NV_RECV
 */
boolean xdr_nv_recv_union_nv_item_type_ecc_list_arr (xdr_s_type *xdrs, __packed byte (*objp)[3])
{
  if ( ! XDR_RECV_PACKED_BYTES(xdrs, (*objp), 3) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_nv_recv_union_nv_item_type_ecc_list_arr */




/*
 * nv_item_type XDR_NV_SEND
 */
boolean xdr_nv_send_nv_item_type (xdr_s_type *xdrs, nv_item_type const *uptr, nv_items_enum_type obj_disc)
{
  int i;
  switch ( obj_disc ){
 case NV_ESN_I:
      if ( ! xdr_nv_send_nv_esn_type( xdrs, &(uptr->esn) ) ) {
        return (FALSE);
      }

      break;
 case NV_ESN_CHKSUM_I:
      if ( ! xdr_nv_send_nv_esn_chksum_type( xdrs, &(uptr->esn_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_SCM_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->scm) ) ) {
        return (FALSE);
      }

      break;
 case NV_SLOT_CYCLE_INDEX_I:
      if ( ! XDR_SEND_PACKED_INT8( xdrs, &(uptr->slot_cycle_index) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_CAI_REV_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->mob_cai_rev) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_FIRM_REV_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->mob_firm_rev) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_MODEL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->mob_model) ) ) {
        return (FALSE);
      }

      break;
 case NV_CONFIG_CHKSUM_I:
      if ( ! xdr_nv_send_nv_config_chksum_type( xdrs, &(uptr->config_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_PREF_MODE_I:
      if ( ! xdr_nv_send_nv_pref_mode_type( xdrs, &(uptr->pref_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_SID_LOCK_I:
      if ( ! xdr_nv_send_nv_sid_lock_type( xdrs, &(uptr->cdma_sid_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_SID_ACQ_I:
      if ( ! xdr_nv_send_nv_sid_acq_type( xdrs, &(uptr->cdma_sid_acq) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_SID_LOCK_I:
      if ( ! xdr_nv_send_nv_sid_lock_type( xdrs, &(uptr->analog_sid_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_SID_ACQ_I:
      if ( ! xdr_nv_send_nv_sid_acq_type( xdrs, &(uptr->analog_sid_acq) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_FIRSTCHP_I:
      if ( ! xdr_nv_send_nv_firstchp_type( xdrs, &(uptr->analog_firstchp) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_HOME_SID_I:
      if ( ! xdr_nv_send_nv_sid_type( xdrs, &(uptr->analog_home_sid) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_REG_I:
      if ( ! xdr_nv_send_nv_analog_reg_type( xdrs, &(uptr->analog_reg) ) ) {
        return (FALSE);
      }

      break;
 case NV_PCDMACH_I:
      if ( ! xdr_nv_send_nv_cdmach_type( xdrs, &(uptr->pcdmach) ) ) {
        return (FALSE);
      }

      break;
 case NV_SCDMACH_I:
      if ( ! xdr_nv_send_nv_cdmach_type( xdrs, &(uptr->scdmach) ) ) {
        return (FALSE);
      }

      break;
 case NV_NAM_CHKSUM_I:
      if ( ! xdr_nv_send_nv_nam_chksum_type( xdrs, &(uptr->nam_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_A_KEY_I:
      if ( ! xdr_nv_send_nv_a_key_type( xdrs, &(uptr->a_key) ) ) {
        return (FALSE);
      }

      break;
 case NV_A_KEY_CHKSUM_I:
      if ( ! xdr_nv_send_nv_a_key_chksum_type( xdrs, &(uptr->a_key_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_SSD_A_I:
      if ( ! xdr_nv_send_nv_ssd_type( xdrs, &(uptr->ssd_a) ) ) {
        return (FALSE);
      }

      break;
 case NV_SSD_B_I:
      if ( ! xdr_nv_send_nv_ssd_type( xdrs, &(uptr->ssd_b) ) ) {
        return (FALSE);
      }

      break;
 case NV_COUNT_I:
      if ( ! xdr_nv_send_nv_count_type( xdrs, &(uptr->count) ) ) {
        return (FALSE);
      }

      break;
 case NV_MIN1_I:
      if ( ! xdr_nv_send_nv_min1_type( xdrs, &(uptr->min1) ) ) {
        return (FALSE);
      }

      break;
 case NV_MIN2_I:
      if ( ! xdr_nv_send_nv_min2_type( xdrs, &(uptr->min2) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_TERM_HOME_I:
      if ( ! xdr_nv_send_nv_mob_term_type( xdrs, &(uptr->mob_term_home) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_TERM_FOR_SID_I:
      if ( ! xdr_nv_send_nv_mob_term_type( xdrs, &(uptr->mob_term_for_sid) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_TERM_FOR_NID_I:
      if ( ! xdr_nv_send_nv_mob_term_type( xdrs, &(uptr->mob_term_for_nid) ) ) {
        return (FALSE);
      }

      break;
 case NV_ACCOLC_I:
      if ( ! xdr_nv_send_nv_accolc_type( xdrs, &(uptr->accolc) ) ) {
        return (FALSE);
      }

      break;
 case NV_SID_NID_I:
      if ( ! xdr_nv_send_nv_sid_nid_type( xdrs, &(uptr->sid_nid) ) ) {
        return (FALSE);
      }

      break;
 case NV_MIN_CHKSUM_I:
      if ( ! xdr_nv_send_nv_min_chksum_type( xdrs, &(uptr->min_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_CURR_NAM_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->curr_nam) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_NAM_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auto_nam) ) ) {
        return (FALSE);
      }

      break;
 case NV_NAME_NAM_I:
      if ( ! xdr_nv_send_nv_name_nam_type( xdrs, &(uptr->name_nam) ) ) {
        return (FALSE);
      }

      break;
 case NV_AIR_TIMER_I:
      if ( ! xdr_nv_send_nv_call_time_type( xdrs, &(uptr->air_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAM_TIMER_I:
      if ( ! xdr_nv_send_nv_call_time_type( xdrs, &(uptr->roam_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_RUN_TIMER_I:
      if ( ! xdr_nv_send_nv_run_time_type( xdrs, &(uptr->run_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAL_I:
      if ( ! xdr_nv_send_nv_dial_type( xdrs, &(uptr->dial) ) ) {
        return (FALSE);
      }

      break;
 case NV_STACK_I:
      if ( ! xdr_nv_send_nv_stdial_type( xdrs, &(uptr->stack) ) ) {
        return (FALSE);
      }

      break;
 case NV_STACK_IDX_I:
      if ( ! xdr_nv_send_nv_stack_idx_type( xdrs, &(uptr->stack_idx) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAGE_SET_I:
      if ( ! xdr_nv_send_nv_page_set_type( xdrs, &(uptr->page_set) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAGE_MSG_I:
      if ( ! xdr_nv_send_nv_page_msg_type( xdrs, &(uptr->page_msg) ) ) {
        return (FALSE);
      }

      break;
 case NV_EAR_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ear_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_SPEAKER_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->speaker_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_RINGER_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ringer_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->beep_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_BEEP_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->call_beep) ) ) {
        return (FALSE);
      }

      break;
 case NV_CONT_KEY_DTMF_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->cont_key_dtmf) ) ) {
        return (FALSE);
      }

      break;
 case NV_CONT_STR_DTMF_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->cont_str_dtmf) ) ) {
        return (FALSE);
      }

      break;
 case NV_SVC_AREA_ALERT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->svc_area_alert) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_FADE_ALERT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->call_fade_alert) ) ) {
        return (FALSE);
      }

      break;
 case NV_BANNER_I:
      if ( ! xdr_nv_send_nv_banner_type( xdrs, &(uptr->banner) ) ) {
        return (FALSE);
      }

      break;
 case NV_LCD_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->lcd) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_ANSWER_I:
      if ( ! xdr_nv_send_nv_auto_answer_type( xdrs, &(uptr->auto_answer) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_REDIAL_I:
      if ( ! xdr_nv_send_nv_auto_redial_type( xdrs, &(uptr->auto_redial) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_HYPHEN_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auto_hyphen) ) ) {
        return (FALSE);
      }

      break;
 case NV_BACK_LIGHT_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->back_light) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_MUTE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auto_mute) ) ) {
        return (FALSE);
      }

      break;
 case NV_LOCK_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_LOCK_CODE_I:
      if ( ! xdr_nv_send_nv_lock_code_type( xdrs, &(uptr->lock_code) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_LOCK_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auto_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_RSTRC_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->call_rstrc) ) ) {
        return (FALSE);
      }

      break;
 case NV_SEC_CODE_I:
      if ( ! xdr_nv_send_nv_sec_code_type( xdrs, &(uptr->sec_code) ) ) {
        return (FALSE);
      }

      break;
 case NV_HORN_ALERT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->horn_alert) ) ) {
        return (FALSE);
      }

      break;
 case NV_ERR_LOG_I:
      if ( ! xdr_nv_send_nv_err_log_type( xdrs, &(uptr->err_log) ) ) {
        return (FALSE);
      }

      break;
 case NV_VBATT_I:
      if ( ! xdr_nv_send_nv_minmax_type( xdrs, &(uptr->vbatt) ) ) {
        return (FALSE);
      }

      break;
 case NV_SPARE_2_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->spare_2) ) ) {
        return (FALSE);
      }

      break;
 case NV_MAX_TX_ADJ_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->max_tx_adj) ) ) {
        return (FALSE);
      }

      break;
 case NV_INI_MUTE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->init_mute) ) ) {
        return (FALSE);
      }

      break;
 case NV_FACTORY_INFO_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->fact_info, 100) ) {
        return (FALSE);
      }

      break;
 case NV_DM_ADDR_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->dm_addr) ) ) {
        return (FALSE);
      }

      break;
 case NV_ONETOUCH_DIAL_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->onetouch_dial) ) ) {
        return (FALSE);
      }

      break;
 case NV_ALERTS_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->alerts_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_ALERTS_LVL_SHADOW_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->alerts_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_LVL_SHADOW_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->beep_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_EAR_LVL_SHADOW_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ear_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_AIR_CNT_I:
      if ( ! xdr_nv_send_nv_call_cnt_type( xdrs, &(uptr->air_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAM_CNT_I:
      if ( ! xdr_nv_send_nv_call_cnt_type( xdrs, &(uptr->roam_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_LIFE_CNT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->life_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_MCC_I:
      if ( ! xdr_nv_send_nv_imsi_mcc_type( xdrs, &(uptr->imsi_mcc) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_11_12_I:
      if ( ! xdr_nv_send_nv_imsi_11_12_type( xdrs, &(uptr->imsi_11_12) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIR_NUMBER_I:
      if ( ! xdr_nv_send_nv_dir_number_type( xdrs, &(uptr->dir_number) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOICE_PRIV_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->voice_priv) ) ) {
        return (FALSE);
      }

      break;
 case NV_FSC_I:
      if ( ! xdr_nv_send_nv_fsc_type( xdrs, &(uptr->fsc) ) ) {
        return (FALSE);
      }

      break;
 case NV_LIFE_TIMER_G_I:
      if ( ! xdr_nv_send_nv_run_time_type( xdrs, &(uptr->life_timer_g) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_TIMER_G_I:
      if ( ! xdr_nv_send_nv_run_time_type( xdrs, &(uptr->call_timer_g) ) ) {
        return (FALSE);
      }

      break;
 case NV_PWR_DWN_CNT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->pwr_dwn_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_FSC2_I:
      if ( ! xdr_nv_send_nv_fsc2_type( xdrs, &(uptr->fsc2) ) ) {
        return (FALSE);
      }

      break;
 case NV_FSC2_CHKSUM_I:
      if ( ! xdr_nv_send_nv_fsc2_chksum_type( xdrs, &(uptr->fsc2_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_WDC_I:
      if ( ! xdr_nv_send_nv_wdc_type( xdrs, &(uptr->wdc) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_ADDR_NUM_I:
      if ( ! xdr_nv_send_nv_imsi_addr_num_type( xdrs, &(uptr->imsi_addr_num) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIR_NUMBER_PCS_I:
      if ( ! xdr_nv_send_nv_mob_dir_data_type( xdrs, &(uptr->mob_dir_number) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAMING_LIST_I:
      if ( ! xdr_nv_send_nv_roaming_list_union_type( xdrs, &(uptr->roaming_list) ) ) {
        return (FALSE);
      }

      break;
 case NV_OTKSL_I:
      if ( ! xdr_nv_send_nv_sec_code_type( xdrs, &(uptr->otksl) ) ) {
        return (FALSE);
      }

      break;
 case NV_TIMED_PREF_MODE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->timed_pref_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_RINGER_TYPE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ringer_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_BACK_LIGHT_HFK_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->back_light_hfk) ) ) {
        return (FALSE);
      }

      break;
 case NV_KEY_SOUND_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->key_sound) ) ) {
        return (FALSE);
      }

      break;
 case NV_LANGUAGE_SELECTION_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->language_selection) ) ) {
        return (FALSE);
      }

      break;
 case NV_MENU_FORMAT_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->menu_format) ) ) {
        return (FALSE);
      }

      break;
 case NV_RINGER_SPKR_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ringer_spkr_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_SPKR_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->beep_spkr_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_VIBRATOR_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->vibrator) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_QNC_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->data_qnc_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_SO_SET_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->data_so_set) ) ) {
        return (FALSE);
      }

      break;
 case NV_SID_NID_LOCK_I:
      if ( ! xdr_nv_send_nv_sid_nid_lock_type( xdrs, &(uptr->sid_nid_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_PRL_ENABLED_I:
      if ( ! xdr_nv_send_nv_enabled_type( xdrs, &(uptr->prl_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAMING_LIST_683_I:
      if ( ! xdr_nv_send_nv_roaming_list_union_type( xdrs, &(uptr->roaming_list_683) ) ) {
        return (FALSE);
      }

      break;
 case NV_SYSTEM_PREF_I:
      if ( ! xdr_nv_send_nv_sys_pref_type( xdrs, &(uptr->system_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_HOME_SID_NID_I:
      if ( ! xdr_nv_send_nv_home_sid_nid_type( xdrs, &(uptr->home_sid_nid) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_S1_I:
      if ( ! xdr_nv_send_nv_min1_type( xdrs, &(uptr->imsi_t_s1) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_S2_I:
      if ( ! xdr_nv_send_nv_min2_type( xdrs, &(uptr->imsi_t_s2) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_MCC_I:
      if ( ! xdr_nv_send_nv_imsi_mcc_type( xdrs, &(uptr->imsi_t_mcc) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_11_12_I:
      if ( ! xdr_nv_send_nv_imsi_11_12_type( xdrs, &(uptr->imsi_t_11_12) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_ADDR_NUM_I:
      if ( ! xdr_nv_send_nv_imsi_addr_num_type( xdrs, &(uptr->imsi_t_addr_num) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_UTC_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->sms_utc) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAM_RINGER_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->roam_ringer) ) ) {
        return (FALSE);
      }

      break;
 case NV_PREF_VOICE_SO_I:
      if ( ! xdr_nv_send_nv_pref_voice_so_type( xdrs, &(uptr->pref_voice_so) ) ) {
        return (FALSE);
      }

      break;
 case NV_SPC_CHANGE_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->spc_change_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_MDR_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->data_mdr_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_PKT_ORIG_STR_I:
      if ( ! xdr_nv_send_nv_data_pkt_orig_str_type( xdrs, &(uptr->data_pkt_orig_str) ) ) {
        return (FALSE);
      }

      break;
 case NV_UP_KEY_I:
      if ( ! xdr_nv_send_nv_up_key_type( xdrs, &(uptr->up_key) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_VOLUME_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auto_volume_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAP_USER_ID_I:
      if ( ! xdr_nv_send_nv_pap_user_id_type( xdrs, &(uptr->pap_user_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAP_PASSWORD_I:
      if ( ! xdr_nv_send_nv_pap_password_type( xdrs, &(uptr->pap_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_BC_SERVICE_TABLE_SIZE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->sms_bc_service_table_size) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_BC_SERVICE_TABLE_I:
      if ( ! xdr_nv_send_nv_sms_bc_service_table_type( xdrs, &(uptr->sms_bc_service_table) ) ) {
        return (FALSE);
      }

      break;
 case NV_RUIM_CHV_1_I:
      if ( ! xdr_nv_send_nv_ruim_chv_type( xdrs, &(uptr->ruim_chv_1) ) ) {
        return (FALSE);
      }

      break;
 case NV_RUIM_CHV_2_I:
      if ( ! xdr_nv_send_nv_ruim_chv_type( xdrs, &(uptr->ruim_chv_2) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_PDE_ADDRESS_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->gps1_pde_address) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_PDE_TRANSPORT_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->gps1_pde_transport) ) ) {
        return (FALSE);
      }

      break;
 case NV_TTY_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->tty) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_SDAC_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->mm_sdac_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_SDAC_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->beep_sdac_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_SDAC_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->sdac_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->mm_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_LVL_SHADOW_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->mm_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_SPEAKER_LVL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->mm_speaker_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_PRIMARY_DNS_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->primary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_SECONDARY_DNS_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->secondary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_PDE_PORT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->gps1_pde_port) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_RINGER_FILE_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->mm_ringer_file, 40) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_SCRM_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->data_scrm_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_BD_ADDR_I:
      if ( ! xdr_nv_send_nv_bd_addr_type( xdrs, &(uptr->bd_addr) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_TRTL_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->data_trtl_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_LOCK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->gps1_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_FTM_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ftm_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_DEFAULT_BAUDRATE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->ds_default_baudrate) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEFAULT_BAUDRATE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->diag_default_baudrate) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_QCMIP_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_qcmip) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RETRIES_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_mip_retries) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RETRY_INT_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_mip_retry_int) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_PRE_RE_RRQ_TIME_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_mip_pre_re_rrq_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_ACTIVE_PROF_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_mip_active_prof) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_GEN_USER_PROF_I:
      if ( ! xdr_nv_send_nv_ds_mip_gen_user_prof_type( xdrs, &(uptr->ds_mip_gen_user_prof) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_SS_USER_PROF_I:
      if ( ! xdr_nv_send_nv_ds_mip_ss_user_prof_type( xdrs, &(uptr->ds_mip_ss_user_prof) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRAMP_ADDRESS_DATA_I:
      if ( ! xdr_nv_send_nv_hdramp_address_data_type( xdrs, &(uptr->hdramp_address_data) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCP_SESSION_STATUS_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hdrscp_session_status) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_MN_HA_TIME_DELTA_I:
      if ( ! xdr_nv_send_nv_ds_mip_mn_ha_time_delta_type( xdrs, &(uptr->ds_mip_mn_ha_time_delta) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_QC_DRS_OPT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_qc_drs_opt) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_2002BIS_MN_HA_AUTH_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_2002bis_mn_ha_auth) ) ) {
        return (FALSE);
      }

      break;
 case NV_UE_IMEI_I:
      if ( ! xdr_nv_send_nv_ue_imei_type( xdrs, &(uptr->ue_imei) ) ) {
        return (FALSE);
      }

      break;
 case NV_GSM_A5_ALGORITHMS_SUPPORTED_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->gsm_a5_algorithms_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_VM_NUMBER_I:
      if ( ! xdr_nv_send_nv_sms_vm_number_type( xdrs, &(uptr->sms_vm_number) ) ) {
        return (FALSE);
      }

      break;
 case NV_HYBRID_PREF_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->hybrid_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_SP_ECC_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->sp_ecc_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_ECC_LIST_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (10); i++ ) {
        /*lint -save -e545*/
        if ( ! xdr_nv_send_union_nv_item_type_ecc_list_arr( xdrs, &(uptr->ecc_list[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_RF_CAL_VER_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->rf_cal_ver, 8) ) {
        return (FALSE);
      }

      break;
 case NV_RF_CAL_DATE_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->rf_cal_date) ) ) {
        return (FALSE);
      }

      break;
 case NV_DOMAIN_NAME_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->domain_name, 100) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_NAI_I:
      if ( ! xdr_nv_send_nv_hdr_an_auth_nai_type( xdrs, &(uptr->hdr_an_auth_nai) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_PASSWORD_I:
      if ( ! xdr_nv_send_nv_hdr_an_auth_password_type( xdrs, &(uptr->hdr_an_auth_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RRQ_IF_TFRK_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_rrq_if_tfrk) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_ENABLE_PROF_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->ds_mip_enable_prof, 6) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_RX_DIVERSITY_CTRL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->hdr_rx_diversity_ctrl) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_CFG_ROUTING_I:
      if ( ! xdr_nv_send_nv_sms_routing_type( xdrs, &(uptr->sms_cfg_routing) ) ) {
        return (FALSE);
      }

      break;
 case NV_SERVICE_DOMAIN_PREF_I:
      if ( ! xdr_nv_send_nv_srv_domain_pref_type( xdrs, &(uptr->service_domain_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_DMU_PKOID_I:
      if ( ! xdr_nv_send_nv_ds_mip_dmu_pkoid_type( xdrs, &(uptr->ds_mip_dmu_pkoid) ) ) {
        return (FALSE);
      }

      break;
 case NV_RTRE_CONFIG_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->rtre_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_DYNAMIC_RANGE_I:
      if ( ! XDR_SEND_PACKED_INT16( xdrs, &(uptr->cdma_dynamic_range) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_MIN_RX_RSSI_I:
      if ( ! XDR_SEND_PACKED_INT16( xdrs, &(uptr->cdma_min_rx_rssi) ) ) {
        return (FALSE);
      }

      break;
 case NV_LAST_TX_DATA_COUNT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->last_tx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_LAST_RX_DATA_COUNT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->last_rx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_TOTAL_TX_DATA_COUNT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->total_tx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_TOTAL_RX_DATA_COUNT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->total_rx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_DMU_MN_AUTH_I:
      if ( ! xdr_nv_send_nv_ds_mip_dmu_mn_auth_type( xdrs, &(uptr->ds_mip_dmu_mn_auth) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_M511_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->jcdma_m511_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_M512_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->jcdma_m512_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_M513_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->jcdma_m513_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_ERR_FATAL_OPTIONS_I:
      if ( ! xdr_nv_send_nv_err_fatal_options_type( xdrs, &(uptr->err_fatal_options) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_PASSWORD_I:
      if ( ! xdr_nv_send_nv_ppp_password_type( xdrs, &(uptr->ppp_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_USER_ID_I:
      if ( ! xdr_nv_send_nv_ppp_user_id_type( xdrs, &(uptr->ppp_user_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_UIM_PREF_PROTOCOL_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->uim_pref_protocol) ) ) {
        return (FALSE);
      }

      break;
 case NV_WCDMACS_SYNC_TEST_APP_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->wcdmacs_sync_test_app_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_PPP_PASSWORD_I:
      if ( ! xdr_nv_send_nv_hdr_an_ppp_password_type( xdrs, &(uptr->hdr_an_ppp_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_RTSP_PROXY_SERVER_ADDR_I:
      if ( ! xdr_nv_send_nv_rtsp_proxy_server_addr_type( xdrs, &(uptr->rtsp_proxy_server_addr) ) ) {
        return (FALSE);
      }

      break;
 case NV_HYSTERISIS_ACTIVATION_TIMER_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hysterisis_activation_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_HYSTERISIS_TIMER_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hysterisis_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_PKT_DIAL_STRINGS_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (4); i++ ) {
        /*lint -save -e545*/
        if ( ! xdr_nv_send_nv_pkt_dial_string_type( xdrs, &(uptr->pkt_dial_strings[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_DATA_INCOMING_CSD_CALL_IS_INTERNAL_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->data_incoming_csd_call_is_internal) ) ) {
        return (FALSE);
      }

      break;
 case NV_SSPR_P_REV_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->sspr_p_rev) ) ) {
        return (FALSE);
      }

      break;
 case NV_RTSP_PROXY_SERVER_ADDR_ALTERNATE_I:
      if ( ! xdr_nv_send_nv_rtsp_proxy_server_addr_type( xdrs, &(uptr->rtsp_proxy_server_addr_alternate) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPRS_ANITE_GCF_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->gprs_anite_gcf) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_RX_DIVERSITY_CTRL_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->cdma_rx_diversity_ctrl) ) ) {
        return (FALSE);
      }

      break;
 case NV_LONG_PDPACT_TOLERANCE_FOR_TE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->long_pdpact_tolerance_for_te) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_PASSWD_LONG_I:
      if ( ! xdr_nv_send_nv_hdr_an_auth_passwd_long_type( xdrs, &(uptr->hdr_an_auth_passwd_long) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_QC_HANDDOWN_TO_1X_OPT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_qc_handdown_to_1x_opt) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_USER_ID_LONG_I:
      if ( ! xdr_nv_send_nv_hdr_an_auth_user_id_long_type( xdrs, &(uptr->hdr_an_auth_user_id_long) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_CONFIG_DATA_I:
      if ( ! xdr_nv_send_nv_ppp_configure_data_type( xdrs, &(uptr->ppp_config_data) ) ) {
        return (FALSE);
      }

      break;
 case NV_GSM_AMR_CALL_CONFIG_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->gsm_amr_call_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_SEC_DEVICE_KEY_I:
      if ( ! xdr_nv_send_nv_sec_device_key_type( xdrs, &(uptr->sec_device_key) ) ) {
        return (FALSE);
      }

      break;
 case NV_TCP_GRACEFUL_DORMANT_CLOSE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->tcp_graceful_dormant_close) ) ) {
        return (FALSE);
      }

      break;
 case NV_EPLMN_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->eplmn_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DTMF_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->dtmf) ) ) {
        return (FALSE);
      }

      break;
 case NV_SN_PPP_STATUS_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->sn_ppp_status) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RLP_VERSION_GSM_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_ucsd_rlp_version_gsm) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RLP_VERSION_WCDMA_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_ucsd_rlp_version_wcdma) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_ATCOP_RLP_VERSION_DEFAULT_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_atcop_rlp_version_default) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RECOVERY_MODE_SREJ_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ds_ucsd_recovery_mode_srej) ) ) {
        return (FALSE);
      }

      break;
 case NV_TCP_KEEPALIVE_IDLE_TIME_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->tcp_keepalive_idle_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_FORCE_CDMA_PKT_STR_I:
      if ( ! xdr_nv_send_nv_data_pkt_orig_str_type( xdrs, &(uptr->data_force_cdma_pkt_str) ) ) {
        return (FALSE);
      }

      break;
 case NV_PS_IPV6_IID_I:
      if ( ! xdr_nv_send_nv_ps_ipv6_iid_type( xdrs, &(uptr->ps_ipv6_iid) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEBUG_CONTROL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->diag_debug_control) ) ) {
        return (FALSE);
      }

      break;
 case NV_QVP_APP_DEFAULT_CAPABILITY_TYPE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->qvp_app_default_capability_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEBUG_DETAIL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->diag_debug_detail) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ipv6_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_SM_CONFIG_I:
      if ( ! xdr_nv_send_nv_ip6_sm_config_type( xdrs, &(uptr->ipv6_sm_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_PRIMARY_DNS_I:
      if ( ! xdr_nv_send_nv_ipv6_address_type( xdrs, &(uptr->ipv6_primary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_SECONDARY_DNS_I:
      if ( ! xdr_nv_send_nv_ipv6_address_type( xdrs, &(uptr->ipv6_secondary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTH_REQUIRE_PASSWORD_ENCRYPTION_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auth_require_password_encryption) ) ) {
        return (FALSE);
      }

      break;
 case NV_PDSN_AS_PROXY_IPV6_DNS_SERVER_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->pdsn_as_proxy_ipv6_dns_server) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_FAILOVER_CONFIG_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ipv6_failover_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_MEID_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (2); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->meid[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_DIAG_DEBUG_MAX_FILES_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->diag_debug_max_files) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_CARRIER_ID_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->brew_carrier_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_PLATFORM_ID_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->brew_platform_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_BKEY_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->brew_bkey, 16) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_SERVER_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->brew_server, 64) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_DOWNLOAD_FLAGS_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->brew_download_flags) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_AUTH_POLICY_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->brew_auth_policy) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_PRIVACY_POLICY_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->brew_privacy_policy) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_SUBSCRIBER_ID_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->brew_subscriber_id, 32) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RLP_ADM_IDLE_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->ds_ucsd_rlp_adm_idle) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_CCP_REJECT_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ppp_ccp_reject) ) ) {
        return (FALSE);
      }

      break;
 case NV_USB_CURRENT_DEVICE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->usb_current_device) ) ) {
        return (FALSE);
      }

      break;
 case NV_ACTIVE_EPZID_TYPE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->active_epzid_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_SW_VERSION_INFO_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->sw_version_info, 30) ) {
        return (FALSE);
      }

      break;
 case NV_SET_AUTO_TIME_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->set_auto_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_INCOMING_DATA_RLP_TIMEOUT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->ds_incoming_data_rlp_timeout) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_DEFAULT_INACTIVITY_RESTORE_TIMEOUT_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->ds_default_inactivity_restore_timeout) ) ) {
        return (FALSE);
      }

      break;
 case NV_CHG_USB_NVDISABLE_VAL_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->chg_usb_nvdisable_val) ) ) {
        return (FALSE);
      }

      break;
 case NV_DEVICE_SERIAL_NO_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->device_serial_no, 16) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RM_NAI_I:
      if ( ! xdr_nv_send_nv_ds_mip_rm_nai_type( xdrs, &(uptr->ds_mip_rm_nai) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_ACTIVE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->bt_active) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_VISIBLE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->bt_visible) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SAP_ENABLE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->bt_sap_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_TIMEZONE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->timezone) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_RM_NAI_I:
      if ( ! xdr_nv_send_nv_ds_sip_rm_nai_type( xdrs, &(uptr->ds_sip_rm_nai) ) ) {
        return (FALSE);
      }

      break;
 case NV_BROWSER_MAILTO_EMAIL_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->browser_mailto_email) ) ) {
        return (FALSE);
      }

      break;
 case NV_IRDA_PNP_MANUFACTURER_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->irda_pnp_manufacturer, 31) ) {
        return (FALSE);
      }

      break;
 case NV_IRDA_PNP_NAME_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->irda_pnp_name, 91) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEBUG_CALL_TRACE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->diag_debug_call_trace) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_SPC_UNLOCK_TTL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->diag_spc_unlock_ttl) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_UMTS_FAILOVER_CONFIG_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ipv6_umts_failover_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_SET_RINGER_VOLUME_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->set_ringer_volume) ) ) {
        return (FALSE);
      }

      break;
 case NV_MANUAL_PLMN_SEL_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->manual_plmn_sel) ) ) {
        return (FALSE);
      }

      break;
 case NV_MANUAL_PLMN_SELECTION_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->manual_plmn_selection) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_ENABLE_PS_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->wlan_enable_ps_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRMRLP_NUM_BYTES_TO_LOG_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hdrmrlp_num_bytes_to_log) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_NT_CONNECT_STATE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_ucsd_nt_connect_state) ) ) {
        return (FALSE);
      }

      break;
 case NV_CCBS_SEL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ccbs_sel) ) ) {
        return (FALSE);
      }

      break;
 case NV_TRM_CONFIG_I:
      if ( ! xdr_nv_send_nv_trm_config_type( xdrs, &(uptr->trm_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_ENS_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ens_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_SPC_TIMEOUT_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->diag_spc_timeout) ) ) {
        return (FALSE);
      }

      break;
 case NV_BACK_LIGHT_INTENSITY_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->back_light_intensity) ) ) {
        return (FALSE);
      }

      break;
 case NV_DISPLAY_DURATION_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->display_duration) ) ) {
        return (FALSE);
      }

      break;
 case NV_TIMEZONE_CITY_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->timezone_city) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_TIME_ENABLE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->auto_time_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_RETRY_PERIOD_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->sms_mo_retry_period) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_RETRY_INTERVAL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->sms_mo_retry_interval) ) ) {
        return (FALSE);
      }

      break;
 case NV_RMNET_AUTOCONNECT_I:
      if ( ! xdr_nv_send_nv_rmnet_autoconnect_type( xdrs, &(uptr->rmnet_autoconnect) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_EVDO_SIP_PROFILE_ID_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_qos_evdo_sip_profile_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_EVDO_RTP_PROFILE_ID_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_qos_evdo_rtp_profile_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_EVDO_DSCP_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_qos_evdo_dscp) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_DTX_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->voip_dtx_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WCDMA_RRC_VERSION_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->wcdma_rrc_version) ) ) {
        return (FALSE);
      }

      break;
 case NV_NITZ_NW_INFO_I:
      if ( ! xdr_nv_send_nv_nitz_plmn_type( xdrs, &(uptr->nitz_nw_info) ) ) {
        return (FALSE);
      }

      break;
 case NV_DQOS_SUPPORTED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->dqos_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_WLAN_PRFRD_CODEC_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_wlan_prfrd_codec) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_EVDO_PRFRD_CODEC_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_evdo_prfrd_codec) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_QOS_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->wlan_qos_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_GAME_MODE_PERSIST_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->game_mode_persist) ) ) {
        return (FALSE);
      }

      break;
 case NV_GAME_MODE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->game_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->voip_qos_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_WRITE_MEDIA_FILE_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->voip_write_media_file_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_READ_MEDIA_FILE_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->voip_read_media_file_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_OEM_INDEX_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->wlan_oem_index) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCP_FORCE_REL0_CONFIG_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->hdrscp_force_rel0_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCMDB_SECURE_CONFIG_I:
      if ( ! xdr_nv_send_nv_hdrscmdb_secure_config_type( xdrs, &(uptr->hdrscmdb_secure_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MAX_PAYLOAD_LENGTH_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->sms_max_payload_length) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_ON_ACCESS_CHANNEL_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->sms_mo_on_access_channel) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_ON_TRAFFIC_CHANNEL_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->sms_mo_on_traffic_channel) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_CONFRD_URI_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_confrd_uri) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_QOS_FLOW_PRIORITY_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->hdr_qos_flow_priority) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_REGISTRATION_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->voip_registration_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_DCVS_ACPU_ENABLE_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->dcvs_acpu_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPSONE_PASSWORD_I:
      if ( ! xdr_nv_send_nv_gpsone_password_type( xdrs, &(uptr->gpsone_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_CALLERID_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_callerid_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_MULTIDOMAIN_CAPABILITY_PREF_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->wlan_multidomain_capability_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_UMTS_NBNS_ENABLE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->umts_nbns_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_REFER_SUBSCRIPTION_EXPIRES_DURATION_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_refer_subscription_expires_duration) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_NOTIFY_REFER_RESPONSE_DURATION_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->voip_notify_refer_response_duration) ) ) {
        return (FALSE);
      }

      break;
 case NV_DCVSAPPS_NV_I:
      if ( ! xdr_nv_send_nv_dcvsapps_type( xdrs, &(uptr->dcvsapps_nv) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_SEC_UPDATE_RATE_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->gps1_sec_update_rate) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_DEREG_RETRIES_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_mip_dereg_retries) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_PRIVACY_EXTENSIONS_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ipv6_privacy_extensions_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_PRIVATE_ADDRESS_CONFIG_I:
      if ( ! xdr_nv_send_nv_ipv6_priv_addr_config( xdrs, &(uptr->ipv6_private_address_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_DISABLED_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->bt_disabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_CURRENT_COMPOSITION_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hs_usb_current_composition) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_ENABLE_BT_COEX_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->wlan_enable_bt_coex) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_EMPA_SUPPORTED_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->hdr_empa_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_PRECONDITION_ENABLE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->voip_precondition_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_DB_LP_SEC_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->db_lp_sec) ) ) {
        return (FALSE);
      }

      break;
 case NV_DB_LTM_OFF_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->db_ltm_off) ) ) {
        return (FALSE);
      }

      break;
 case NV_DB_DAYLT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->db_daylt) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS707_GO_NULL_TIMER_1X_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->ds707_go_null_timer_1x) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS707_GO_NULL_TIMER_DO_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->ds707_go_null_timer_do) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_MAC_ADDRESS_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->wlan_mac_address, 6) ) {
        return (FALSE);
      }

      break;
 case NV_DISPLAY_UPDATE_POWER_TEST_MODE_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->display_update_power_test_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_QVP_APP_PSVT_ENABLE_FLAG_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->qvp_app_psvt_enable_flag) ) ) {
        return (FALSE);
      }

      break;
 case NV_ICMP6_ND_CONFIG_I:
      if ( ! xdr_nv_send_nv_nd_config_items( xdrs, &(uptr->icmp6_nd_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_REMOTE_WAKEUP_DELAY_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->hs_usb_remote_wakeup_delay) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_RUIM_ID_I:
      if ( ! xdr_nv_send_nv_jcdma_ruimid( xdrs, &(uptr->jcdma_ruim_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_UIM_LOCK_I:
      if ( ! xdr_nv_send_nv_jcdma_uim_lock_data( xdrs, &(uptr->jcdma_uim_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_FTM_MODE_SWITCH_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->diag_ftm_mode_switch) ) ) {
        return (FALSE);
      }

      break;
 case NV_DHCP4_OPTIONS_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->dhcp4_options_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_DHCP6_OPTIONS_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->dhcp6_options_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_V250_REGISTERS_I:
      if ( ! xdr_nv_send_nv_ds_at_v250_registers_type( xdrs, &(uptr->ds_at_v250_registers) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_S_REGISTERS_I:
      if ( ! xdr_nv_send_nv_ds_at_s_registers_type( xdrs, &(uptr->ds_at_s_registers) ) ) {
        return (FALSE);
      }

      break;
 case NV_PROCESS_RESTART_SWITCH_I:
      if ( ! xdr_nv_send_nv_process_restart_switch_type( xdrs, &(uptr->process_restart_switch) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCP_FORCE_AT_CONFIG_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hdrscp_force_at_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_DIAG_ON_LEGACY_USB_PORT_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_diag_on_legacy_usb_port) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DIAGBUF_TX_SLEEP_TIME_NV_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->diag_diagbuf_tx_sleep_time_nv) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_LISBON_DISABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->bt_lisbon_disabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_CSAS_CSCA_SETTINGS_I:
      if ( ! xdr_nv_send_nv_ds_at_csas_csca_register( xdrs, &(uptr->ds_at_csas_csca_settings) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_CSAS_CSMP_SETTINGS_I:
      if ( ! xdr_nv_send_nv_ds_at_csas_csmp_settings( xdrs, &(uptr->ds_at_csas_csmp_settings) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_CSAS_CSCB_SETTINGS_I:
      if ( ! xdr_nv_send_nv_ds_at_csas_cscb_settings( xdrs, &(uptr->ds_at_csas_cscb_settings) ) ) {
        return (FALSE);
      }

      break;
 case NV_UE_IMEISV_SVN_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ue_imeisv_svn) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_PROFILE_I:
      if ( ! xdr_nv_send_nv_ds_sip_profile_type( xdrs, &(uptr->ds_sip_profile) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_ACTIVE_PROFILE_INDEX_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_sip_active_profile_index) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_NUM_VALID_PROFILES_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->ds_sip_num_valid_profiles) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_NAI_INFO_I:
      if ( ! xdr_nv_send_nv_ds_sip_profile_nai_info_type( xdrs, &(uptr->ds_sip_nai_info) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_PPP_SS_INFO_I:
      if ( ! xdr_nv_send_nv_ds_sip_ppp_ss_info_type( xdrs, &(uptr->ds_sip_ppp_ss_info) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DIAGBUF_TX_SLEEP_THRESHOLD_EXT_I:
      if ( ! XDR_SEND_PACKED_UINT64( xdrs, &(uptr->diag_diagbuf_tx_sleep_threshold_ext) ) ) {
        return (FALSE);
      }

      break;
 case NV_WWAN_ACCESS_OVER_WIFI_PREFERRED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->wwan_access_over_wifi_preferred) ) ) {
        return (FALSE);
      }

      break;
 case NV_WIFI_LOCAL_BREAKOUT_ALLOWED_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->wifi_local_breakout_allowed) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_OPERATOR_ID_I:
      if ( ! xdr_nv_send_nv_iwlan_operator_id_type( xdrs, &(uptr->iwlan_operator_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_REALM_I:
      if ( ! xdr_nv_send_nv_iwlan_realm_type( xdrs, &(uptr->iwlan_realm) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_AUTH_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->iwlan_auth_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_SOFT_IKE_REKEY_TIME_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_soft_ike_rekey_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_HARD_IKE_REKEY_TIME_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_hard_ike_rekey_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_SOFT_LIFETIME_SEC_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_soft_lifetime_sec) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_HARD_LIFETIME_SEC_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_hard_lifetime_sec) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_SOFT_LIFETIME_BYTES_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_soft_lifetime_bytes) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_HARD_LIFETIME_BYTES_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_hard_lifetime_bytes) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_NAT_KEEPALIVE_INTERVAL_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_nat_keepalive_interval) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_RETRANSMISSION_INTERVAL_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_retransmission_interval) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_MAX_RETRANSMISSIONS_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_max_retransmissions) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKEV2_IP_CONFIG_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_ikev2_ip_config_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_DHCP_IP_CONFIG_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_dhcp_ip_config_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_ENC_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_enc_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_SA_ENC_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_sa_enc_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_SA_PRF_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_sa_prf_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_AUTH_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_auth_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_SA_AUTH_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_sa_auth_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_DH_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->iwlan_ike_dh_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_PDIF_ADDRESS_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_pdif_address) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_MULTI_AUTH_SUPPORTED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->iwlan_multi_auth_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_PDIF_FQDN_I:
      if ( ! xdr_nv_send_nv_iwlan_pdif_fqdn_type( xdrs, &(uptr->iwlan_pdif_fqdn) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_DPD_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->iwlan_dpd_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_DPD_TIME_SECS_I:
      if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->iwlan_dpd_time_secs) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_REFCLOCK_TYPE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->bt_soc_refclock_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_CLK_SHARING_TYPE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->bt_soc_clk_sharing_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_INBAND_SLEEP_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->bt_soc_inband_sleep) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_LOGGING_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->bt_soc_logging_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_PM_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->bt_soc_pm_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_MGRF_SUPPORTED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->mgrf_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_USE_PMIC_OTG_COMPARATORS_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_use_pmic_otg_comparators) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_NUMBER_OF_SDCC_LUNS_I:
      if ( ! XDR_SEND_PACKED_UINT8( xdrs, &(uptr->hs_usb_number_of_sdcc_luns) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_EAP_SHARED_SECRET_I:
      if ( ! xdr_nv_send_nv_eap_shared_secret_type( xdrs, &(uptr->ppp_eap_shared_secret) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_VSNCP_CONFIG_DATA_I:
      if ( ! xdr_nv_send_nv_ppp_vsncp_config_data_type( xdrs, &(uptr->ppp_vsncp_config_data) ) ) {
        return (FALSE);
      }

      break;
 case NV_EHRPD_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ehrpd_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_ATHEROS_SPECIFIC_CFG_I:
      if ( ! xdr_nv_send_nv_wlan_atheros_specific_cfg( xdrs, &(uptr->wlan_atheros_specific_cfg) ) ) {
        return (FALSE);
      }

      break;
 case NV_GAN_MODE_PREFERENCE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->gan_mode_preference) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_ENFORCE_PEER_CERT_AUTH_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->iwlan_enforce_peer_cert_auth) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_HOST_MODE_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_host_mode_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_CPU_FLOW_CONTROL_CFG_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->wlan_cpu_flow_control_cfg) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_RECONNECT_ON_RESET_DURING_SUSPEND_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_reconnect_on_reset_during_suspend) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_DISABLE_SLEEP_VOTING_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_disable_sleep_voting) ) ) {
        return (FALSE);
      }

      break;
 case NV_EHRPD_MILENAGE_OP_I:
      if ( ! xdr_nv_send_nv_ehrpd_milenage_op_type( xdrs, &(uptr->ehrpd_milenage_op) ) ) {
        return (FALSE);
      }

      break;
 case NV_EHRPD_IMSI_I:
      if ( ! xdr_nv_send_nv_ehrpd_imsi_type( xdrs, &(uptr->ehrpd_imsi) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_UUID_I:
      if ( ! XDR_SEND_PACKED_BYTES(xdrs, uptr->wlan_uuid, 16) ) {
        return (FALSE);
      }

      break;
 case NV_QDJ_DEQUEUE_LOG_ENABLED_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->qdj_dequeue_log_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_QSOC_NVM_MODE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->bt_qsoc_nvm_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_QSOC_CLASS_TYPE_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->bt_qsoc_class_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_PERFORMANCE_SETTING_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->hs_usb_performance_setting) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_PAL_LINK_PREF_I:
      if ( ! XDR_SEND_PACKED_UINT16( xdrs, &(uptr->wlan_pal_link_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_DISABLE_LPM_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_disable_lpm) ) ) {
        return (FALSE);
      }

      break;
 case NV_OEM_ITEM_1_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_1[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_2_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_2[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_3_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_3[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_4_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_4[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_5_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_5[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_6_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_6[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_7_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_7[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_8_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_PACKED_UINT32( xdrs, &(uptr->oem_item_8[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_EHRPD_AUTH_IN_USIM_I:
      if ( ! XDR_SEND_PACKED_BOOLEAN( xdrs, &(uptr->ehrpd_auth_in_usim) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_PHY_CONFIG_I:
      if ( ! xdr_nv_send_nv_hs_usb_phy_config_type( xdrs, &(uptr->hs_usb_phy_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_CALL_OVER_EHRPD_ONLY_I:
      if ( ! xdr_nv_send_nv_data_call_over_ehrpd_only_type( xdrs, &(uptr->data_call_over_ehrpd_only) ) ) {
        return (FALSE);
      }

      break;
    default:

      XDR_UNKNOWN_DISCRIMINATOR_MSG(obj_disc);

  }

  return (TRUE);
} /* xdr_nv_send_nv_item_type */

/*
 * nv_item_type XDR_NV_RECV
 */
boolean xdr_nv_recv_nv_item_type (xdr_s_type *xdrs, nv_item_type *uptr, nv_items_enum_type obj_disc)
{
  int i;
  switch ( obj_disc ){
 case NV_ESN_I:
      if ( ! xdr_nv_recv_nv_esn_type( xdrs, &(uptr->esn) ) ) {
        return (FALSE);
      }

      break;
 case NV_ESN_CHKSUM_I:
      if ( ! xdr_nv_recv_nv_esn_chksum_type( xdrs, &(uptr->esn_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_SCM_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->scm) ) ) {
        return (FALSE);
      }

      break;
 case NV_SLOT_CYCLE_INDEX_I:
      if ( ! XDR_RECV_PACKED_INT8( xdrs, &(uptr->slot_cycle_index) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_CAI_REV_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->mob_cai_rev) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_FIRM_REV_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->mob_firm_rev) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_MODEL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->mob_model) ) ) {
        return (FALSE);
      }

      break;
 case NV_CONFIG_CHKSUM_I:
      if ( ! xdr_nv_recv_nv_config_chksum_type( xdrs, &(uptr->config_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_PREF_MODE_I:
      if ( ! xdr_nv_recv_nv_pref_mode_type( xdrs, &(uptr->pref_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_SID_LOCK_I:
      if ( ! xdr_nv_recv_nv_sid_lock_type( xdrs, &(uptr->cdma_sid_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_SID_ACQ_I:
      if ( ! xdr_nv_recv_nv_sid_acq_type( xdrs, &(uptr->cdma_sid_acq) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_SID_LOCK_I:
      if ( ! xdr_nv_recv_nv_sid_lock_type( xdrs, &(uptr->analog_sid_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_SID_ACQ_I:
      if ( ! xdr_nv_recv_nv_sid_acq_type( xdrs, &(uptr->analog_sid_acq) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_FIRSTCHP_I:
      if ( ! xdr_nv_recv_nv_firstchp_type( xdrs, &(uptr->analog_firstchp) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_HOME_SID_I:
      if ( ! xdr_nv_recv_nv_sid_type( xdrs, &(uptr->analog_home_sid) ) ) {
        return (FALSE);
      }

      break;
 case NV_ANALOG_REG_I:
      if ( ! xdr_nv_recv_nv_analog_reg_type( xdrs, &(uptr->analog_reg) ) ) {
        return (FALSE);
      }

      break;
 case NV_PCDMACH_I:
      if ( ! xdr_nv_recv_nv_cdmach_type( xdrs, &(uptr->pcdmach) ) ) {
        return (FALSE);
      }

      break;
 case NV_SCDMACH_I:
      if ( ! xdr_nv_recv_nv_cdmach_type( xdrs, &(uptr->scdmach) ) ) {
        return (FALSE);
      }

      break;
 case NV_NAM_CHKSUM_I:
      if ( ! xdr_nv_recv_nv_nam_chksum_type( xdrs, &(uptr->nam_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_A_KEY_I:
      if ( ! xdr_nv_recv_nv_a_key_type( xdrs, &(uptr->a_key) ) ) {
        return (FALSE);
      }

      break;
 case NV_A_KEY_CHKSUM_I:
      if ( ! xdr_nv_recv_nv_a_key_chksum_type( xdrs, &(uptr->a_key_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_SSD_A_I:
      if ( ! xdr_nv_recv_nv_ssd_type( xdrs, &(uptr->ssd_a) ) ) {
        return (FALSE);
      }

      break;
 case NV_SSD_B_I:
      if ( ! xdr_nv_recv_nv_ssd_type( xdrs, &(uptr->ssd_b) ) ) {
        return (FALSE);
      }

      break;
 case NV_COUNT_I:
      if ( ! xdr_nv_recv_nv_count_type( xdrs, &(uptr->count) ) ) {
        return (FALSE);
      }

      break;
 case NV_MIN1_I:
      if ( ! xdr_nv_recv_nv_min1_type( xdrs, &(uptr->min1) ) ) {
        return (FALSE);
      }

      break;
 case NV_MIN2_I:
      if ( ! xdr_nv_recv_nv_min2_type( xdrs, &(uptr->min2) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_TERM_HOME_I:
      if ( ! xdr_nv_recv_nv_mob_term_type( xdrs, &(uptr->mob_term_home) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_TERM_FOR_SID_I:
      if ( ! xdr_nv_recv_nv_mob_term_type( xdrs, &(uptr->mob_term_for_sid) ) ) {
        return (FALSE);
      }

      break;
 case NV_MOB_TERM_FOR_NID_I:
      if ( ! xdr_nv_recv_nv_mob_term_type( xdrs, &(uptr->mob_term_for_nid) ) ) {
        return (FALSE);
      }

      break;
 case NV_ACCOLC_I:
      if ( ! xdr_nv_recv_nv_accolc_type( xdrs, &(uptr->accolc) ) ) {
        return (FALSE);
      }

      break;
 case NV_SID_NID_I:
      if ( ! xdr_nv_recv_nv_sid_nid_type( xdrs, &(uptr->sid_nid) ) ) {
        return (FALSE);
      }

      break;
 case NV_MIN_CHKSUM_I:
      if ( ! xdr_nv_recv_nv_min_chksum_type( xdrs, &(uptr->min_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_CURR_NAM_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->curr_nam) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_NAM_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auto_nam) ) ) {
        return (FALSE);
      }

      break;
 case NV_NAME_NAM_I:
      if ( ! xdr_nv_recv_nv_name_nam_type( xdrs, &(uptr->name_nam) ) ) {
        return (FALSE);
      }

      break;
 case NV_AIR_TIMER_I:
      if ( ! xdr_nv_recv_nv_call_time_type( xdrs, &(uptr->air_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAM_TIMER_I:
      if ( ! xdr_nv_recv_nv_call_time_type( xdrs, &(uptr->roam_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_RUN_TIMER_I:
      if ( ! xdr_nv_recv_nv_run_time_type( xdrs, &(uptr->run_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAL_I:
      if ( ! xdr_nv_recv_nv_dial_type( xdrs, &(uptr->dial) ) ) {
        return (FALSE);
      }

      break;
 case NV_STACK_I:
      if ( ! xdr_nv_recv_nv_stdial_type( xdrs, &(uptr->stack) ) ) {
        return (FALSE);
      }

      break;
 case NV_STACK_IDX_I:
      if ( ! xdr_nv_recv_nv_stack_idx_type( xdrs, &(uptr->stack_idx) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAGE_SET_I:
      if ( ! xdr_nv_recv_nv_page_set_type( xdrs, &(uptr->page_set) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAGE_MSG_I:
      if ( ! xdr_nv_recv_nv_page_msg_type( xdrs, &(uptr->page_msg) ) ) {
        return (FALSE);
      }

      break;
 case NV_EAR_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ear_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_SPEAKER_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->speaker_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_RINGER_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ringer_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->beep_level) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_BEEP_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->call_beep) ) ) {
        return (FALSE);
      }

      break;
 case NV_CONT_KEY_DTMF_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->cont_key_dtmf) ) ) {
        return (FALSE);
      }

      break;
 case NV_CONT_STR_DTMF_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->cont_str_dtmf) ) ) {
        return (FALSE);
      }

      break;
 case NV_SVC_AREA_ALERT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->svc_area_alert) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_FADE_ALERT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->call_fade_alert) ) ) {
        return (FALSE);
      }

      break;
 case NV_BANNER_I:
      if ( ! xdr_nv_recv_nv_banner_type( xdrs, &(uptr->banner) ) ) {
        return (FALSE);
      }

      break;
 case NV_LCD_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->lcd) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_ANSWER_I:
      if ( ! xdr_nv_recv_nv_auto_answer_type( xdrs, &(uptr->auto_answer) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_REDIAL_I:
      if ( ! xdr_nv_recv_nv_auto_redial_type( xdrs, &(uptr->auto_redial) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_HYPHEN_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auto_hyphen) ) ) {
        return (FALSE);
      }

      break;
 case NV_BACK_LIGHT_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->back_light) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_MUTE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auto_mute) ) ) {
        return (FALSE);
      }

      break;
 case NV_LOCK_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_LOCK_CODE_I:
      if ( ! xdr_nv_recv_nv_lock_code_type( xdrs, &(uptr->lock_code) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_LOCK_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auto_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_RSTRC_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->call_rstrc) ) ) {
        return (FALSE);
      }

      break;
 case NV_SEC_CODE_I:
      if ( ! xdr_nv_recv_nv_sec_code_type( xdrs, &(uptr->sec_code) ) ) {
        return (FALSE);
      }

      break;
 case NV_HORN_ALERT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->horn_alert) ) ) {
        return (FALSE);
      }

      break;
 case NV_ERR_LOG_I:
      if ( ! xdr_nv_recv_nv_err_log_type( xdrs, &(uptr->err_log) ) ) {
        return (FALSE);
      }

      break;
 case NV_VBATT_I:
      if ( ! xdr_nv_recv_nv_minmax_type( xdrs, &(uptr->vbatt) ) ) {
        return (FALSE);
      }

      break;
 case NV_SPARE_2_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->spare_2) ) ) {
        return (FALSE);
      }

      break;
 case NV_MAX_TX_ADJ_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->max_tx_adj) ) ) {
        return (FALSE);
      }

      break;
 case NV_INI_MUTE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->init_mute) ) ) {
        return (FALSE);
      }

      break;
 case NV_FACTORY_INFO_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->fact_info, 100) ) {
        return (FALSE);
      }

      break;
 case NV_DM_ADDR_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->dm_addr) ) ) {
        return (FALSE);
      }

      break;
 case NV_ONETOUCH_DIAL_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->onetouch_dial) ) ) {
        return (FALSE);
      }

      break;
 case NV_ALERTS_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->alerts_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_ALERTS_LVL_SHADOW_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->alerts_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_LVL_SHADOW_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->beep_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_EAR_LVL_SHADOW_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ear_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_AIR_CNT_I:
      if ( ! xdr_nv_recv_nv_call_cnt_type( xdrs, &(uptr->air_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAM_CNT_I:
      if ( ! xdr_nv_recv_nv_call_cnt_type( xdrs, &(uptr->roam_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_LIFE_CNT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->life_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_MCC_I:
      if ( ! xdr_nv_recv_nv_imsi_mcc_type( xdrs, &(uptr->imsi_mcc) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_11_12_I:
      if ( ! xdr_nv_recv_nv_imsi_11_12_type( xdrs, &(uptr->imsi_11_12) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIR_NUMBER_I:
      if ( ! xdr_nv_recv_nv_dir_number_type( xdrs, &(uptr->dir_number) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOICE_PRIV_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->voice_priv) ) ) {
        return (FALSE);
      }

      break;
 case NV_FSC_I:
      if ( ! xdr_nv_recv_nv_fsc_type( xdrs, &(uptr->fsc) ) ) {
        return (FALSE);
      }

      break;
 case NV_LIFE_TIMER_G_I:
      if ( ! xdr_nv_recv_nv_run_time_type( xdrs, &(uptr->life_timer_g) ) ) {
        return (FALSE);
      }

      break;
 case NV_CALL_TIMER_G_I:
      if ( ! xdr_nv_recv_nv_run_time_type( xdrs, &(uptr->call_timer_g) ) ) {
        return (FALSE);
      }

      break;
 case NV_PWR_DWN_CNT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->pwr_dwn_cnt) ) ) {
        return (FALSE);
      }

      break;
 case NV_FSC2_I:
      if ( ! xdr_nv_recv_nv_fsc2_type( xdrs, &(uptr->fsc2) ) ) {
        return (FALSE);
      }

      break;
 case NV_FSC2_CHKSUM_I:
      if ( ! xdr_nv_recv_nv_fsc2_chksum_type( xdrs, &(uptr->fsc2_chksum) ) ) {
        return (FALSE);
      }

      break;
 case NV_WDC_I:
      if ( ! xdr_nv_recv_nv_wdc_type( xdrs, &(uptr->wdc) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_ADDR_NUM_I:
      if ( ! xdr_nv_recv_nv_imsi_addr_num_type( xdrs, &(uptr->imsi_addr_num) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIR_NUMBER_PCS_I:
      if ( ! xdr_nv_recv_nv_mob_dir_data_type( xdrs, &(uptr->mob_dir_number) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAMING_LIST_I:
      if ( ! xdr_nv_recv_nv_roaming_list_union_type( xdrs, &(uptr->roaming_list) ) ) {
        return (FALSE);
      }

      break;
 case NV_OTKSL_I:
      if ( ! xdr_nv_recv_nv_sec_code_type( xdrs, &(uptr->otksl) ) ) {
        return (FALSE);
      }

      break;
 case NV_TIMED_PREF_MODE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->timed_pref_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_RINGER_TYPE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ringer_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_BACK_LIGHT_HFK_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->back_light_hfk) ) ) {
        return (FALSE);
      }

      break;
 case NV_KEY_SOUND_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->key_sound) ) ) {
        return (FALSE);
      }

      break;
 case NV_LANGUAGE_SELECTION_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->language_selection) ) ) {
        return (FALSE);
      }

      break;
 case NV_MENU_FORMAT_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->menu_format) ) ) {
        return (FALSE);
      }

      break;
 case NV_RINGER_SPKR_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ringer_spkr_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_SPKR_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->beep_spkr_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_VIBRATOR_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->vibrator) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_QNC_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->data_qnc_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_SO_SET_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->data_so_set) ) ) {
        return (FALSE);
      }

      break;
 case NV_SID_NID_LOCK_I:
      if ( ! xdr_nv_recv_nv_sid_nid_lock_type( xdrs, &(uptr->sid_nid_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_PRL_ENABLED_I:
      if ( ! xdr_nv_recv_nv_enabled_type( xdrs, &(uptr->prl_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAMING_LIST_683_I:
      if ( ! xdr_nv_recv_nv_roaming_list_union_type( xdrs, &(uptr->roaming_list_683) ) ) {
        return (FALSE);
      }

      break;
 case NV_SYSTEM_PREF_I:
      if ( ! xdr_nv_recv_nv_sys_pref_type( xdrs, &(uptr->system_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_HOME_SID_NID_I:
      if ( ! xdr_nv_recv_nv_home_sid_nid_type( xdrs, &(uptr->home_sid_nid) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_S1_I:
      if ( ! xdr_nv_recv_nv_min1_type( xdrs, &(uptr->imsi_t_s1) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_S2_I:
      if ( ! xdr_nv_recv_nv_min2_type( xdrs, &(uptr->imsi_t_s2) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_MCC_I:
      if ( ! xdr_nv_recv_nv_imsi_mcc_type( xdrs, &(uptr->imsi_t_mcc) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_11_12_I:
      if ( ! xdr_nv_recv_nv_imsi_11_12_type( xdrs, &(uptr->imsi_t_11_12) ) ) {
        return (FALSE);
      }

      break;
 case NV_IMSI_T_ADDR_NUM_I:
      if ( ! xdr_nv_recv_nv_imsi_addr_num_type( xdrs, &(uptr->imsi_t_addr_num) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_UTC_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->sms_utc) ) ) {
        return (FALSE);
      }

      break;
 case NV_ROAM_RINGER_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->roam_ringer) ) ) {
        return (FALSE);
      }

      break;
 case NV_PREF_VOICE_SO_I:
      if ( ! xdr_nv_recv_nv_pref_voice_so_type( xdrs, &(uptr->pref_voice_so) ) ) {
        return (FALSE);
      }

      break;
 case NV_SPC_CHANGE_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->spc_change_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_MDR_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->data_mdr_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_PKT_ORIG_STR_I:
      if ( ! xdr_nv_recv_nv_data_pkt_orig_str_type( xdrs, &(uptr->data_pkt_orig_str) ) ) {
        return (FALSE);
      }

      break;
 case NV_UP_KEY_I:
      if ( ! xdr_nv_recv_nv_up_key_type( xdrs, &(uptr->up_key) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_VOLUME_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auto_volume_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAP_USER_ID_I:
      if ( ! xdr_nv_recv_nv_pap_user_id_type( xdrs, &(uptr->pap_user_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_PAP_PASSWORD_I:
      if ( ! xdr_nv_recv_nv_pap_password_type( xdrs, &(uptr->pap_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_BC_SERVICE_TABLE_SIZE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->sms_bc_service_table_size) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_BC_SERVICE_TABLE_I:
      if ( ! xdr_nv_recv_nv_sms_bc_service_table_type( xdrs, &(uptr->sms_bc_service_table) ) ) {
        return (FALSE);
      }

      break;
 case NV_RUIM_CHV_1_I:
      if ( ! xdr_nv_recv_nv_ruim_chv_type( xdrs, &(uptr->ruim_chv_1) ) ) {
        return (FALSE);
      }

      break;
 case NV_RUIM_CHV_2_I:
      if ( ! xdr_nv_recv_nv_ruim_chv_type( xdrs, &(uptr->ruim_chv_2) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_PDE_ADDRESS_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->gps1_pde_address) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_PDE_TRANSPORT_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->gps1_pde_transport) ) ) {
        return (FALSE);
      }

      break;
 case NV_TTY_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->tty) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_SDAC_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->mm_sdac_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_BEEP_SDAC_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->beep_sdac_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_SDAC_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->sdac_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->mm_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_LVL_SHADOW_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->mm_lvl_shadow) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_SPEAKER_LVL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->mm_speaker_lvl) ) ) {
        return (FALSE);
      }

      break;
 case NV_PRIMARY_DNS_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->primary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_SECONDARY_DNS_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->secondary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_PDE_PORT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->gps1_pde_port) ) ) {
        return (FALSE);
      }

      break;
 case NV_MM_RINGER_FILE_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->mm_ringer_file, 40) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_SCRM_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->data_scrm_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_BD_ADDR_I:
      if ( ! xdr_nv_recv_nv_bd_addr_type( xdrs, &(uptr->bd_addr) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_TRTL_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->data_trtl_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_LOCK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->gps1_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_FTM_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ftm_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_DEFAULT_BAUDRATE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->ds_default_baudrate) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEFAULT_BAUDRATE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->diag_default_baudrate) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_QCMIP_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_qcmip) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RETRIES_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_mip_retries) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RETRY_INT_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_mip_retry_int) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_PRE_RE_RRQ_TIME_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_mip_pre_re_rrq_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_ACTIVE_PROF_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_mip_active_prof) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_GEN_USER_PROF_I:
      if ( ! xdr_nv_recv_nv_ds_mip_gen_user_prof_type( xdrs, &(uptr->ds_mip_gen_user_prof) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_SS_USER_PROF_I:
      if ( ! xdr_nv_recv_nv_ds_mip_ss_user_prof_type( xdrs, &(uptr->ds_mip_ss_user_prof) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRAMP_ADDRESS_DATA_I:
      if ( ! xdr_nv_recv_nv_hdramp_address_data_type( xdrs, &(uptr->hdramp_address_data) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCP_SESSION_STATUS_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hdrscp_session_status) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_MN_HA_TIME_DELTA_I:
      if ( ! xdr_nv_recv_nv_ds_mip_mn_ha_time_delta_type( xdrs, &(uptr->ds_mip_mn_ha_time_delta) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_QC_DRS_OPT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_qc_drs_opt) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_2002BIS_MN_HA_AUTH_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_2002bis_mn_ha_auth) ) ) {
        return (FALSE);
      }

      break;
 case NV_UE_IMEI_I:
      if ( ! xdr_nv_recv_nv_ue_imei_type( xdrs, &(uptr->ue_imei) ) ) {
        return (FALSE);
      }

      break;
 case NV_GSM_A5_ALGORITHMS_SUPPORTED_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->gsm_a5_algorithms_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_VM_NUMBER_I:
      if ( ! xdr_nv_recv_nv_sms_vm_number_type( xdrs, &(uptr->sms_vm_number) ) ) {
        return (FALSE);
      }

      break;
 case NV_HYBRID_PREF_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->hybrid_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_SP_ECC_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->sp_ecc_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_ECC_LIST_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (10); i++ ) {
        /*lint -save -e545*/
        if ( ! xdr_nv_recv_union_nv_item_type_ecc_list_arr( xdrs, &(uptr->ecc_list[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_RF_CAL_VER_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->rf_cal_ver, 8) ) {
        return (FALSE);
      }

      break;
 case NV_RF_CAL_DATE_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->rf_cal_date) ) ) {
        return (FALSE);
      }

      break;
 case NV_DOMAIN_NAME_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->domain_name, 100) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_NAI_I:
      if ( ! xdr_nv_recv_nv_hdr_an_auth_nai_type( xdrs, &(uptr->hdr_an_auth_nai) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_PASSWORD_I:
      if ( ! xdr_nv_recv_nv_hdr_an_auth_password_type( xdrs, &(uptr->hdr_an_auth_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RRQ_IF_TFRK_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_rrq_if_tfrk) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_ENABLE_PROF_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->ds_mip_enable_prof, 6) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_RX_DIVERSITY_CTRL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->hdr_rx_diversity_ctrl) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_CFG_ROUTING_I:
      if ( ! xdr_nv_recv_nv_sms_routing_type( xdrs, &(uptr->sms_cfg_routing) ) ) {
        return (FALSE);
      }

      break;
 case NV_SERVICE_DOMAIN_PREF_I:
      if ( ! xdr_nv_recv_nv_srv_domain_pref_type( xdrs, &(uptr->service_domain_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_DMU_PKOID_I:
      if ( ! xdr_nv_recv_nv_ds_mip_dmu_pkoid_type( xdrs, &(uptr->ds_mip_dmu_pkoid) ) ) {
        return (FALSE);
      }

      break;
 case NV_RTRE_CONFIG_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->rtre_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_DYNAMIC_RANGE_I:
      if ( ! XDR_RECV_PACKED_INT16( xdrs, &(uptr->cdma_dynamic_range) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_MIN_RX_RSSI_I:
      if ( ! XDR_RECV_PACKED_INT16( xdrs, &(uptr->cdma_min_rx_rssi) ) ) {
        return (FALSE);
      }

      break;
 case NV_LAST_TX_DATA_COUNT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->last_tx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_LAST_RX_DATA_COUNT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->last_rx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_TOTAL_TX_DATA_COUNT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->total_tx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_TOTAL_RX_DATA_COUNT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->total_rx_data_count) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_DMU_MN_AUTH_I:
      if ( ! xdr_nv_recv_nv_ds_mip_dmu_mn_auth_type( xdrs, &(uptr->ds_mip_dmu_mn_auth) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_M511_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->jcdma_m511_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_M512_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->jcdma_m512_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_M513_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->jcdma_m513_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_ERR_FATAL_OPTIONS_I:
      if ( ! xdr_nv_recv_nv_err_fatal_options_type( xdrs, &(uptr->err_fatal_options) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_PASSWORD_I:
      if ( ! xdr_nv_recv_nv_ppp_password_type( xdrs, &(uptr->ppp_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_USER_ID_I:
      if ( ! xdr_nv_recv_nv_ppp_user_id_type( xdrs, &(uptr->ppp_user_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_UIM_PREF_PROTOCOL_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->uim_pref_protocol) ) ) {
        return (FALSE);
      }

      break;
 case NV_WCDMACS_SYNC_TEST_APP_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->wcdmacs_sync_test_app_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_PPP_PASSWORD_I:
      if ( ! xdr_nv_recv_nv_hdr_an_ppp_password_type( xdrs, &(uptr->hdr_an_ppp_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_RTSP_PROXY_SERVER_ADDR_I:
      if ( ! xdr_nv_recv_nv_rtsp_proxy_server_addr_type( xdrs, &(uptr->rtsp_proxy_server_addr) ) ) {
        return (FALSE);
      }

      break;
 case NV_HYSTERISIS_ACTIVATION_TIMER_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hysterisis_activation_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_HYSTERISIS_TIMER_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hysterisis_timer) ) ) {
        return (FALSE);
      }

      break;
 case NV_PKT_DIAL_STRINGS_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (4); i++ ) {
        /*lint -save -e545*/
        if ( ! xdr_nv_recv_nv_pkt_dial_string_type( xdrs, &(uptr->pkt_dial_strings[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_DATA_INCOMING_CSD_CALL_IS_INTERNAL_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->data_incoming_csd_call_is_internal) ) ) {
        return (FALSE);
      }

      break;
 case NV_SSPR_P_REV_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->sspr_p_rev) ) ) {
        return (FALSE);
      }

      break;
 case NV_RTSP_PROXY_SERVER_ADDR_ALTERNATE_I:
      if ( ! xdr_nv_recv_nv_rtsp_proxy_server_addr_type( xdrs, &(uptr->rtsp_proxy_server_addr_alternate) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPRS_ANITE_GCF_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->gprs_anite_gcf) ) ) {
        return (FALSE);
      }

      break;
 case NV_CDMA_RX_DIVERSITY_CTRL_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->cdma_rx_diversity_ctrl) ) ) {
        return (FALSE);
      }

      break;
 case NV_LONG_PDPACT_TOLERANCE_FOR_TE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->long_pdpact_tolerance_for_te) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_PASSWD_LONG_I:
      if ( ! xdr_nv_recv_nv_hdr_an_auth_passwd_long_type( xdrs, &(uptr->hdr_an_auth_passwd_long) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_QC_HANDDOWN_TO_1X_OPT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ds_mip_qc_handdown_to_1x_opt) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_AN_AUTH_USER_ID_LONG_I:
      if ( ! xdr_nv_recv_nv_hdr_an_auth_user_id_long_type( xdrs, &(uptr->hdr_an_auth_user_id_long) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_CONFIG_DATA_I:
      if ( ! xdr_nv_recv_nv_ppp_configure_data_type( xdrs, &(uptr->ppp_config_data) ) ) {
        return (FALSE);
      }

      break;
 case NV_GSM_AMR_CALL_CONFIG_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->gsm_amr_call_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_SEC_DEVICE_KEY_I:
      if ( ! xdr_nv_recv_nv_sec_device_key_type( xdrs, &(uptr->sec_device_key) ) ) {
        return (FALSE);
      }

      break;
 case NV_TCP_GRACEFUL_DORMANT_CLOSE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->tcp_graceful_dormant_close) ) ) {
        return (FALSE);
      }

      break;
 case NV_EPLMN_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->eplmn_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DTMF_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->dtmf) ) ) {
        return (FALSE);
      }

      break;
 case NV_SN_PPP_STATUS_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->sn_ppp_status) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RLP_VERSION_GSM_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_ucsd_rlp_version_gsm) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RLP_VERSION_WCDMA_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_ucsd_rlp_version_wcdma) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_ATCOP_RLP_VERSION_DEFAULT_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_atcop_rlp_version_default) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RECOVERY_MODE_SREJ_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ds_ucsd_recovery_mode_srej) ) ) {
        return (FALSE);
      }

      break;
 case NV_TCP_KEEPALIVE_IDLE_TIME_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->tcp_keepalive_idle_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_FORCE_CDMA_PKT_STR_I:
      if ( ! xdr_nv_recv_nv_data_pkt_orig_str_type( xdrs, &(uptr->data_force_cdma_pkt_str) ) ) {
        return (FALSE);
      }

      break;
 case NV_PS_IPV6_IID_I:
      if ( ! xdr_nv_recv_nv_ps_ipv6_iid_type( xdrs, &(uptr->ps_ipv6_iid) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEBUG_CONTROL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->diag_debug_control) ) ) {
        return (FALSE);
      }

      break;
 case NV_QVP_APP_DEFAULT_CAPABILITY_TYPE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->qvp_app_default_capability_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEBUG_DETAIL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->diag_debug_detail) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ipv6_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_SM_CONFIG_I:
      if ( ! xdr_nv_recv_nv_ip6_sm_config_type( xdrs, &(uptr->ipv6_sm_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_PRIMARY_DNS_I:
      if ( ! xdr_nv_recv_nv_ipv6_address_type( xdrs, &(uptr->ipv6_primary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_SECONDARY_DNS_I:
      if ( ! xdr_nv_recv_nv_ipv6_address_type( xdrs, &(uptr->ipv6_secondary_dns) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTH_REQUIRE_PASSWORD_ENCRYPTION_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auth_require_password_encryption) ) ) {
        return (FALSE);
      }

      break;
 case NV_PDSN_AS_PROXY_IPV6_DNS_SERVER_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->pdsn_as_proxy_ipv6_dns_server) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_FAILOVER_CONFIG_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ipv6_failover_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_MEID_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (2); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->meid[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_DIAG_DEBUG_MAX_FILES_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->diag_debug_max_files) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_CARRIER_ID_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->brew_carrier_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_PLATFORM_ID_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->brew_platform_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_BKEY_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->brew_bkey, 16) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_SERVER_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->brew_server, 64) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_DOWNLOAD_FLAGS_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->brew_download_flags) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_AUTH_POLICY_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->brew_auth_policy) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_PRIVACY_POLICY_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->brew_privacy_policy) ) ) {
        return (FALSE);
      }

      break;
 case NV_BREW_SUBSCRIBER_ID_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->brew_subscriber_id, 32) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_RLP_ADM_IDLE_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->ds_ucsd_rlp_adm_idle) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_CCP_REJECT_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ppp_ccp_reject) ) ) {
        return (FALSE);
      }

      break;
 case NV_USB_CURRENT_DEVICE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->usb_current_device) ) ) {
        return (FALSE);
      }

      break;
 case NV_ACTIVE_EPZID_TYPE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->active_epzid_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_SW_VERSION_INFO_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->sw_version_info, 30) ) {
        return (FALSE);
      }

      break;
 case NV_SET_AUTO_TIME_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->set_auto_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_INCOMING_DATA_RLP_TIMEOUT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->ds_incoming_data_rlp_timeout) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_DEFAULT_INACTIVITY_RESTORE_TIMEOUT_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->ds_default_inactivity_restore_timeout) ) ) {
        return (FALSE);
      }

      break;
 case NV_CHG_USB_NVDISABLE_VAL_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->chg_usb_nvdisable_val) ) ) {
        return (FALSE);
      }

      break;
 case NV_DEVICE_SERIAL_NO_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->device_serial_no, 16) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_RM_NAI_I:
      if ( ! xdr_nv_recv_nv_ds_mip_rm_nai_type( xdrs, &(uptr->ds_mip_rm_nai) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_ACTIVE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->bt_active) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_VISIBLE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->bt_visible) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SAP_ENABLE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->bt_sap_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_TIMEZONE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->timezone) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_RM_NAI_I:
      if ( ! xdr_nv_recv_nv_ds_sip_rm_nai_type( xdrs, &(uptr->ds_sip_rm_nai) ) ) {
        return (FALSE);
      }

      break;
 case NV_BROWSER_MAILTO_EMAIL_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->browser_mailto_email) ) ) {
        return (FALSE);
      }

      break;
 case NV_IRDA_PNP_MANUFACTURER_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->irda_pnp_manufacturer, 31) ) {
        return (FALSE);
      }

      break;
 case NV_IRDA_PNP_NAME_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->irda_pnp_name, 91) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DEBUG_CALL_TRACE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->diag_debug_call_trace) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_SPC_UNLOCK_TTL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->diag_spc_unlock_ttl) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_UMTS_FAILOVER_CONFIG_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ipv6_umts_failover_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_SET_RINGER_VOLUME_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->set_ringer_volume) ) ) {
        return (FALSE);
      }

      break;
 case NV_MANUAL_PLMN_SEL_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->manual_plmn_sel) ) ) {
        return (FALSE);
      }

      break;
 case NV_MANUAL_PLMN_SELECTION_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->manual_plmn_selection) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_ENABLE_PS_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->wlan_enable_ps_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRMRLP_NUM_BYTES_TO_LOG_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hdrmrlp_num_bytes_to_log) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_UCSD_NT_CONNECT_STATE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_ucsd_nt_connect_state) ) ) {
        return (FALSE);
      }

      break;
 case NV_CCBS_SEL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ccbs_sel) ) ) {
        return (FALSE);
      }

      break;
 case NV_TRM_CONFIG_I:
      if ( ! xdr_nv_recv_nv_trm_config_type( xdrs, &(uptr->trm_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_ENS_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ens_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_SPC_TIMEOUT_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->diag_spc_timeout) ) ) {
        return (FALSE);
      }

      break;
 case NV_BACK_LIGHT_INTENSITY_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->back_light_intensity) ) ) {
        return (FALSE);
      }

      break;
 case NV_DISPLAY_DURATION_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->display_duration) ) ) {
        return (FALSE);
      }

      break;
 case NV_TIMEZONE_CITY_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->timezone_city) ) ) {
        return (FALSE);
      }

      break;
 case NV_AUTO_TIME_ENABLE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->auto_time_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_RETRY_PERIOD_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->sms_mo_retry_period) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_RETRY_INTERVAL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->sms_mo_retry_interval) ) ) {
        return (FALSE);
      }

      break;
 case NV_RMNET_AUTOCONNECT_I:
      if ( ! xdr_nv_recv_nv_rmnet_autoconnect_type( xdrs, &(uptr->rmnet_autoconnect) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_EVDO_SIP_PROFILE_ID_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_qos_evdo_sip_profile_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_EVDO_RTP_PROFILE_ID_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_qos_evdo_rtp_profile_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_EVDO_DSCP_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_qos_evdo_dscp) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_DTX_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->voip_dtx_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WCDMA_RRC_VERSION_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->wcdma_rrc_version) ) ) {
        return (FALSE);
      }

      break;
 case NV_NITZ_NW_INFO_I:
      if ( ! xdr_nv_recv_nv_nitz_plmn_type( xdrs, &(uptr->nitz_nw_info) ) ) {
        return (FALSE);
      }

      break;
 case NV_DQOS_SUPPORTED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->dqos_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_WLAN_PRFRD_CODEC_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_wlan_prfrd_codec) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_EVDO_PRFRD_CODEC_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_evdo_prfrd_codec) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_QOS_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->wlan_qos_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_GAME_MODE_PERSIST_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->game_mode_persist) ) ) {
        return (FALSE);
      }

      break;
 case NV_GAME_MODE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->game_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_QOS_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->voip_qos_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_WRITE_MEDIA_FILE_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->voip_write_media_file_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_READ_MEDIA_FILE_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->voip_read_media_file_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_OEM_INDEX_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->wlan_oem_index) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCP_FORCE_REL0_CONFIG_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->hdrscp_force_rel0_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCMDB_SECURE_CONFIG_I:
      if ( ! xdr_nv_recv_nv_hdrscmdb_secure_config_type( xdrs, &(uptr->hdrscmdb_secure_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MAX_PAYLOAD_LENGTH_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->sms_max_payload_length) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_ON_ACCESS_CHANNEL_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->sms_mo_on_access_channel) ) ) {
        return (FALSE);
      }

      break;
 case NV_SMS_MO_ON_TRAFFIC_CHANNEL_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->sms_mo_on_traffic_channel) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_CONFRD_URI_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_confrd_uri) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_QOS_FLOW_PRIORITY_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->hdr_qos_flow_priority) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_REGISTRATION_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->voip_registration_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_DCVS_ACPU_ENABLE_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->dcvs_acpu_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPSONE_PASSWORD_I:
      if ( ! xdr_nv_recv_nv_gpsone_password_type( xdrs, &(uptr->gpsone_password) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_CALLERID_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_callerid_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_MULTIDOMAIN_CAPABILITY_PREF_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->wlan_multidomain_capability_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_UMTS_NBNS_ENABLE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->umts_nbns_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_REFER_SUBSCRIPTION_EXPIRES_DURATION_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_refer_subscription_expires_duration) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_NOTIFY_REFER_RESPONSE_DURATION_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->voip_notify_refer_response_duration) ) ) {
        return (FALSE);
      }

      break;
 case NV_DCVSAPPS_NV_I:
      if ( ! xdr_nv_recv_nv_dcvsapps_type( xdrs, &(uptr->dcvsapps_nv) ) ) {
        return (FALSE);
      }

      break;
 case NV_GPS1_SEC_UPDATE_RATE_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->gps1_sec_update_rate) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_MIP_DEREG_RETRIES_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_mip_dereg_retries) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_PRIVACY_EXTENSIONS_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ipv6_privacy_extensions_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_IPV6_PRIVATE_ADDRESS_CONFIG_I:
      if ( ! xdr_nv_recv_nv_ipv6_priv_addr_config( xdrs, &(uptr->ipv6_private_address_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_DISABLED_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->bt_disabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_CURRENT_COMPOSITION_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hs_usb_current_composition) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_ENABLE_BT_COEX_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->wlan_enable_bt_coex) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDR_EMPA_SUPPORTED_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->hdr_empa_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_VOIP_PRECONDITION_ENABLE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->voip_precondition_enable) ) ) {
        return (FALSE);
      }

      break;
 case NV_DB_LP_SEC_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->db_lp_sec) ) ) {
        return (FALSE);
      }

      break;
 case NV_DB_LTM_OFF_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->db_ltm_off) ) ) {
        return (FALSE);
      }

      break;
 case NV_DB_DAYLT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->db_daylt) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS707_GO_NULL_TIMER_1X_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->ds707_go_null_timer_1x) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS707_GO_NULL_TIMER_DO_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->ds707_go_null_timer_do) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_MAC_ADDRESS_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->wlan_mac_address, 6) ) {
        return (FALSE);
      }

      break;
 case NV_DISPLAY_UPDATE_POWER_TEST_MODE_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->display_update_power_test_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_QVP_APP_PSVT_ENABLE_FLAG_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->qvp_app_psvt_enable_flag) ) ) {
        return (FALSE);
      }

      break;
 case NV_ICMP6_ND_CONFIG_I:
      if ( ! xdr_nv_recv_nv_nd_config_items( xdrs, &(uptr->icmp6_nd_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_REMOTE_WAKEUP_DELAY_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->hs_usb_remote_wakeup_delay) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_RUIM_ID_I:
      if ( ! xdr_nv_recv_nv_jcdma_ruimid( xdrs, &(uptr->jcdma_ruim_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_JCDMA_UIM_LOCK_I:
      if ( ! xdr_nv_recv_nv_jcdma_uim_lock_data( xdrs, &(uptr->jcdma_uim_lock) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_FTM_MODE_SWITCH_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->diag_ftm_mode_switch) ) ) {
        return (FALSE);
      }

      break;
 case NV_DHCP4_OPTIONS_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->dhcp4_options_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_DHCP6_OPTIONS_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->dhcp6_options_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_V250_REGISTERS_I:
      if ( ! xdr_nv_recv_nv_ds_at_v250_registers_type( xdrs, &(uptr->ds_at_v250_registers) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_S_REGISTERS_I:
      if ( ! xdr_nv_recv_nv_ds_at_s_registers_type( xdrs, &(uptr->ds_at_s_registers) ) ) {
        return (FALSE);
      }

      break;
 case NV_PROCESS_RESTART_SWITCH_I:
      if ( ! xdr_nv_recv_nv_process_restart_switch_type( xdrs, &(uptr->process_restart_switch) ) ) {
        return (FALSE);
      }

      break;
 case NV_HDRSCP_FORCE_AT_CONFIG_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hdrscp_force_at_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_DIAG_ON_LEGACY_USB_PORT_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_diag_on_legacy_usb_port) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DIAGBUF_TX_SLEEP_TIME_NV_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->diag_diagbuf_tx_sleep_time_nv) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_LISBON_DISABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->bt_lisbon_disabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_CSAS_CSCA_SETTINGS_I:
      if ( ! xdr_nv_recv_nv_ds_at_csas_csca_register( xdrs, &(uptr->ds_at_csas_csca_settings) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_CSAS_CSMP_SETTINGS_I:
      if ( ! xdr_nv_recv_nv_ds_at_csas_csmp_settings( xdrs, &(uptr->ds_at_csas_csmp_settings) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_AT_CSAS_CSCB_SETTINGS_I:
      if ( ! xdr_nv_recv_nv_ds_at_csas_cscb_settings( xdrs, &(uptr->ds_at_csas_cscb_settings) ) ) {
        return (FALSE);
      }

      break;
 case NV_UE_IMEISV_SVN_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ue_imeisv_svn) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_PROFILE_I:
      if ( ! xdr_nv_recv_nv_ds_sip_profile_type( xdrs, &(uptr->ds_sip_profile) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_ACTIVE_PROFILE_INDEX_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_sip_active_profile_index) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_NUM_VALID_PROFILES_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->ds_sip_num_valid_profiles) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_NAI_INFO_I:
      if ( ! xdr_nv_recv_nv_ds_sip_profile_nai_info_type( xdrs, &(uptr->ds_sip_nai_info) ) ) {
        return (FALSE);
      }

      break;
 case NV_DS_SIP_PPP_SS_INFO_I:
      if ( ! xdr_nv_recv_nv_ds_sip_ppp_ss_info_type( xdrs, &(uptr->ds_sip_ppp_ss_info) ) ) {
        return (FALSE);
      }

      break;
 case NV_DIAG_DIAGBUF_TX_SLEEP_THRESHOLD_EXT_I:
      if ( ! XDR_RECV_PACKED_UINT64( xdrs, &(uptr->diag_diagbuf_tx_sleep_threshold_ext) ) ) {
        return (FALSE);
      }

      break;
 case NV_WWAN_ACCESS_OVER_WIFI_PREFERRED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->wwan_access_over_wifi_preferred) ) ) {
        return (FALSE);
      }

      break;
 case NV_WIFI_LOCAL_BREAKOUT_ALLOWED_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->wifi_local_breakout_allowed) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_OPERATOR_ID_I:
      if ( ! xdr_nv_recv_nv_iwlan_operator_id_type( xdrs, &(uptr->iwlan_operator_id) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_REALM_I:
      if ( ! xdr_nv_recv_nv_iwlan_realm_type( xdrs, &(uptr->iwlan_realm) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_AUTH_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->iwlan_auth_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_SOFT_IKE_REKEY_TIME_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_soft_ike_rekey_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_HARD_IKE_REKEY_TIME_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_hard_ike_rekey_time) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_SOFT_LIFETIME_SEC_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_soft_lifetime_sec) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_HARD_LIFETIME_SEC_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_hard_lifetime_sec) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_SOFT_LIFETIME_BYTES_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_soft_lifetime_bytes) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_HARD_LIFETIME_BYTES_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_hard_lifetime_bytes) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_NAT_KEEPALIVE_INTERVAL_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_nat_keepalive_interval) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_RETRANSMISSION_INTERVAL_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_retransmission_interval) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_MAX_RETRANSMISSIONS_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_max_retransmissions) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKEV2_IP_CONFIG_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_ikev2_ip_config_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_DHCP_IP_CONFIG_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_dhcp_ip_config_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_ENC_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_enc_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_SA_ENC_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_sa_enc_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_SA_PRF_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_sa_prf_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_CHILD_SA_AUTH_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_child_sa_auth_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_SA_AUTH_ALGO_PROPOSAL_MASK_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_ike_sa_auth_algo_proposal_mask) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_IKE_DH_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->iwlan_ike_dh_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_PDIF_ADDRESS_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_pdif_address) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_MULTI_AUTH_SUPPORTED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->iwlan_multi_auth_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_PDIF_FQDN_I:
      if ( ! xdr_nv_recv_nv_iwlan_pdif_fqdn_type( xdrs, &(uptr->iwlan_pdif_fqdn) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_DPD_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->iwlan_dpd_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_DPD_TIME_SECS_I:
      if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->iwlan_dpd_time_secs) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_REFCLOCK_TYPE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->bt_soc_refclock_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_CLK_SHARING_TYPE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->bt_soc_clk_sharing_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_INBAND_SLEEP_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->bt_soc_inband_sleep) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_LOGGING_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->bt_soc_logging_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_SOC_PM_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->bt_soc_pm_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_MGRF_SUPPORTED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->mgrf_supported) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_USE_PMIC_OTG_COMPARATORS_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_use_pmic_otg_comparators) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_NUMBER_OF_SDCC_LUNS_I:
      if ( ! XDR_RECV_PACKED_UINT8( xdrs, &(uptr->hs_usb_number_of_sdcc_luns) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_EAP_SHARED_SECRET_I:
      if ( ! xdr_nv_recv_nv_eap_shared_secret_type( xdrs, &(uptr->ppp_eap_shared_secret) ) ) {
        return (FALSE);
      }

      break;
 case NV_PPP_VSNCP_CONFIG_DATA_I:
      if ( ! xdr_nv_recv_nv_ppp_vsncp_config_data_type( xdrs, &(uptr->ppp_vsncp_config_data) ) ) {
        return (FALSE);
      }

      break;
 case NV_EHRPD_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ehrpd_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_ATHEROS_SPECIFIC_CFG_I:
      if ( ! xdr_nv_recv_nv_wlan_atheros_specific_cfg( xdrs, &(uptr->wlan_atheros_specific_cfg) ) ) {
        return (FALSE);
      }

      break;
 case NV_GAN_MODE_PREFERENCE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->gan_mode_preference) ) ) {
        return (FALSE);
      }

      break;
 case NV_IWLAN_ENFORCE_PEER_CERT_AUTH_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->iwlan_enforce_peer_cert_auth) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_HOST_MODE_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_host_mode_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_CPU_FLOW_CONTROL_CFG_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->wlan_cpu_flow_control_cfg) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_RECONNECT_ON_RESET_DURING_SUSPEND_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_reconnect_on_reset_during_suspend) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_DISABLE_SLEEP_VOTING_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_disable_sleep_voting) ) ) {
        return (FALSE);
      }

      break;
 case NV_EHRPD_MILENAGE_OP_I:
      if ( ! xdr_nv_recv_nv_ehrpd_milenage_op_type( xdrs, &(uptr->ehrpd_milenage_op) ) ) {
        return (FALSE);
      }

      break;
 case NV_EHRPD_IMSI_I:
      if ( ! xdr_nv_recv_nv_ehrpd_imsi_type( xdrs, &(uptr->ehrpd_imsi) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_UUID_I:
      if ( ! XDR_RECV_PACKED_BYTES(xdrs, uptr->wlan_uuid, 16) ) {
        return (FALSE);
      }

      break;
 case NV_QDJ_DEQUEUE_LOG_ENABLED_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->qdj_dequeue_log_enabled) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_QSOC_NVM_MODE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->bt_qsoc_nvm_mode) ) ) {
        return (FALSE);
      }

      break;
 case NV_BT_QSOC_CLASS_TYPE_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->bt_qsoc_class_type) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_PERFORMANCE_SETTING_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->hs_usb_performance_setting) ) ) {
        return (FALSE);
      }

      break;
 case NV_WLAN_PAL_LINK_PREF_I:
      if ( ! XDR_RECV_PACKED_UINT16( xdrs, &(uptr->wlan_pal_link_pref) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_DISABLE_LPM_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->hs_usb_disable_lpm) ) ) {
        return (FALSE);
      }

      break;
 case NV_OEM_ITEM_1_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_1[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_2_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_2[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_3_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_3[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_4_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_4[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_5_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_5[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_6_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_6[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_7_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_7[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_OEM_ITEM_8_I:

      /* Calling array of XDR routines */
      for ( i = 0; i < (31); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_PACKED_UINT32( xdrs, &(uptr->oem_item_8[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case NV_EHRPD_AUTH_IN_USIM_I:
      if ( ! XDR_RECV_PACKED_BOOLEAN( xdrs, &(uptr->ehrpd_auth_in_usim) ) ) {
        return (FALSE);
      }

      break;
 case NV_HS_USB_PHY_CONFIG_I:
      if ( ! xdr_nv_recv_nv_hs_usb_phy_config_type( xdrs, &(uptr->hs_usb_phy_config) ) ) {
        return (FALSE);
      }

      break;
 case NV_DATA_CALL_OVER_EHRPD_ONLY_I:
      if ( ! xdr_nv_recv_nv_data_call_over_ehrpd_only_type( xdrs, &(uptr->data_call_over_ehrpd_only) ) ) {
        return (FALSE);
      }

      break;
    default:

      XDR_UNKNOWN_DISCRIMINATOR_MSG(obj_disc);

  }

  return (TRUE);
} /* xdr_nv_recv_nv_item_type */



