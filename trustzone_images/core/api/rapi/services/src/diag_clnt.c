/* DIAG TOOL VERSION: 4.35 */
/*=============================================================================
                             D I A G _ C L N T . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that provides RPC client functionality for the
  diag API.

  ---------------------------------------------------------------------------
  Copyright (c) 2009 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */
/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/htorpc.pl#7 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Start.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Htoxdr.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/XDR.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Output.pm#6 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Parser.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Metacomments.pm#4 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/SymbolTable.pm#2   

diag Definition File(s):
Id: //source/qcom/qct/core/services/diag/api/inc/main/latest/diag.h#10
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/services/src/diag_clnt.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "diag.h"
#include "diag_rpc.h"

/* Only one copy needed per API */
static opaque_auth DiagCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth DiagVerf = { ONCRPC_AUTH_NONE, 0, 0 };

//static opaque_auth DiagcbCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth DiagcbVerf = { ONCRPC_AUTH_NONE, 0, 0 };


/*=======================================================================
             Prototypes for the API Callbacks RPC Server Functions
=======================================================================*/
  
static void diagcbprog_0x00010001 ( struct svc_req *rqstp, xdr_s_type *srv );

static void diag_event_listener_svc_0x00010001( xdr_s_type *srv );

static void diag_log_listener_svc_0x00010001( xdr_s_type *srv );

static void diag_cmd_rsp_svc_0x00010001( xdr_s_type *srv );


/*=======================================================================
             API RPC Clients Implementation
=======================================================================*/

boolean diag_null(void)
{
  xdr_s_type       *clnt;
  rpc_reply_header  reply_header;
  boolean           xdr_status = TRUE;
  int               xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_NULL_VERS, 0);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(DIAGPROG, DIAG_NULL_VERS, 0);
    return FALSE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( ! xdr_call_msg_start( clnt,
                             DIAGPROG, DIAG_NULL_VERS,
                             ONCRPC_DIAG_NULL_PROC, &DiagCred, &DiagVerf ) ) {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return FALSE;
  }
  /* XDR OP NUMBER = 1 */

  if ( reply_header.stat != RPC_MSG_ACCEPTED )
  {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }
  else if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS )
  {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 2 */
    xdr_op_number = 2;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }
  else
  {
    return TRUE;
  }
} /* diag_null */

boolean diag_rpc_glue_code_info_remote
(
  uint32 *toolvers,
  uint32 *features,
  uint32 *proghash,
  uint32 *cbproghash
)
{
  xdr_s_type       *clnt;
  rpc_reply_header  reply_header;
  uint32            result;
  boolean           xdr_status = TRUE;
  int               xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_RPC_GLUE_CODE_INFO_REMOTE_VERS, 0);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(DIAGPROG, DIAG_RPC_GLUE_CODE_INFO_REMOTE_VERS, 0);
    return FALSE;
  }
  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( ! xdr_call_msg_start( clnt,
                             DIAGPROG, DIAG_RPC_GLUE_CODE_INFO_REMOTE_VERS,
                             ONCRPC_DIAG_RPC_GLUE_CODE_INFO_REMOTE_PROC, &DiagCred, &DiagVerf ) ) {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    // return here without calling XDR_MSG_DONE
    return FALSE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( reply_header.stat != RPC_MSG_ACCEPTED )
  {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }
  else if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS )
  {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 2 */
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }

  if ( xdr_status )
  {
    if ( toolvers != NULL )
    {
      *toolvers = result;
    }
    /* XDR OP NUMBER = 3 */
    xdr_op_number = 3;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }
  if ( xdr_status )
  {
    if ( features != NULL )
    {
      *features = result;
    }
    /* XDR OP NUMBER = 4 */
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }

  if ( xdr_status )
  {
    if ( proghash != NULL )
    {
      *proghash = result;
    }
    /* XDR OP NUMBER = 5 */
    xdr_op_number = 5;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }
  if ( xdr_status )
  {
    if ( cbproghash != NULL )
    {
      *cbproghash = result;
    }
    /* XDR OP NUMBER = 6 */
    xdr_op_number = 6;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 7 */
    xdr_op_number = 7;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }
  else
  {
    return TRUE;
  }
} /* diag_rpc_glue_code_info_remote */

boolean diag_rpc_glue_code_info_local
(
  uint32 *toolvers,
  uint32 *features,
  uint32 *proghash,
  uint32 *cbproghash
)
{
  if ( toolvers != NULL )
  {
    *toolvers = DIAG_TOOLVERS; /* 4.35 */
  }

  if ( features != NULL )
  {
    *features = DIAG_FEATURES; /* ONCRPC Server Cleanup Support */
                
  }

  if ( proghash != NULL )
  {
    *proghash = 0x00010001; /* 0x00010001 */
  }

  if ( cbproghash != NULL )
  {
    *cbproghash = 0x00010001; /* 0x00010001 */
  }

  return TRUE;
} /* diag_rpc_glue_code_info_local */

boolean diag_add_event_listener_on_modem(const unsigned int event_id,  const diag_event_listener listener,  void *param)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  boolean diag_add_event_listener_on_modem_result;

  do {
    clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_ADD_EVENT_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(DIAGPROG, DIAG_ADD_EVENT_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               DIAGPROG, DIAG_ADD_EVENT_LISTENER_VERS,
                               ONCRPC_DIAG_ADD_EVENT_LISTENER_PROC, &DiagCred, &DiagVerf );

    (void) XDR_SEND_UINT( clnt, &event_id );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) listener );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_HANDLE( clnt, &param );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_BOOLEAN( clnt, &diag_add_event_listener_on_modem_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return diag_add_event_listener_on_modem_result;
} /* diag_add_event_listener_on_modem */

boolean diag_remove_event_listener_on_modem(const unsigned int event_id,  const diag_event_listener listener,  void *param)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  boolean diag_remove_event_listener_on_modem_result;

  do {
    clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_REMOVE_EVENT_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(DIAGPROG, DIAG_REMOVE_EVENT_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               DIAGPROG, DIAG_REMOVE_EVENT_LISTENER_VERS,
                               ONCRPC_DIAG_REMOVE_EVENT_LISTENER_PROC, &DiagCred, &DiagVerf );

    (void) XDR_SEND_UINT( clnt, &event_id );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) listener );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_HANDLE( clnt, &param );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_BOOLEAN( clnt, &diag_remove_event_listener_on_modem_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return diag_remove_event_listener_on_modem_result;
} /* diag_remove_event_listener_on_modem */

boolean diag_add_log_listener_on_modem(const unsigned int log_code,  const diag_log_listener listener,  void *param)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  boolean diag_add_log_listener_on_modem_result;

  do {
    clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_ADD_LOG_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(DIAGPROG, DIAG_ADD_LOG_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               DIAGPROG, DIAG_ADD_LOG_LISTENER_VERS,
                               ONCRPC_DIAG_ADD_LOG_LISTENER_PROC, &DiagCred, &DiagVerf );

    (void) XDR_SEND_UINT( clnt, &log_code );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) listener );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_HANDLE( clnt, &param );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_BOOLEAN( clnt, &diag_add_log_listener_on_modem_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return diag_add_log_listener_on_modem_result;
} /* diag_add_log_listener_on_modem */

boolean diag_remove_log_listener_on_modem(const unsigned int log_code,  const diag_log_listener listener,  void *param)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  boolean diag_remove_log_listener_on_modem_result;

  do {
    clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_REMOVE_LOG_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(DIAGPROG, DIAG_REMOVE_LOG_LISTENER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               DIAGPROG, DIAG_REMOVE_LOG_LISTENER_VERS,
                               ONCRPC_DIAG_REMOVE_LOG_LISTENER_PROC, &DiagCred, &DiagVerf );

    (void) XDR_SEND_UINT( clnt, &log_code );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) listener );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_HANDLE( clnt, &param );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_BOOLEAN( clnt, &diag_remove_log_listener_on_modem_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return diag_remove_log_listener_on_modem_result;
} /* diag_remove_log_listener_on_modem */

boolean diag_cmd_req_on_modem(
  const byte *req,
  unsigned int length,
  const diag_cmd_rsp rsp_cb,
  void *param
)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  unsigned int length_unsigned_int;
  boolean diag_cmd_req_on_modem_result;

  do {
    clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_CMD_REQ_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(DIAGPROG, DIAG_CMD_REQ_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               DIAGPROG, DIAG_CMD_REQ_VERS,
                               ONCRPC_DIAG_CMD_REQ_PROC, &DiagCred, &DiagVerf );

    if ( req != NULL ) {
      length_unsigned_int = length;

      (void) XDR_SEND_UINT( clnt, &length_unsigned_int );

      (void) XDR_SEND_BYTES(clnt, req, length_unsigned_int);
    } else {
      length_unsigned_int = 0;

      (void) XDR_SEND_UINT( clnt, &length_unsigned_int );
    }

    (void) XDR_SEND_UINT( clnt, &length );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) rsp_cb );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_HANDLE( clnt, &param );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_BOOLEAN( clnt, &diag_cmd_req_on_modem_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return diag_cmd_req_on_modem_result;
} /* diag_cmd_req_on_modem */


/*=======================================================================
             API Standard Functions for version info
=======================================================================*/

uint32 *diag_api_versions(uint32 *len)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 i;
  boolean output_pointer_not_null;
  void *memset_temp;
  uint32 length_uint32;
  uint32 *diag_api_versions_result = NULL;

  do {
    clnt = rpc_clnt_lookup2(DIAGPROG, DIAG_API_VERSIONS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(DIAGPROG, DIAG_API_VERSIONS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               DIAGPROG, DIAG_API_VERSIONS_VERS,
                               ONCRPC_DIAG_API_VERSIONS_PROC, &DiagCred, &DiagVerf );

    /* Send TRUE if len is not NULL and FALSE otherwise so that the server
     * can correctly allocate memory for the service to use
     */
    output_pointer_not_null = (len != NULL);

    (void) XDR_SEND_UINT8( clnt, &output_pointer_not_null );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &length_uint32 );

  if ( length_uint32 > 0 )
  {
    memset_temp = oncrpcxdr_mem_alloc( clnt, length_uint32 * sizeof( *diag_api_versions_result ));
    memset(memset_temp, 0, length_uint32 * sizeof( *diag_api_versions_result ));
    diag_api_versions_result = memset_temp;


    /* Calling array of XDR routines */
    for ( i = 0; i < (length_uint32); i++ ) {
      /*lint -save -e545*/
      (void) XDR_RECV_UINT32( clnt, &(diag_api_versions_result[i]) );
      /*lint -restore */
    }
  }

  /*lint -save -e123*/
  XDR_RECV_POINTER_NO_ERRCHK( clnt, &len, XDR_RECV_UINT32 );
  /*lint -restore */


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return diag_api_versions_result;
} /* diag_api_versions */




/*=======================================================================
             API Callbacks RPC Server Implementation
=======================================================================*/

void diagcb_app_init( void )
{
  (void) svc_register_auto(DIAGCBPROG, DIAGCBVERS, diagcbprog_0x00010001);
} /* diagcb_app_init */

void diagcb_app_lock( boolean lock )
{
  svc_lock( DIAGCBPROG, DIAGCBVERS, lock );
} /* diagcb_app_enable */

static void diagcbprog_0x00010001( struct svc_req *rqstp, xdr_s_type *srv )
{
  switch( rqstp->rq_proc ) {
    case ONCRPC_DIAG_EVENT_LISTENER_PROC:
      diag_event_listener_svc_0x00010001( srv );
      break;
    case ONCRPC_DIAG_LOG_LISTENER_PROC:
      diag_log_listener_svc_0x00010001( srv );
      break;
    case ONCRPC_DIAG_CMD_RSP_PROC:
      diag_cmd_rsp_svc_0x00010001( srv );
      break;
    default:
      // invalid RPC procedure number
      (void) XDR_MSG_DONE( srv );
      svcerr_default_err( srv, rqstp, diag_api_versions  );
      break;
  }

  oncrpcxdr_mem_free( srv );
} /* diagcbprog_0x00010001 */

static void diag_event_listener_svc_0x00010001( xdr_s_type *srv )
{
  diag_event_listener cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  uint32 seq_num;
  diag_event_type *event = NULL;
  void *param;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( srv, &seq_num );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    XDR_RECV_POINTER( srv, &event, xdr_diag_recv_diag_event_type, xdr_status );
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_HANDLE( srv, &param );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (diag_event_listener ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func(seq_num, event, param);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &DiagcbVerf );

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* diag_event_listener_svc_0x00010001 */

static void diag_log_listener_svc_0x00010001( xdr_s_type *srv )
{
  diag_log_listener cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  void *memset_temp;
  unsigned int length_unsigned_int;
  uint32 seq_num;
  byte *log = NULL;
  unsigned int length;
  void *param;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( srv, &seq_num );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_RECV_UINT( srv, &length_unsigned_int );

    /* XDR OP NUMBER = 4 */
    if ( xdr_status && length_unsigned_int > 0 )
    {
      xdr_op_number  = 4;
      memset_temp = oncrpcxdr_mem_alloc( srv, length_unsigned_int * sizeof( *log ));
      memset(memset_temp, 0, length_unsigned_int * sizeof( *log ));
      log = memset_temp;

      xdr_status = XDR_RECV_BYTES(srv, log, length_unsigned_int);
    }
  }

  /* XDR OP NUMBER = 5 */
  if ( xdr_status )
  {
    xdr_op_number = 5;
    xdr_status = XDR_RECV_UINT( srv, &length );
  }

  /* XDR OP NUMBER = 6 */
  if ( xdr_status )
  {
    xdr_op_number = 6;
    xdr_status = XDR_RECV_HANDLE( srv, &param );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (diag_log_listener ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func(seq_num, log, length, param);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &DiagcbVerf );

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* diag_log_listener_svc_0x00010001 */

static void diag_cmd_rsp_svc_0x00010001( xdr_s_type *srv )
{
  diag_cmd_rsp cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  void *memset_temp;
  unsigned int length_unsigned_int;
  byte *rsp = NULL;
  unsigned int length;
  void *param;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT( srv, &length_unsigned_int );

    /* XDR OP NUMBER = 3 */
    if ( xdr_status && length_unsigned_int > 0 )
    {
      xdr_op_number  = 3;
      memset_temp = oncrpcxdr_mem_alloc( srv, length_unsigned_int * sizeof( *rsp ));
      memset(memset_temp, 0, length_unsigned_int * sizeof( *rsp ));
      rsp = memset_temp;

      xdr_status = XDR_RECV_BYTES(srv, rsp, length_unsigned_int);
    }
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT( srv, &length );
  }

  /* XDR OP NUMBER = 5 */
  if ( xdr_status )
  {
    xdr_op_number = 5;
    xdr_status = XDR_RECV_HANDLE( srv, &param );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (diag_cmd_rsp ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func(rsp, length, param);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &DiagcbVerf );

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* diag_cmd_rsp_svc_0x00010001 */

