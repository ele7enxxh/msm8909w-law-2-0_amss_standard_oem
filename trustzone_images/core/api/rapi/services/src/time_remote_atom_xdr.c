/* TIME_REMOTE_ATOM TOOL VERSION: 4.39 */
/* GENERATED: TUE AUG 17 2010 */
/*=============================================================================
                  T I M E _ R E M O T E _ A T O M _ X D R . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that provides xdr encode/decode routines
  specific to the data types used in the exported time_remote_atom API.

  ---------------------------------------------------------------------------
  Copyright (c) 2010 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */

/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/htorpc.pl#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/Start.pm#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/Htoxdr.pm#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/XDR.pm#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/Output.pm#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/Parser.pm#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/Metacomments.pm#8 
Id: //source/qcom/qct/core/pkg/2H09/halcyon_modem/main/latest/AMSS/products/7x30/tools/htorpc/lib/Htorpc/SymbolTable.pm#8   

time_remote_atom Definition File(s):
Id: //source/qcom/qct/core/api/services/time/main/latest/time_remote_atom.h#1
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/services/src/time_remote_atom_xdr.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "time_remote_atom.h"
#include "time_remote_atom_rpc.h"


/*=======================================================================
                  XDR send/recv routines for API types
=======================================================================*/


/*
 * time_julian_type XDR_TIME_REMOTE_ATOM_SEND
 */
boolean xdr_time_remote_atom_send_time_julian_type (xdr_s_type *xdrs, time_julian_type const *sptr)
{
  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->year) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->month) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->day) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->hour) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->minute) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->second) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_UINT16( xdrs, &(sptr->day_of_week) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_send_time_julian_type */

/*
 * time_julian_type XDR_TIME_REMOTE_ATOM_RECV
 */
boolean xdr_time_remote_atom_recv_time_julian_type (xdr_s_type *xdrs, time_julian_type *sptr)
{
  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->year) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->month) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->day) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->hour) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->minute) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->second) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_UINT16( xdrs, &(sptr->day_of_week) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_recv_time_julian_type */




/*
 * union time_remote_opr XDR_TIME_REMOTE_ATOM_SEND
 */
boolean xdr_time_remote_atom_send_time_remote_opr (xdr_s_type *xdrs, union time_remote_opr const *uptr, time_unit_type obj_disc)
{
  int i;
  switch ( obj_disc ){
 case TIME_STAMP:
 case TIME_MSEC:
 case TIME_SECS:
 case TIME_20MS_FRAME:

      /* Calling array of XDR routines */
      for ( i = 0; i < (2); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_SEND_UINT32( xdrs, &(uptr->timestamp[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case TIME_JULIAN:
      if ( ! xdr_time_remote_atom_send_time_julian_type( xdrs, &(uptr->julian) ) ) {
        return (FALSE);
      }

      break;
    default:

      XDR_UNKNOWN_DISCRIMINATOR_MSG(obj_disc);

  }

  return (TRUE);
} /* xdr_time_remote_atom_send_time_remote_opr */

/*
 * union time_remote_opr XDR_TIME_REMOTE_ATOM_RECV
 */
boolean xdr_time_remote_atom_recv_time_remote_opr (xdr_s_type *xdrs, union time_remote_opr *uptr, time_unit_type obj_disc)
{
  int i;
  switch ( obj_disc ){
 case TIME_STAMP:
 case TIME_MSEC:
 case TIME_SECS:
 case TIME_20MS_FRAME:

      /* Calling array of XDR routines */
      for ( i = 0; i < (2); i++ ) {
        /*lint -save -e545*/
        if ( ! XDR_RECV_UINT32( xdrs, &(uptr->timestamp[i]) ) ) {
          return (FALSE);
        }
        /*lint -restore */
      }

      break;
 case TIME_JULIAN:
      if ( ! xdr_time_remote_atom_recv_time_julian_type( xdrs, &(uptr->julian) ) ) {
        return (FALSE);
      }

      break;
    default:

      XDR_UNKNOWN_DISCRIMINATOR_MSG(obj_disc);

  }

  return (TRUE);
} /* xdr_time_remote_atom_recv_time_remote_opr */




/*
 * struct ats_tod_update_s XDR_TIME_REMOTE_ATOM_SEND
 */
boolean xdr_time_remote_atom_send_ats_tod_update_s (xdr_s_type *xdrs, struct ats_tod_update_s const *sptr)
{
  int i;
  if ( ! XDR_SEND_UINT32( xdrs, &(sptr->tick) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_SEND_UINT32( xdrs, &(sptr->stamp[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  if ( ! XDR_SEND_UINT32( xdrs, &(sptr->freq) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_send_ats_tod_update_s */

/*
 * struct ats_tod_update_s XDR_TIME_REMOTE_ATOM_RECV
 */
boolean xdr_time_remote_atom_recv_ats_tod_update_s (xdr_s_type *xdrs, struct ats_tod_update_s *sptr)
{
  int i;
  if ( ! XDR_RECV_UINT32( xdrs, &(sptr->tick) ) ) {
    return (FALSE);
  }


  /* Calling array of XDR routines */
  for ( i = 0; i < (2); i++ ) {
    /*lint -save -e545*/
    if ( ! XDR_RECV_UINT32( xdrs, &(sptr->stamp[i]) ) ) {
      return (FALSE);
    }
    /*lint -restore */
  }

  if ( ! XDR_RECV_UINT32( xdrs, &(sptr->freq) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_recv_ats_tod_update_s */




/*
 * struct ats_genoff_update_s XDR_TIME_REMOTE_ATOM_SEND
 */
boolean xdr_time_remote_atom_send_ats_genoff_update_s (xdr_s_type *xdrs, struct ats_genoff_update_s const *sptr)
{
  if ( ! XDR_SEND_ENUM( xdrs, &(sptr->base_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_SEND_INT64( xdrs, &(sptr->offset) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_send_ats_genoff_update_s */

/*
 * struct ats_genoff_update_s XDR_TIME_REMOTE_ATOM_RECV
 */
boolean xdr_time_remote_atom_recv_ats_genoff_update_s (xdr_s_type *xdrs, struct ats_genoff_update_s *sptr)
{
  if ( ! XDR_RECV_ENUM( xdrs, &(sptr->base_type) ) ) {
    return (FALSE);
  }

  if ( ! XDR_RECV_INT64( xdrs, &(sptr->offset) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_recv_ats_genoff_update_s */




/*
 * struct ats_timer_ipc_update_s XDR_TIME_REMOTE_ATOM_SEND
 */
boolean xdr_time_remote_atom_send_ats_timer_ipc_update_s (xdr_s_type *xdrs, struct ats_timer_ipc_update_s const *sptr)
{
  if ( ! XDR_SEND_UINT32( xdrs, &(sptr->expired_handle) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_send_ats_timer_ipc_update_s */

/*
 * struct ats_timer_ipc_update_s XDR_TIME_REMOTE_ATOM_RECV
 */
boolean xdr_time_remote_atom_recv_ats_timer_ipc_update_s (xdr_s_type *xdrs, struct ats_timer_ipc_update_s *sptr)
{
  if ( ! XDR_RECV_UINT32( xdrs, &(sptr->expired_handle) ) ) {
    return (FALSE);
  }

  return (TRUE);
} /* xdr_time_remote_atom_recv_ats_timer_ipc_update_s */




/*
 * union ats_cfg_event_info_u XDR_TIME_REMOTE_ATOM_SEND
 */
boolean xdr_time_remote_atom_send_ats_cfg_event_info_u (xdr_s_type *xdrs, union ats_cfg_event_info_u const *uptr, ats_cfg_event_e_type obj_disc)
{
  switch ( obj_disc ){
 case ATS_CFG_EVENT_TOD_CHANGE:
      if ( ! xdr_time_remote_atom_send_ats_tod_update_s( xdrs, &(uptr->ats_tod_update) ) ) {
        return (FALSE);
      }

      break;
 case ATS_CFG_EVENT_GENOFF_CHANGE:
      if ( ! xdr_time_remote_atom_send_ats_genoff_update_s( xdrs, &(uptr->ats_genoff_update) ) ) {
        return (FALSE);
      }

      break;
 case ATS_CFG_EVENT_TIMER_IPC_CHANGE:
      if ( ! xdr_time_remote_atom_send_ats_timer_ipc_update_s( xdrs, &(uptr->ats_timer_ipc_update) ) ) {
        return (FALSE);
      }

      break;
    default:

      XDR_UNKNOWN_DISCRIMINATOR_MSG(obj_disc);

  }

  return (TRUE);
} /* xdr_time_remote_atom_send_ats_cfg_event_info_u */

/*
 * union ats_cfg_event_info_u XDR_TIME_REMOTE_ATOM_RECV
 */
boolean xdr_time_remote_atom_recv_ats_cfg_event_info_u (xdr_s_type *xdrs, union ats_cfg_event_info_u *uptr, ats_cfg_event_e_type obj_disc)
{
  switch ( obj_disc ){
 case ATS_CFG_EVENT_TOD_CHANGE:
      if ( ! xdr_time_remote_atom_recv_ats_tod_update_s( xdrs, &(uptr->ats_tod_update) ) ) {
        return (FALSE);
      }

      break;
 case ATS_CFG_EVENT_GENOFF_CHANGE:
      if ( ! xdr_time_remote_atom_recv_ats_genoff_update_s( xdrs, &(uptr->ats_genoff_update) ) ) {
        return (FALSE);
      }

      break;
 case ATS_CFG_EVENT_TIMER_IPC_CHANGE:
      if ( ! xdr_time_remote_atom_recv_ats_timer_ipc_update_s( xdrs, &(uptr->ats_timer_ipc_update) ) ) {
        return (FALSE);
      }

      break;
    default:

      XDR_UNKNOWN_DISCRIMINATOR_MSG(obj_disc);

  }

  return (TRUE);
} /* xdr_time_remote_atom_recv_ats_cfg_event_info_u */



