/* REMOTE_STORAGE TOOL VERSION: 4.34 */
/*=============================================================================
                   R E M O T E _ S T O R A G E _ C L N T . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that provides RPC client functionality for the
  remote_storage API.

  ---------------------------------------------------------------------------
  Copyright (c) 2010 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */
/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/htorpc.pl#18 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Start.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Htoxdr.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/XDR.pm#6 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Output.pm#26 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Parser.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Metacomments.pm#4 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/SymbolTable.pm#2   

remote_storage Definition File(s):
Id: //source/qcom/qct/core/storage/remotefs/01/amss/remote_storage.h#2
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/storage/src/remote_storage_clnt.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "remote_storage.h"
#include "remote_storage_rpc.h"

/* Only one copy needed per API */
static opaque_auth Remote_storageCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth Remote_storageVerf = { ONCRPC_AUTH_NONE, 0, 0 };

//static opaque_auth Remote_storagecbCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth Remote_storagecbVerf = { ONCRPC_AUTH_NONE, 0, 0 };


/*=======================================================================
             Prototypes for the API Callbacks RPC Server Functions
=======================================================================*/
  
static void remote_storagecbprog_0x00010001 ( struct svc_req *rqstp, xdr_s_type *srv );

static void remotefs_srv_open_cb_type_svc_0x00010001( xdr_s_type *srv );

static void remotefs_srv_write_iovec_cb_type_svc_0x00010001( xdr_s_type *srv );

static void remotefs_srv_event_cb_type_svc_0x00010001( xdr_s_type *srv );

static void remotefs_srv_read_iovec_cb_type_svc_0x00010001( xdr_s_type *srv );


/*=======================================================================
             API RPC Clients Implementation
=======================================================================*/

boolean remote_storage_null(void)
{
  xdr_s_type       *clnt;
  rpc_reply_header  reply_header;
  boolean           xdr_status = TRUE;
  int               xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTE_STORAGE_NULL_VERS, 0);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTE_STORAGE_NULL_VERS, 0);
    return FALSE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( ! xdr_call_msg_start( clnt,
                             REMOTE_STORAGEPROG, REMOTE_STORAGE_NULL_VERS,
                             ONCRPC_REMOTE_STORAGE_NULL_PROC, &Remote_storageCred, &Remote_storageVerf ) ) {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return FALSE;
  }
  /* XDR OP NUMBER = 1 */

  if ( reply_header.stat != RPC_MSG_ACCEPTED )
  {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }
  else if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS )
  {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 2 */
    xdr_op_number = 2;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }
  else
  {
    return TRUE;
  }
} /* remote_storage_null */

boolean remote_storage_rpc_glue_code_info_remote
(
  uint32 *toolvers,
  uint32 *features,
  uint32 *proghash,
  uint32 *cbproghash
)
{
  xdr_s_type       *clnt;
  rpc_reply_header  reply_header;
  uint32            result;
  boolean           xdr_status = TRUE;
  int               xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTE_STORAGE_RPC_GLUE_CODE_INFO_REMOTE_VERS, 0);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTE_STORAGE_RPC_GLUE_CODE_INFO_REMOTE_VERS, 0);
    return FALSE;
  }
  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( ! xdr_call_msg_start( clnt,
                             REMOTE_STORAGEPROG, REMOTE_STORAGE_RPC_GLUE_CODE_INFO_REMOTE_VERS,
                             ONCRPC_REMOTE_STORAGE_RPC_GLUE_CODE_INFO_REMOTE_PROC, &Remote_storageCred, &Remote_storageVerf ) ) {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    // return here without calling XDR_MSG_DONE
    return FALSE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( reply_header.stat != RPC_MSG_ACCEPTED )
  {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }
  else if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS )
  {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 2 */
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }

  if ( xdr_status )
  {
    if ( toolvers != NULL )
    {
      *toolvers = result;
    }
    /* XDR OP NUMBER = 3 */
    xdr_op_number = 3;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }
  if ( xdr_status )
  {
    if ( features != NULL )
    {
      *features = result;
    }
    /* XDR OP NUMBER = 4 */
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }

  if ( xdr_status )
  {
    if ( proghash != NULL )
    {
      *proghash = result;
    }
    /* XDR OP NUMBER = 5 */
    xdr_op_number = 5;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }
  if ( xdr_status )
  {
    if ( cbproghash != NULL )
    {
      *cbproghash = result;
    }
    /* XDR OP NUMBER = 6 */
    xdr_op_number = 6;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 7 */
    xdr_op_number = 7;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }
  else
  {
    return TRUE;
  }
} /* remote_storage_rpc_glue_code_info_remote */

boolean remote_storage_rpc_glue_code_info_local
(
  uint32 *toolvers,
  uint32 *features,
  uint32 *proghash,
  uint32 *cbproghash
)
{
  if ( toolvers != NULL )
  {
    *toolvers = REMOTE_STORAGE_TOOLVERS; /* 4.34 */
  }

  if ( features != NULL )
  {
    *features = REMOTE_STORAGE_FEATURES; /* ONCRPC Server Cleanup Support */
                
  }

  if ( proghash != NULL )
  {
    *proghash = 0x00010001; /* 0x00010001 */
  }

  if ( cbproghash != NULL )
  {
    *cbproghash = 0x00010001; /* 0x00010001 */
  }

  return TRUE;
} /* remote_storage_rpc_glue_code_info_local */

remotefs_status_type remotefs_srv_write_finish(void *handle,  remotefs_status_type last_op_status,  uint32 user_data)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  remotefs_status_type remotefs_srv_write_finish_result;

  do {
    clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_WRITE_FINISH_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(REMOTE_STORAGEPROG, REMOTEFS_SRV_WRITE_FINISH_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               REMOTE_STORAGEPROG, REMOTEFS_SRV_WRITE_FINISH_VERS,
                               ONCRPC_REMOTEFS_SRV_WRITE_FINISH_PROC, &Remote_storageCred, &Remote_storageVerf );

    (void) XDR_SEND_HANDLE( clnt, &handle );

    (void) XDR_SEND_ENUM( clnt, &last_op_status );

    (void) XDR_SEND_UINT32( clnt, &user_data );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_ENUM( clnt, &remotefs_srv_write_finish_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return remotefs_srv_write_finish_result;
} /* remotefs_srv_write_finish */

remotefs_status_type remotefs_srv_register_open(remotefs_srv_event_e_type open_event,  remotefs_srv_open_cb_type remotefs_srv_open_cb)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  remotefs_status_type remotefs_srv_register_open_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_OPEN_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_OPEN_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return RMFS_ERROR_PIPE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_OPEN_VERS,
                                   ONCRPC_REMOTEFS_SRV_REGISTER_OPEN_PROC, &Remote_storageCred, &Remote_storageVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( clnt, &open_event );
  }

  /*lint -save -e611*/
  cb_id = rpc_clnt_callback_register( (void *) remotefs_srv_open_cb );
  /*lint -restore */

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_SEND_UINT32( clnt, &cb_id );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return RMFS_ERROR_PIPE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return RMFS_ERROR_PIPE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( clnt, &remotefs_srv_register_open_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return RMFS_ERROR_PIPE;
  }

  return remotefs_srv_register_open_result;
} /* remotefs_srv_register_open */

remotefs_status_type remotefs_srv_register_write_iovec(remotefs_srv_event_e_type write_iovec_event,  remotefs_srv_write_iovec_cb_type remotefs_srv_write_iovec_cb)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  remotefs_status_type remotefs_srv_register_write_iovec_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_WRITE_IOVEC_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_WRITE_IOVEC_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return RMFS_ERROR_PIPE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_WRITE_IOVEC_VERS,
                                   ONCRPC_REMOTEFS_SRV_REGISTER_WRITE_IOVEC_PROC, &Remote_storageCred, &Remote_storageVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( clnt, &write_iovec_event );
  }

  /*lint -save -e611*/
  cb_id = rpc_clnt_callback_register( (void *) remotefs_srv_write_iovec_cb );
  /*lint -restore */

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_SEND_UINT32( clnt, &cb_id );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return RMFS_ERROR_PIPE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return RMFS_ERROR_PIPE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( clnt, &remotefs_srv_register_write_iovec_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return RMFS_ERROR_PIPE;
  }

  return remotefs_srv_register_write_iovec_result;
} /* remotefs_srv_register_write_iovec */

remotefs_status_type remotefs_srv_register_cb(remotefs_srv_event_e_type remotefs_srv_event_e,  remotefs_srv_event_cb_type remotefs_srv_event_cb)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  remotefs_status_type remotefs_srv_register_cb_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return RMFS_ERROR_PIPE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_CB_VERS,
                                   ONCRPC_REMOTEFS_SRV_REGISTER_CB_PROC, &Remote_storageCred, &Remote_storageVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( clnt, &remotefs_srv_event_e );
  }

  /*lint -save -e611*/
  cb_id = rpc_clnt_callback_register( (void *) remotefs_srv_event_cb );
  /*lint -restore */

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_SEND_UINT32( clnt, &cb_id );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return RMFS_ERROR_PIPE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return RMFS_ERROR_PIPE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( clnt, &remotefs_srv_register_cb_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return RMFS_ERROR_PIPE;
  }

  return remotefs_srv_register_cb_result;
} /* remotefs_srv_register_cb */

remotefs_status_type remotefs_srv_un_register_cb(remotefs_srv_event_e_type remotefs_srv_event_e)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  remotefs_status_type remotefs_srv_un_register_cb_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_UN_REGISTER_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_UN_REGISTER_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return RMFS_ERROR_PIPE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_UN_REGISTER_CB_VERS,
                                   ONCRPC_REMOTEFS_SRV_UN_REGISTER_CB_PROC, &Remote_storageCred, &Remote_storageVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( clnt, &remotefs_srv_event_e );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return RMFS_ERROR_PIPE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return RMFS_ERROR_PIPE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( clnt, &remotefs_srv_un_register_cb_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return RMFS_ERROR_PIPE;
  }

  return remotefs_srv_un_register_cb_result;
} /* remotefs_srv_un_register_cb */

int remotefs_srv_force_sync(void)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  int remotefs_srv_force_sync_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_FORCE_SYNC_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_FORCE_SYNC_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return REMOTEFS_FAILURE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_FORCE_SYNC_VERS,
                                   ONCRPC_REMOTEFS_SRV_FORCE_SYNC_PROC, &Remote_storageCred, &Remote_storageVerf );

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return REMOTEFS_FAILURE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return REMOTEFS_FAILURE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_INT( clnt, &remotefs_srv_force_sync_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return REMOTEFS_FAILURE;
  }

  return remotefs_srv_force_sync_result;
} /* remotefs_srv_force_sync */

int remotefs_srv_get_sync_status(int sync_token)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  int remotefs_srv_get_sync_status_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_GET_SYNC_STATUS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_GET_SYNC_STATUS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return REMOTEFS_FAILURE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_GET_SYNC_STATUS_VERS,
                                   ONCRPC_REMOTEFS_SRV_GET_SYNC_STATUS_PROC, &Remote_storageCred, &Remote_storageVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_INT( clnt, &sync_token );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return REMOTEFS_FAILURE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return REMOTEFS_FAILURE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_INT( clnt, &remotefs_srv_get_sync_status_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return REMOTEFS_FAILURE;
  }

  return remotefs_srv_get_sync_status_result;
} /* remotefs_srv_get_sync_status */

remotefs_status_type remotefs_srv_register_read_iovec(remotefs_srv_event_e_type read_iovec_event,  remotefs_srv_read_iovec_cb_type remotefs_srv_read_iovec_cb)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  remotefs_status_type remotefs_srv_register_read_iovec_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_READ_IOVEC_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_READ_IOVEC_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return RMFS_ERROR_PIPE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   REMOTE_STORAGEPROG, REMOTEFS_SRV_REGISTER_READ_IOVEC_VERS,
                                   ONCRPC_REMOTEFS_SRV_REGISTER_READ_IOVEC_PROC, &Remote_storageCred, &Remote_storageVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( clnt, &read_iovec_event );
  }

  /*lint -save -e611*/
  cb_id = rpc_clnt_callback_register( (void *) remotefs_srv_read_iovec_cb );
  /*lint -restore */

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_SEND_UINT32( clnt, &cb_id );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return RMFS_ERROR_PIPE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return RMFS_ERROR_PIPE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( clnt, &remotefs_srv_register_read_iovec_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return RMFS_ERROR_PIPE;
  }

  return remotefs_srv_register_read_iovec_result;
} /* remotefs_srv_register_read_iovec */


/*=======================================================================
             API Standard Functions for version info
=======================================================================*/

uint32 *remote_storage_api_versions(uint32 *len)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 i;
  boolean output_pointer_not_null;
  void *memset_temp;
  uint32 length_uint32;
  uint32 *remote_storage_api_versions_result = NULL;

  do {
    clnt = rpc_clnt_lookup2(REMOTE_STORAGEPROG, REMOTE_STORAGE_API_VERSIONS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(REMOTE_STORAGEPROG, REMOTE_STORAGE_API_VERSIONS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               REMOTE_STORAGEPROG, REMOTE_STORAGE_API_VERSIONS_VERS,
                               ONCRPC_REMOTE_STORAGE_API_VERSIONS_PROC, &Remote_storageCred, &Remote_storageVerf );

    /* Send TRUE if len is not NULL and FALSE otherwise so that the server
     * can correctly allocate memory for the service to use
     */
    output_pointer_not_null = (len != NULL);

    (void) XDR_SEND_UINT8( clnt, &output_pointer_not_null );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &length_uint32 );

  if ( length_uint32 > 0 )
  {
    memset_temp = oncrpcxdr_mem_alloc( clnt, length_uint32 * sizeof( *remote_storage_api_versions_result ));
    memset(memset_temp, 0, length_uint32 * sizeof( *remote_storage_api_versions_result ));
    remote_storage_api_versions_result = memset_temp;


    /* Calling array of XDR routines */
    for ( i = 0; i < (length_uint32); i++ ) {
      /*lint -save -e545*/
      (void) XDR_RECV_UINT32( clnt, &(remote_storage_api_versions_result[i]) );
      /*lint -restore */
    }
  }

  /*lint -save -e123*/
  XDR_RECV_POINTER_NO_ERRCHK( clnt, &len, XDR_RECV_UINT32 );
  /*lint -restore */


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return remote_storage_api_versions_result;
} /* remote_storage_api_versions */




/*=======================================================================
             API Callbacks RPC Server Implementation
=======================================================================*/

void remote_storagecb_app_init( void )
{
  (void) svc_register_auto(REMOTE_STORAGECBPROG, REMOTE_STORAGECBVERS, remote_storagecbprog_0x00010001);
} /* remote_storagecb_app_init */

void remote_storagecb_app_lock( boolean lock )
{
  svc_lock( REMOTE_STORAGECBPROG, REMOTE_STORAGECBVERS, lock );
} /* remote_storagecb_app_enable */

static void remote_storagecbprog_0x00010001( struct svc_req *rqstp, xdr_s_type *srv )
{
  switch( rqstp->rq_proc ) {
    case ONCRPC_REMOTEFS_SRV_OPEN_CB_TYPE_PROC:
      remotefs_srv_open_cb_type_svc_0x00010001( srv );
      break;
    case ONCRPC_REMOTEFS_SRV_WRITE_IOVEC_CB_TYPE_PROC:
      remotefs_srv_write_iovec_cb_type_svc_0x00010001( srv );
      break;
    case ONCRPC_REMOTEFS_SRV_EVENT_CB_TYPE_PROC:
      remotefs_srv_event_cb_type_svc_0x00010001( srv );
      break;
    case ONCRPC_REMOTEFS_SRV_READ_IOVEC_CB_TYPE_PROC:
      remotefs_srv_read_iovec_cb_type_svc_0x00010001( srv );
      break;
    default:
      // invalid RPC procedure number
      (void) XDR_MSG_DONE( srv );
      svcerr_default_err( srv, rqstp, remote_storage_api_versions  );
      break;
  }

  oncrpcxdr_mem_free( srv );
} /* remote_storagecbprog_0x00010001 */

static void remotefs_srv_open_cb_type_svc_0x00010001( xdr_s_type *srv )
{
  remotefs_srv_open_cb_type cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  void *memset_temp;
  uint32 length_uint32;
  remotefs_srv_event_e_type open_event = REMOTEFS_SRV_EVNT_OPEN;
  char *path = NULL;

  void *cmd_cb_func_result = NULL;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( srv, &open_event );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_RECV_UINT32( srv, &length_uint32 );

    /* XDR OP NUMBER = 4 */
    if ( xdr_status && length_uint32 > 0 )
    {
      xdr_op_number  = 4;
      if ( path == NULL ) {
        memset_temp = oncrpcxdr_mem_alloc( srv, length_uint32);
        memset(memset_temp, 0, length_uint32);
        path = memset_temp;
      }

        xdr_status = XDR_RECV_STRING(srv, path, length_uint32);
    }
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (remotefs_srv_open_cb_type ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func_result = cmd_cb_func(open_event, path);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Remote_storagecbVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_HANDLE( srv, &cmd_cb_func_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* remotefs_srv_open_cb_type_svc_0x00010001 */

static void remotefs_srv_write_iovec_cb_type_svc_0x00010001( xdr_s_type *srv )
{
  remotefs_srv_write_iovec_cb_type cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  unsigned long int i;
  void *memset_temp;
  unsigned long int length_unsigned_long_int;
  remotefs_srv_event_e_type write_iovec_event = REMOTEFS_SRV_EVNT_OPEN;
  void *handle;
  remotefs_iovec_desc_type *iovec_struct = NULL;
  uint32 ent_cnt;

  remotefs_status_type cmd_cb_func_result;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( srv, &write_iovec_event );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_RECV_HANDLE( srv, &handle );
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( srv, &length_unsigned_long_int );

    /* XDR OP NUMBER = 5 */
    if ( xdr_status && length_unsigned_long_int > 0 )
    {
      xdr_op_number  = 5;
      memset_temp = oncrpcxdr_mem_alloc( srv, length_unsigned_long_int * sizeof( *iovec_struct ));
      memset(memset_temp, 0, length_unsigned_long_int * sizeof( *iovec_struct ));
      iovec_struct = memset_temp;


      /* Calling array of XDR routines */
      for ( i = 0; xdr_status && i < (length_unsigned_long_int); i++ ) {
        /*lint -save -e545*/
        xdr_status = xdr_remote_storage_recv_remotefs_iovec_desc( srv, &(iovec_struct[i]) );
        /*lint -restore */
      }
    }
  }

  /* XDR OP NUMBER = 6 */
  if ( xdr_status )
  {
    xdr_op_number = 6;
    xdr_status = XDR_RECV_UINT32( srv, &ent_cnt );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (remotefs_srv_write_iovec_cb_type ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func_result = cmd_cb_func(write_iovec_event, handle, iovec_struct, ent_cnt);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Remote_storagecbVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &cmd_cb_func_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* remotefs_srv_write_iovec_cb_type_svc_0x00010001 */

static void remotefs_srv_event_cb_type_svc_0x00010001( xdr_s_type *srv )
{
  remotefs_srv_event_cb_type cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  remotefs_srv_event_e_type event = REMOTEFS_SRV_EVNT_OPEN;
  remotefs_srv_cfg_event_info_u_type *info_ptr = NULL;

  remotefs_status_type cmd_cb_func_result;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( srv, &event );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_RECV_U_POINTER2( srv, &info_ptr, &(event), xdr_remote_storage_recv_remotefs_srv_evnt_info_u, XDR_RECV_ENUM, xdr_status );
    /*lint -restore */
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (remotefs_srv_event_cb_type ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func_result = cmd_cb_func(event, info_ptr);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Remote_storagecbVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &cmd_cb_func_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* remotefs_srv_event_cb_type_svc_0x00010001 */

static void remotefs_srv_read_iovec_cb_type_svc_0x00010001( xdr_s_type *srv )
{
  remotefs_srv_read_iovec_cb_type cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  unsigned long int i;
  void *memset_temp;
  unsigned long int length_unsigned_long_int;
  remotefs_srv_event_e_type read_iovec_event = REMOTEFS_SRV_EVNT_OPEN;
  void *handle;
  remotefs_iovec_desc_type *iovec_struct = NULL;
  uint32 ent_cnt;

  remotefs_status_type cmd_cb_func_result;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_ENUM( srv, &read_iovec_event );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_RECV_HANDLE( srv, &handle );
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( srv, &length_unsigned_long_int );

    /* XDR OP NUMBER = 5 */
    if ( xdr_status && length_unsigned_long_int > 0 )
    {
      xdr_op_number  = 5;
      memset_temp = oncrpcxdr_mem_alloc( srv, length_unsigned_long_int * sizeof( *iovec_struct ));
      memset(memset_temp, 0, length_unsigned_long_int * sizeof( *iovec_struct ));
      iovec_struct = memset_temp;


      /* Calling array of XDR routines */
      for ( i = 0; xdr_status && i < (length_unsigned_long_int); i++ ) {
        /*lint -save -e545*/
        xdr_status = xdr_remote_storage_recv_remotefs_iovec_desc( srv, &(iovec_struct[i]) );
        /*lint -restore */
      }
    }
  }

  /* XDR OP NUMBER = 6 */
  if ( xdr_status )
  {
    xdr_op_number = 6;
    xdr_status = XDR_RECV_UINT32( srv, &ent_cnt );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (remotefs_srv_read_iovec_cb_type ) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func_result = cmd_cb_func(read_iovec_event, handle, iovec_struct, ent_cnt);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Remote_storagecbVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_ENUM( srv, &cmd_cb_func_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* remotefs_srv_read_iovec_cb_type_svc_0x00010001 */

