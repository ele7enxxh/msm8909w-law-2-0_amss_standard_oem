/* OEM_RAPI TOOL VERSION: 4.30 */
/*=============================================================================
                          O E M _ R A P I _ S V C . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that dispatches RPC requests targetting the
  oem_rapi api.

  ---------------------------------------------------------------------------
  Copyright (c) 2009 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */
/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/htorpc.pl#11 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Start.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Htoxdr.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/XDR.pm#6 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Output.pm#20 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Parser.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/Metacomments.pm#3 
Id: //source/qcom/qct/core/mproc/tools/rel/04.01/htorpc/lib/Htorpc/SymbolTable.pm#2  
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/mproc/src/oem_rapi_svc.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "oem_rapi.h"
#include "oem_rapi_rpc.h"

/* Only one copy needed per API */
//static opaque_auth Oem_rapiCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth Oem_rapiVerf = { ONCRPC_AUTH_NONE, 0, 0 };

static opaque_auth Oem_rapicbCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth Oem_rapicbVerf = { ONCRPC_AUTH_NONE, 0, 0 };

/*=======================================================================
             Prototypes for the API's RPC Server Functions
=======================================================================*/

static void oem_rapiprog_0x00010001 ( struct svc_req *rqstp, xdr_s_type *srv );
static void oem_rapi_null_0x00010001( xdr_s_type *svc );
static void oem_rapi_rpc_glue_code_info_remote_0x00010001( xdr_s_type *svc );
static void oem_rapi_api_versions_0x00010001( xdr_s_type *srv );

static void oem_rapi_streaming_function_0x00010001( xdr_s_type *srv );


/*=======================================================================
             Prototypes for the API's Callback RPC clients
=======================================================================*/

static void oem_rapi_streaming_cb_type_clnt_0x00010001(
  oem_rapi_server_event_e_type event,
  void *handle,
  uint32 in_len,
  byte *input,
  uint32 *out_len,
  byte *output
);

/******************************************************************************/
/*=======================================================================
             API RPC Server Implementation
=======================================================================*/
static uint32 oem_rapi_api_versions_table[] = {  0x00010001  };

static uint32 * oem_rapi_api_versions(uint32 *size_entries)
{
  *size_entries = sizeof( oem_rapi_api_versions_table) / sizeof(uint32);
  return oem_rapi_api_versions_table;
}

void oem_rapi_app_init( void )
{
  (void) svc_register_auto_apiversions(OEM_RAPIPROG, OEM_RAPIVERS, oem_rapiprog_0x00010001, 
                                       oem_rapi_api_versions);
} /* oem_rapi_app_init */

void oem_rapi_app_lock( boolean lock )
{
  svc_lock( OEM_RAPIPROG, OEM_RAPIVERS, lock );
} /* oem_rapi_app_lock */

static void oem_rapiprog_0x00010001 ( struct svc_req *rqstp, xdr_s_type *srv )
{
  switch ( rqstp->rq_proc ) {
    case ONCRPC_OEM_RAPI_NULL_PROC:
      oem_rapi_null_0x00010001( srv );
      break;
    case ONCRPC_OEM_RAPI_RPC_GLUE_CODE_INFO_REMOTE_PROC:
      oem_rapi_rpc_glue_code_info_remote_0x00010001( srv );
      break;
    case ONCRPC_OEM_RAPI_STREAMING_FUNCTION_PROC:
      oem_rapi_streaming_function_0x00010001( srv );
      break;
    case ONCRPC_OEM_RAPI_API_VERSIONS_PROC:
      oem_rapi_api_versions_0x00010001( srv );
      break;

    default:
      // invalid RPC procedure number
      (void) XDR_MSG_DONE( srv );
      svcerr_default_err( srv, rqstp, oem_rapi_api_versions  );
      break;
  }

  oncrpcxdr_mem_free( srv );
} /* oem_rapiprog_0x00010001 */

/******************************************************************************/

static void oem_rapi_null_0x00010001( xdr_s_type *srv )
{
  if ( ! XDR_MSG_DONE( srv ) ) {
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  if ( ! xdr_reply_msg_start( srv, &Oem_rapiVerf ) ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* oem_rapi_null_0x00010001 */

static void oem_rapi_rpc_glue_code_info_remote_0x00010001( xdr_s_type *srv )
{
  uint32 toolvers   = OEM_RAPI_TOOLVERS; /* 4.30 */
  uint32 proghash   = 0x00010001; /* 0x00010001 */
  uint32 cbproghash = 0x00010001; /* 0x00010001 */
  uint32 features   = OEM_RAPI_FEATURES; /* ONCRPC Server Cleanup Support */
                      

  if ( ! XDR_MSG_DONE( srv ) ) {
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  if ( ! xdr_reply_msg_start( srv, &Oem_rapiVerf ) ||
       ! XDR_SEND_UINT32( srv, &toolvers ) ||
       ! XDR_SEND_UINT32( srv, &features ) ||
       ! XDR_SEND_UINT32( srv, &proghash ) ||
       ! XDR_SEND_UINT32( srv, &cbproghash ) )
  {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* oem_rapi_rpc_glue_code_info_remote_0x00010001 */

static void oem_rapi_streaming_function_0x00010001( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  uint32 cb_id1;
  void *memset_temp;
  uint32 length_uint32;
  boolean output_pointer_not_null;
  oem_rapi_client_event_e_type event = OEM_RAPI_CLIENT_EVENT_NONE;
  oem_rapi_streaming_cb_type *cb1;
  void *handle;
  uint32 in_len;
  byte *input = NULL;
  uint32 *out_len = NULL;
  byte *output = NULL;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_ENUM( srv, &event );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( srv, &cb_id1 );
  }

  /*lint -save -e611*/
  cb1 = (oem_rapi_streaming_cb_type *) rpc_svc_callback_register( (void *) oem_rapi_streaming_cb_type_clnt_0x00010001, srv, cb_id1 ); 
  /*lint -restore */

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_RECV_HANDLE( srv, &handle );
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( srv, &in_len );
  }

  /* XDR OP NUMBER = 5 */
  if ( xdr_status )
  {
    xdr_op_number = 5;
    xdr_status = XDR_RECV_UINT32( srv, &length_uint32 );

    /* XDR OP NUMBER = 6 */
    if ( xdr_status && length_uint32 > 0 )
    {
      xdr_op_number  = 6;
      memset_temp = oncrpcxdr_mem_alloc( srv, length_uint32 * sizeof( *input ));
      memset(memset_temp, 0, length_uint32 * sizeof( *input ));
      input = memset_temp;

      xdr_status = XDR_RECV_BYTES(srv, input, length_uint32);
    }
  }

  /* XDR OP NUMBER = 7 */
  if ( xdr_status )
  {
    xdr_op_number = 7;
    /* The server must know whether to allocate memory for the output parameter
     * out_len or not. A boolean is received to indicate that.
     */
    xdr_status = XDR_RECV_UINT8( srv, &output_pointer_not_null );

    if ( xdr_status && output_pointer_not_null ) {
      out_len = oncrpcxdr_mem_alloc( srv, sizeof(*out_len) );
    }
  }

  /* XDR OP NUMBER = 8 */
  if ( xdr_status )
  {
    xdr_op_number = 8;
    /* The server must know whether to allocate memory for the output parameter
     * output or not. A boolean is received to indicate that. The maximum number
     * of objects that could be pointed to by this pointer is also received.
     */
    xdr_status = XDR_RECV_UINT8( srv, &output_pointer_not_null );

    if ( xdr_status && output_pointer_not_null ) {

      xdr_status = XDR_RECV_UINT32( srv, &length_uint32 );

      output = oncrpcxdr_mem_alloc( srv, length_uint32 * sizeof(*output) );
    }
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  oem_rapi_streaming_function(event, cb1, handle, in_len, input, out_len, output);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Oem_rapiVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    /*lint -save -e123*/
    XDR_SEND_POINTER( srv, &out_len, XDR_SEND_UINT32, xdr_status );
    /*lint -restore */
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    if ( output != NULL ) {
      length_uint32 = *out_len;
      length_uint32 = ( length_uint32 > OEM_RAPI_MAX_CLIENT_OUTPUT_BUFFER_SIZE ? OEM_RAPI_MAX_CLIENT_OUTPUT_BUFFER_SIZE : length_uint32 );

      xdr_status = XDR_SEND_UINT32( srv, &length_uint32 );

      /* XDR OP NUMBER = 4 */
      if ( xdr_status )
      {
        xdr_op_number  = 4;
        xdr_status = XDR_SEND_BYTES(srv, output, length_uint32);
      }
    } else {
      length_uint32 = 0;

      xdr_status = XDR_SEND_UINT32( srv, &length_uint32 );
    }
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* oem_rapi_streaming_function_0x00010001 */

/*=========================================================================== 
              API Standard function for api versioning
===========================================================================*/

static void oem_rapi_api_versions_0x00010001( xdr_s_type *srv )
{
  boolean xdr_status = TRUE;
  int xdr_op_number = 0;
  uint32 i;
  boolean output_pointer_not_null;
  uint32 length_uint32;
  uint32 *len = NULL;

  uint32 *oem_rapi_api_versions_result = NULL;

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  /* The server must know whether to allocate memory for the output parameter
   * len or not. A boolean is received to indicate that.
   */
  xdr_status = XDR_RECV_UINT8( srv, &output_pointer_not_null );

  if ( xdr_status && output_pointer_not_null ) {
    len = oncrpcxdr_mem_alloc( srv, sizeof(*len) );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  oem_rapi_api_versions_result = oem_rapi_api_versions(len);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Oem_rapiVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    if ( oem_rapi_api_versions_result != NULL ) {
      length_uint32 = *len;

      xdr_status = XDR_SEND_UINT32( srv, &length_uint32 );

      /* XDR OP NUMBER = 3 */
      if ( xdr_status )
      {
        xdr_op_number  = 3;

        /* Calling array of XDR routines */
        for ( i = 0; xdr_status && i < (length_uint32); i++ ) {
          /*lint -save -e545*/
          xdr_status = XDR_SEND_UINT32( srv, &(oem_rapi_api_versions_result[i]) );
          /*lint -restore */
        }
      }
    } else {
      length_uint32 = 0;

      xdr_status = XDR_SEND_UINT32( srv, &length_uint32 );
    }
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    /*lint -save -e123*/
    XDR_SEND_POINTER( srv, &len, XDR_SEND_UINT32, xdr_status );
    /*lint -restore */
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* oem_rapi_api_versions_0x00010001 */

/*=========================================================================== 
              API Callback Clients
===========================================================================*/

static void oem_rapi_streaming_cb_type_clnt_0x00010001(
  oem_rapi_server_event_e_type event,
  void *handle,
  uint32 in_len,
  byte *input,
  uint32 *out_len,
  byte *output
)
{
  xdr_s_type       *clnt          = NULL;
  rpc_reply_header  reply_header;
  rpc_cb_data_type *rpc_cb_data   = NULL;
  boolean           xdr_status    = TRUE;
  int               xdr_op_number = 0;

  unsigned long int length_unsigned_long_int;
  boolean output_pointer_not_null;
  uint32 length_uint32;
  rpc_cb_data = rpc_svc_cb_data_lookup();

  if ( rpc_cb_data == NULL )
  {
    RPC_SVC_CB_DATA_LOOKUP_ERR();
    return;
  }

  clnt = rpc_clnt_for_callback( rpc_cb_data );

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   OEM_RAPICBPROG, OEM_RAPI_STREAMING_CB_TYPE_VERS,
                                   ONCRPC_OEM_RAPI_STREAMING_CB_TYPE_PROC, &Oem_rapicbCred, &Oem_rapicbVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_UINT32( clnt, &rpc_cb_data->cb_id );
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_SEND_ENUM( clnt, &event );
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_SEND_HANDLE( clnt, &handle );
  }

  /* XDR OP NUMBER = 5 */
  if ( xdr_status )
  {
    xdr_op_number = 5;
    xdr_status = XDR_SEND_UINT32( clnt, &in_len );
  }

  /* XDR OP NUMBER = 6 */
  if ( xdr_status )
  {
    xdr_op_number = 6;
    if ( input != NULL ) {
      length_unsigned_long_int = in_len;
      length_unsigned_long_int = ( length_unsigned_long_int > OEM_RAPI_MAX_SERVER_INPUT_BUFFER_SIZE ? OEM_RAPI_MAX_SERVER_INPUT_BUFFER_SIZE : length_unsigned_long_int );

      xdr_status = XDR_SEND_UINT32( clnt, &length_unsigned_long_int );

      /* XDR OP NUMBER = 7 */
      if ( xdr_status )
      {
        xdr_op_number  = 7;
        xdr_status = XDR_SEND_BYTES(clnt, input, length_unsigned_long_int);
      }
    } else {
      length_unsigned_long_int = 0;

      xdr_status = XDR_SEND_UINT32( clnt, &length_unsigned_long_int );
    }
  }

  /* XDR OP NUMBER = 8 */
  if ( xdr_status )
  {
    xdr_op_number = 8;
    /* Send TRUE if out_len is not NULL and FALSE otherwise so that the server
     * can correctly allocate memory for the service to use
     */
    output_pointer_not_null = (out_len != NULL);

    xdr_status = XDR_SEND_UINT8( clnt, &output_pointer_not_null );
  }

  /* XDR OP NUMBER = 9 */
  if ( xdr_status )
  {
    xdr_op_number = 9;
    /* Send TRUE if output is not NULL and FALSE otherwise so that the server
     * can correctly allocate memory for the service to use. Also since this is
     * a pointer to a variable array, we need to send the maximum size of the
     * array too.
     */
    output_pointer_not_null = (output != NULL);

    xdr_status = XDR_SEND_UINT8( clnt, &output_pointer_not_null );

    if ( xdr_status && output_pointer_not_null ) {
      length_uint32 = OEM_RAPI_MAX_SERVER_OUTPUT_BUFFER_SIZE;

      xdr_status = XDR_SEND_UINT32( clnt, &length_uint32 );

    }
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    /*lint -save -e123*/
    XDR_RECV_POINTER( clnt, &out_len, XDR_RECV_UINT32, xdr_status );
    /*lint -restore */
  }

  /* XDR OP NUMBER = 3 */
  if ( xdr_status )
  {
    xdr_op_number = 3;
    xdr_status = XDR_RECV_UINT32( clnt, &length_unsigned_long_int );

    /* XDR OP NUMBER = 4 */
    if ( xdr_status && length_unsigned_long_int > 0 )
    {
      xdr_op_number  = 4;
      if ( output != NULL ) {
        xdr_status = XDR_RECV_BYTES(clnt, output, length_unsigned_long_int);
      }
    }
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
        XDR_OP_ERR( clnt, xdr_op_number );
    return;
  }
} /* oem_rapi_streaming_cb_type_clnt_0x00010001 */


