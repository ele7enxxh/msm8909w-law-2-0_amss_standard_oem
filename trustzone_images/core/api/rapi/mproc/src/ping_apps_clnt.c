/* PING_APPS TOOL VERSION: 4.33 */
/*=============================================================================
                        P I N G _ A P P S _ C L N T . C

GENERAL DESCRIPTION
  This is an AUTO GENERATED file that provides RPC client functionality for the
  ping_apps API.

  ---------------------------------------------------------------------------
  Copyright (c) 2009 Qualcomm Technologies Incorporated.
  All Rights Reserved. QUALCOMM Proprietary and Confidential.
  ---------------------------------------------------------------------------
=============================================================================*/

/*=============================================================================

                              Edit History

                             AUTO GENERATED                                  */
/* Generated by following versions of Htorpc modules:
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/htorpc.pl#4 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Start.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Htoxdr.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/XDR.pm#2 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Output.pm#4 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Parser.pm#1 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/Metacomments.pm#3 
Id: //source/qcom/qct/core/mproc/tools/rel/04.07/htorpc/lib/Htorpc/SymbolTable.pm#2  
=============================================================================*/
/*=============================================================================
$Header: //components/rel/tz.bf/2.5.c10/trustzone_images/core/api/rapi/mproc/src/ping_apps_clnt.c#1 $ 
=============================================================================*/


/* Include files */
#include "oncrpc.h"
#include "ping_apps.h"
#include "ping_apps_rpc.h"

/* Only one copy needed per API */
static opaque_auth Ping_appsCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth Ping_appsVerf = { ONCRPC_AUTH_NONE, 0, 0 };

//static opaque_auth Ping_appscbCred = { ONCRPC_AUTH_NONE, 0, 0 };
static opaque_auth Ping_appscbVerf = { ONCRPC_AUTH_NONE, 0, 0 };


/*=======================================================================
             Prototypes for the API Callbacks RPC Server Functions
=======================================================================*/
  
static void ping_appscbprog_0x00010001 ( struct svc_req *rqstp, xdr_s_type *srv );

static void ping_apps_data_cb_f_type_svc_0x00010001( xdr_s_type *srv );

static void ping_apps_cb_f_type_svc_0x00010001( xdr_s_type *srv );


/*=======================================================================
             API RPC Clients Implementation
=======================================================================*/

boolean ping_apps_null(void)
{
  xdr_s_type       *clnt;
  rpc_reply_header  reply_header;
  boolean           xdr_status = TRUE;
  int               xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_NULL_VERS, 0);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(PING_APPSPROG, PING_APPS_NULL_VERS, 0);
    return FALSE;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( ! xdr_call_msg_start( clnt,
                             PING_APPSPROG, PING_APPS_NULL_VERS,
                             ONCRPC_PING_APPS_NULL_PROC, &Ping_appsCred, &Ping_appsVerf ) ) {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return FALSE;
  }
  /* XDR OP NUMBER = 1 */

  if ( reply_header.stat != RPC_MSG_ACCEPTED )
  {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }
  else if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS )
  {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 2 */
    xdr_op_number = 2;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }
  else
  {
    return TRUE;
  }
} /* ping_apps_null */

boolean ping_apps_rpc_glue_code_info_remote
(
  uint32 *toolvers,
  uint32 *features,
  uint32 *proghash,
  uint32 *cbproghash
)
{
  xdr_s_type       *clnt;
  rpc_reply_header  reply_header;
  uint32            result;
  boolean           xdr_status = TRUE;
  int               xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_RPC_GLUE_CODE_INFO_REMOTE_VERS, 0);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(PING_APPSPROG, PING_APPS_RPC_GLUE_CODE_INFO_REMOTE_VERS, 0);
    return FALSE;
  }
  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( ! xdr_call_msg_start( clnt,
                             PING_APPSPROG, PING_APPS_RPC_GLUE_CODE_INFO_REMOTE_VERS,
                             ONCRPC_PING_APPS_RPC_GLUE_CODE_INFO_REMOTE_PROC, &Ping_appsCred, &Ping_appsVerf ) ) {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    // return here without calling XDR_MSG_DONE
    return FALSE;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  if ( reply_header.stat != RPC_MSG_ACCEPTED )
  {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }
  else if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS )
  {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 2 */
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }

  if ( xdr_status )
  {
    if ( toolvers != NULL )
    {
      *toolvers = result;
    }
    /* XDR OP NUMBER = 3 */
    xdr_op_number = 3;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }
  if ( xdr_status )
  {
    if ( features != NULL )
    {
      *features = result;
    }
    /* XDR OP NUMBER = 4 */
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }

  if ( xdr_status )
  {
    if ( proghash != NULL )
    {
      *proghash = result;
    }
    /* XDR OP NUMBER = 5 */
    xdr_op_number = 5;
    xdr_status = XDR_RECV_UINT32( clnt, &result );
  }
  if ( xdr_status )
  {
    if ( cbproghash != NULL )
    {
      *cbproghash = result;
    }
    /* XDR OP NUMBER = 6 */
    xdr_op_number = 6;
  }

  if ( xdr_status )
  {
    /* XDR OP NUMBER = 7 */
    xdr_op_number = 7;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return FALSE;
  }
  else
  {
    return TRUE;
  }
} /* ping_apps_rpc_glue_code_info_remote */

boolean ping_apps_rpc_glue_code_info_local
(
  uint32 *toolvers,
  uint32 *features,
  uint32 *proghash,
  uint32 *cbproghash
)
{
  if ( toolvers != NULL )
  {
    *toolvers = PING_APPS_TOOLVERS; /* 4.33 */
  }

  if ( features != NULL )
  {
    *features = PING_APPS_FEATURES; /* ONCRPC Server Cleanup Support */
                
  }

  if ( proghash != NULL )
  {
    *proghash = 0x00010001; /* 0x00010001 */
  }

  if ( cbproghash != NULL )
  {
    *cbproghash = 0x00010001; /* 0x00010001 */
  }

  return TRUE;
} /* ping_apps_rpc_glue_code_info_local */

unsigned long ping_apps_register(ping_apps_cb_f_type ping_apps_cb_func,  int num)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  unsigned long ping_apps_register_result;

  do {
    clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_REGISTER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(PING_APPSPROG, PING_APPS_REGISTER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               PING_APPSPROG, PING_APPS_REGISTER_VERS,
                               ONCRPC_PING_APPS_REGISTER_PROC, &Ping_appsCred, &Ping_appsVerf );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) ping_apps_cb_func );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_INT( clnt, &num );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &ping_apps_register_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return ping_apps_register_result;
} /* ping_apps_register */

unsigned long ping_apps_un_register(ping_apps_cb_f_type ping_apps_cb_func)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  unsigned long ping_apps_un_register_result;

  do {
    clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_UN_REGISTER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(PING_APPSPROG, PING_APPS_UN_REGISTER_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               PING_APPSPROG, PING_APPS_UN_REGISTER_VERS,
                               ONCRPC_PING_APPS_UN_REGISTER_PROC, &Ping_appsCred, &Ping_appsVerf );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) ping_apps_cb_func );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &ping_apps_un_register_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return ping_apps_un_register_result;
} /* ping_apps_un_register */

uint32 ping_apps_data(uint32 *data,  uint32 size_words)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 i;
  uint32 length_uint32;
  uint32 ping_apps_data_result;

  boolean xdr_status = TRUE;
  int xdr_op_number = 0;

  clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_DATA_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

  if ( clnt == NULL ) {
    XDR_CLNT_LOOKUP2_ERR(PING_APPSPROG, PING_APPS_DATA_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    return 0;
  }

  oncrpcxdr_mem_free( clnt );

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_call_msg_start( clnt,
                                   PING_APPSPROG, PING_APPS_DATA_VERS,
                                   ONCRPC_PING_APPS_DATA_PROC, &Ping_appsCred, &Ping_appsVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    if ( data != NULL ) {
      length_uint32 = size_words;

      xdr_status = XDR_SEND_UINT32( clnt, &length_uint32 );

      /* XDR OP NUMBER = 3 */
      if ( xdr_status )
      {
        xdr_op_number  = 3;

        /* Calling array of XDR routines */
        for ( i = 0; xdr_status && i < (length_uint32); i++ ) {
          /*lint -save -e545*/
          xdr_status = XDR_SEND_UINT32( clnt, &(data[i]) );
          /*lint -restore */
        }
      }
    } else {
      length_uint32 = 0;

      xdr_status = XDR_SEND_UINT32( clnt, &length_uint32 );
    }
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_SEND_UINT32( clnt, &size_words );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR(clnt, xdr_op_number);
    return 0;
  }

  /* Send the RPC message and block waiting for a reply */
  if ( ! XDR_MSG_SEND( clnt, &reply_header ) )
  {
    XDR_MSG_SEND_ERR( clnt, &reply_header );
    return 0;
  }
  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR( clnt, &reply_header );
    xdr_status = FALSE;
  }

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( clnt, &ping_apps_data_result );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }

  if ( ! XDR_MSG_DONE( clnt ) || ! xdr_status )
  {
    XDR_OP_ERR( clnt, xdr_op_number );
    return 0;
  }

  return ping_apps_data_result;
} /* ping_apps_data */

unsigned long ping_apps_un_register_data_cb(ping_apps_data_cb_f_type ping_apps_data_cb_func)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  unsigned long ping_apps_un_register_data_cb_result;

  do {
    clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_UN_REGISTER_DATA_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(PING_APPSPROG, PING_APPS_UN_REGISTER_DATA_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               PING_APPSPROG, PING_APPS_UN_REGISTER_DATA_CB_VERS,
                               ONCRPC_PING_APPS_UN_REGISTER_DATA_CB_PROC, &Ping_appsCred, &Ping_appsVerf );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) ping_apps_data_cb_func );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &ping_apps_un_register_data_cb_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return ping_apps_un_register_data_cb_result;
} /* ping_apps_un_register_data_cb */

unsigned long ping_apps_register_data_cb(
  ping_apps_data_cb_f_type ping_apps_data_cb_func,
  uint32 num_callbacks,
  uint32 size_words,
  uint32 interval_ms,
  uint32 num_tasks
)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 cb_id;
  unsigned long ping_apps_register_data_cb_result;

  do {
    clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_REGISTER_DATA_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(PING_APPSPROG, PING_APPS_REGISTER_DATA_CB_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               PING_APPSPROG, PING_APPS_REGISTER_DATA_CB_VERS,
                               ONCRPC_PING_APPS_REGISTER_DATA_CB_PROC, &Ping_appsCred, &Ping_appsVerf );

    /*lint -save -e611*/
    cb_id = rpc_clnt_callback_register( (void *) ping_apps_data_cb_func );
    /*lint -restore */

    (void) XDR_SEND_UINT32( clnt, &cb_id );

    (void) XDR_SEND_UINT32( clnt, &num_callbacks );

    (void) XDR_SEND_UINT32( clnt, &size_words );

    (void) XDR_SEND_UINT32( clnt, &interval_ms );

    (void) XDR_SEND_UINT32( clnt, &num_tasks );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &ping_apps_register_data_cb_result );


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return ping_apps_register_data_cb_result;
} /* ping_apps_register_data_cb */


/*=======================================================================
             API Standard Functions for version info
=======================================================================*/

uint32 *ping_apps_api_versions(uint32 *len)
{
  xdr_s_type *clnt;
  rpc_reply_header reply_header;

  uint32 i;
  boolean output_pointer_not_null;
  void *memset_temp;
  uint32 length_uint32;
  uint32 *ping_apps_api_versions_result = NULL;

  do {
    clnt = rpc_clnt_lookup2(PING_APPSPROG, PING_APPS_API_VERSIONS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);

    if ( clnt == NULL ) {
      XDR_CLNT_LOOKUP2_ERR_FATAL(PING_APPSPROG, PING_APPS_API_VERSIONS_VERS, RPC_CLNT_LOOKUP_TIMEOUT);
    }

    oncrpcxdr_mem_free( clnt );

    XDR_ERRCHK( clnt, ON );

    (void) xdr_call_msg_start( clnt,
                               PING_APPSPROG, PING_APPS_API_VERSIONS_VERS,
                               ONCRPC_PING_APPS_API_VERSIONS_PROC, &Ping_appsCred, &Ping_appsVerf );

    /* Send TRUE if len is not NULL and FALSE otherwise so that the server
     * can correctly allocate memory for the service to use
     */
    output_pointer_not_null = (len != NULL);

    (void) XDR_SEND_UINT8( clnt, &output_pointer_not_null );


    /* Send the RPC message and block waiting for a reply */
  } while ( ! XDR_MSG_SEND( clnt, &reply_header ) );

  if ( reply_header.stat != RPC_MSG_ACCEPTED ) {
    XDR_CALL_REJECTED_ERR_FATAL( clnt, &reply_header );
  }

  if ( reply_header.u.ar.stat != RPC_ACCEPT_SUCCESS ) {
    XDR_ERR_ON_SERVER_ERR_FATAL( clnt, &reply_header );
  }

  (void) XDR_RECV_UINT32( clnt, &length_uint32 );

  if ( length_uint32 > 0 )
  {
    memset_temp = oncrpcxdr_mem_alloc( clnt, length_uint32 * sizeof( *ping_apps_api_versions_result ));
    memset(memset_temp, 0, length_uint32 * sizeof( *ping_apps_api_versions_result ));
    ping_apps_api_versions_result = memset_temp;


    /* Calling array of XDR routines */
    for ( i = 0; i < (length_uint32); i++ ) {
      /*lint -save -e545*/
      (void) XDR_RECV_UINT32( clnt, &(ping_apps_api_versions_result[i]) );
      /*lint -restore */
    }
  }

  /*lint -save -e123*/
  XDR_RECV_POINTER_NO_ERRCHK( clnt, &len, XDR_RECV_UINT32 );
  /*lint -restore */


  if ( ! XDR_MSG_DONE( clnt ) )
  {
    XDR_OP_ERR_FATAL( clnt, 0 );
  }

  XDR_ERRCHK( clnt, OFF );

  return ping_apps_api_versions_result;
} /* ping_apps_api_versions */




/*=======================================================================
             API Callbacks RPC Server Implementation
=======================================================================*/

void ping_appscb_app_init( void )
{
  (void) svc_register_auto(PING_APPSCBPROG, PING_APPSCBVERS, ping_appscbprog_0x00010001);
} /* ping_appscb_app_init */

void ping_appscb_app_lock( boolean lock )
{
  svc_lock( PING_APPSCBPROG, PING_APPSCBVERS, lock );
} /* ping_appscb_app_enable */

static void ping_appscbprog_0x00010001( struct svc_req *rqstp, xdr_s_type *srv )
{
  switch( rqstp->rq_proc ) {
    case ONCRPC_PING_APPS_DATA_CB_F_TYPE_PROC:
      ping_apps_data_cb_f_type_svc_0x00010001( srv );
      break;
    case ONCRPC_PING_APPS_CB_F_TYPE_PROC:
      ping_apps_cb_f_type_svc_0x00010001( srv );
      break;
    default:
      // invalid RPC procedure number
      (void) XDR_MSG_DONE( srv );
      svcerr_default_err( srv, rqstp, ping_apps_api_versions  );
      break;
  }

  oncrpcxdr_mem_free( srv );
} /* ping_appscbprog_0x00010001 */

static void ping_apps_data_cb_f_type_svc_0x00010001( xdr_s_type *srv )
{
  ping_apps_data_cb_f_type *cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  unsigned long int i;
  void *memset_temp;
  unsigned long int length_unsigned_long_int;
  uint32 *data = NULL;
  uint32 size_words;
  uint32 sum;

  boolean cmd_cb_func_result;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_UINT32( srv, &length_unsigned_long_int );

    /* XDR OP NUMBER = 3 */
    if ( xdr_status && length_unsigned_long_int > 0 )
    {
      xdr_op_number  = 3;
      memset_temp = oncrpcxdr_mem_alloc( srv, length_unsigned_long_int * sizeof( *data ));
      memset(memset_temp, 0, length_unsigned_long_int * sizeof( *data ));
      data = memset_temp;


      /* Calling array of XDR routines */
      for ( i = 0; xdr_status && i < (length_unsigned_long_int); i++ ) {
        /*lint -save -e545*/
        xdr_status = XDR_RECV_UINT32( srv, &(data[i]) );
        /*lint -restore */
      }
    }
  }

  /* XDR OP NUMBER = 4 */
  if ( xdr_status )
  {
    xdr_op_number = 4;
    xdr_status = XDR_RECV_UINT32( srv, &size_words );
  }

  /* XDR OP NUMBER = 5 */
  if ( xdr_status )
  {
    xdr_op_number = 5;
    xdr_status = XDR_RECV_UINT32( srv, &sum );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (ping_apps_data_cb_f_type *) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func_result = cmd_cb_func(data, size_words, sum);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Ping_appscbVerf );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_SEND_BOOLEAN( srv, &cmd_cb_func_result );
  }

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* ping_apps_data_cb_f_type_svc_0x00010001 */

static void ping_apps_cb_f_type_svc_0x00010001( xdr_s_type *srv )
{
  ping_apps_cb_f_type *cmd_cb_func;
  uint32  cb_id;
  boolean xdr_status = TRUE;
  int     xdr_op_number = 0;

  int val;


  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = XDR_RECV_UINT32( srv, &cb_id );

  /* XDR OP NUMBER = 2 */
  if ( xdr_status )
  {
    xdr_op_number = 2;
    xdr_status = XDR_RECV_INT( srv, &val );
  }

  if ( xdr_status )
  {
    xdr_op_number++;
  }      

  if ( ! XDR_MSG_DONE( srv ) || ! xdr_status ) {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_decode reply
    svcerr_decode( srv );
    return;
  }

  /*lint -save -e611*/
  cmd_cb_func = (ping_apps_cb_f_type *) rpc_clnt_callback_lookup( cb_id );
  /*lint -restore */

  if ( cmd_cb_func == NULL ) {
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  cmd_cb_func(val);

  /* XDR OP NUMBER = 1 */
  xdr_op_number = 1;
  xdr_status = xdr_reply_msg_start( srv, &Ping_appscbVerf );

  if ( ! xdr_status )
  {
    XDR_OP_ERR( srv, xdr_op_number );
    // send svcerr_systemerr reply
    svcerr_systemerr( srv );
    return;
  }

  if ( ! XDR_MSG_SEND( srv, NULL ) ) {
    XDR_MSG_SEND_ERR( srv, NULL );
  }
} /* ping_apps_cb_f_type_svc_0x00010001 */

